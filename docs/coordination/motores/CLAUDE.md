# Outbox do Claude — só Claude escreve

## ACK — MOTOR-AUTO-20260914 (responde_a: MOTOR-AUTO-20260914)

- Timestamp UTC: 2026-09-14T15:41:00Z (rotina criada às 15:38Z)
- Executor: Claude (sessão CEO-executor, app desktop, aba Code)
- Worktree/caminho da outbox: C:/kineo-wt/restaura/docs/coordination/motores/CLAUDE.md (esta). Nunca C:/kineo (árvore suja).
- Branch de código: codex/fidelidade-0914 (base origin/main a23a2a74…; docs já publicados em 7e80f691). Cópias versionadas desta outbox saem por worktree própria codex/docs-motores-claude-<hhmm> + bash scripts/enfileirar.sh + !RODAR-AGORA.bat (só docs; código de produto segue SEGURADO).
- Rotina CONFIGURADA DE VERDADE (create_scheduled_task, confirmado pelo agendador do app): taskId `motor-auto-20260914-claude`, cron `*/15 * * * *` (a cada 15 min, horário local BRT), arquivo C:/Users/josep/.claude/scheduled-tasks/motor-auto-20260914-claude/SKILL.md. Término: o prompt da rotina confere `date -u` em toda rodada e, em 2026-09-15T03:13:52Z (15/09 00:13 BRT) ou depois, grava ENCERRAMENTO nesta outbox e desativa a si mesma (update_scheduled_task enabled=false). Sem renovação automática. Cursor local `.cursor-claude` (md5 do BOARD.md): rodada sem novidade = silêncio, sem teste repetido.
- Limitação conhecida do agendador (memória da casa): se o app reiniciar, a lista de tarefas pode esvaziar; a rotina só vale enquanto `list_scheduled_tasks` a mostrar. Se sumir, eu recrio do SKILL.md e registro aqui.
- Candidato manifestado: FID-V4 = a3734c39e1f5749b2eb5c6106ff7da5ced019a95 (codex/fidelidade-0914). NÃO publicar fidelidade, NÃO gastar. Veo 5b2dc929 segurado.
- Lido: PROTOCOLO.md, BOARD.md (até FID-V4-R1), MATRIZ.md, origin/main:PEDIDOS (FIDELIDADE-V4). Documentação do Board 811403dc tratada como ENFILEIRADA, não publicada.
- Limite financeiro desta adesão: zero. Nenhum render pago, nenhum dry-run pago.

## FID-V4-R2 — PRONTO_PARA_REVISAO (responde_a: FID-V4-R1)

- Timestamp UTC: 2026-09-14T15:50:00Z · dono: Claude · motor(es): MiniMax H3 (base comum hollywood: Kling 3 / Omni / S25 passam pela mesma biblioteca)
- Branch: codex/fidelidade-0914 · SHA COMPLETO: 0d45b028db63a3c22ad3ee9997c2f70e3ba41a42 (sucede a3734c39e1f5749b2eb5c6106ff7da5ced019a95; base origin/main 7e80f69129c2afc72c64a73027d4ec33583e30a1 para docs; código sobre a23a2a74) · SEGURADO (trava 8.2). Sem publicação, sem render.
- Arquivos (só o delta): lib/hollywood/fidelidade.ts · scripts/test-fidelidade-h3-2026-09-14.mjs. (router e rota inalterados desde a3734c39.)
- Hipótese e critérios definidos ANTES de editar:
  1. "I'd"/"we'd" é ambíguo (had/would). Critério: 'had' SÓ antes de particípio (lista fechada + -ed), 'would' SÓ antes de forma base (lista fechada); qualquer outro caso → nao_suportada com texto INTACTO; testar would e had e o advérbio no meio ("I'd never seen" → "had never seen", "I'd rather die" → "would rather die").
  2. Citar o papel para declará-lo ausente não é presença. Critério: prompt com "<papel> is/was/remains absent|gone|missing|nowhere…" ou "no/without the <papel>" enquanto a narração descreve o papel → cobertura 'divergente' (motivo 'sujeito ausente'); a cláusula de ausência e o sujeito errado SAEM do prompt enviado (sujeito da frase vira o da narração) em vez de prefixar ordem oposta; a ficha NÃO é colada na cláusula de ausência; caso legítimo ("His son is alone… He grips the rail" × "The boy grips the rail") fica intacto e coberto até o submittedPrompt.
- Abordagem (sem regex por frase): a decisão 'had'/'would' é gramatical por classe da palavra seguinte (particípio × base); a ausência é uma relação (papel + verbo de estado + predicado de ausência), não uma frase.
- Comandos/resultados (14/09 ~15:48Z, C:/kineo-wt/restaura @ 0d45b028):
  · node scripts/test-fidelidade-h3-2026-09-14.mjs → 100 ok · 0 falhas (rc 0); 15 verificações novas: 6 de I'd/we'd (would/had/advérbio/nao_suportada/fim de frase), 9 de sujeito ausente (helper, garantirAcaoCentral, cadeia planejador+rota até submittedPrompt, caso legítimo).
  · node …/h3-lituya-review/audit-a3734c39.cjs C:/kineo-wt/restaura → {passed:13, failed:0, total:13} (harness do Board, só leitura, sem fornecedor).
  · npx tsc --noEmit -p tsconfig.json → rc 0.
  · vizinhos: enche-silencio 19/19, visual-contract 330, cinematic-timeline 44, primeira-pessoa 21, scene-truth rc 0, cinematic-speech 184, regua-unica 74, variety-axis rc 0.
- Casos aprovados preservados: Lituya ("My son and I…"), I see/Eu sobrevivi/Yo recuerdo/nurse, negação removida, village intact × collapsed, filho sem ficha do pai, descrição conflitante substituída, estado global, apara 7×10 s×21 palavras.
- Pendências técnicas: (a) QUESTÃO PENDENTE do Board sobre nao_suportada: mantido como limitação DECLARADA (texto intacto em 1ª pessoa, estado na cena e no claim fidelidade_cena.conversao); tratamento proposto para validação em filme: a cena marcada nao_suportada segue narrada como está, sem atribuição inventada e sem bloquear a geração; se o Board preferir devolver a cena ao formato diálogo nativo (fala do próprio personagem) quando o formato permitir apresentador, é uma decisão de produto que registro aqui e não implemento sem pedido. (b) Base EN de verbos é lista fechada (cobre o vocabulário de sobrevivência/documentário); fora dela = declarado, não fingido.
- Limite financeiro: zero. Nenhum render pago ou dry-run pago. Veo 5b2dc929 segurado.
- Próximo do executor (sem pedido novo do Board): avançar testes offline independentes dos motores clássicos (Kineo 1, Seedance 1.5, Kling 2.5) sobre o SHA da main, reconciliando gates existentes sem reconstruir.

## FID-V4-R4 — PRONTO_PARA_REVISAO (responde_a: FID-V4-R3)

- Timestamp UTC: 2026-09-14T16:13:00Z (rodada automática da rotina motor-auto-20260914-claude; primeiro disparo automático com novidade) · dono: Claude · motor(es): MiniMax H3 (base comum hollywood: Kling 3 / Omni / S25)
- Branch: codex/fidelidade-0914 · SHA COMPLETO: 83aeef34f445cfc3c83610c5997e43909bbce6b7 (sucede 0d45b028db63a3c22ad3ee9997c2f70e3ba41a42; código sobre a23a2a74; docs contra origin/main c55b9c762834ee24c52791ad89a1fa6c0712765d) · SEGURADO (trava 8.2). Sem publicação, sem render.
- Arquivos (só o delta): lib/hollywood/fidelidade.ts · scripts/test-fidelidade-h3-2026-09-14.mjs. (router e rota inalterados desde a3734c39.)
- Hipótese e critérios definidos ANTES de editar:
  1. had/would: `auxiliarDeD` dava prioridade ao particípio; "come/run/put" estão nas DUAS tabelas. Critério: a decisão é por CLASSE e as classes não se sobrepõem — palavra na interseção base ∩ particípio → null → nao_suportada com texto INTACTO; nenhuma classe tem prioridade; "-ed" só sugere particípio quando a palavra NÃO está na base ("need" → would). A propriedade é provada sobre a interseção inteira (exportada), não sobre três verbos. Exclusivos preservados: escape → would, seen → had.
  2. ausência: o salto de 220 caracteres atravessava oração e pessoa. Critério: a ausência só se liga ao papel quando ele é SUJEITO DA MESMA ORAÇÃO — entre o papel e o predicado de ausência, no máximo 4 palavras, nenhuma conector (while/and/but/as/because/…/when/who/that/if/or), nenhuma outra pessoa/pronome (son/boy/crew/he/his/…), nenhuma vírgula/ponto-e-vírgula. "The fisherman grips the wheel while his son is missing" = filho desaparecido: sem ausência, coberta (grips/grips), prompt INTACTO até o submittedPrompt. Remoção vira cirúrgica: só a oração do papel sai; "while his son grips the wheel" fica e o sujeito vira o da narração. Relação não compreendida ("The fisherman, however, is absent") NÃO é removida por suspeita. "is absent" / "nowhere to be seen" / "without the fisherman" continuam reconhecidos.
- Abordagem: duas restrições de relação (classe exclusiva; sujeito da mesma oração), sem regex por frase e sem vocabulário novo para vencer o próximo exemplo.
- Comandos/resultados (14/09 16:08–16:12Z, C:/kineo-wt/restaura @ 83aeef34):
  · node scripts/test-fidelidade-h3-2026-09-14.mjs → 117 ok · 0 falhas (rc 0); 17 verificações novas (7 de had/would incl. a propriedade da interseção = 11 palavras; 10 de ausência incl. cadeia planejador+rota até o submittedPrompt).
  · falsificação por mutante (aplicado, provado por grep, restaurado): (A) interseção sem null → 5 vermelhos; (B) lookahead de conector/pessoa removido → 1 vermelho; (C) teto de 4 palavras → 40 → verde (o lookahead segura sozinho); B+C juntos → 5 vermelhos (o caso do Board cai). Duas guardas independentes cobrem a frase do Board.
  · node …/h3-lituya-review/audit-0d45b028.cjs C:/kineo-wt/restaura → {passed:18, failed:0, total:18} (harness do Board, só leitura).
  · npx tsc --noEmit -p tsconfig.json → rc 0.
  · vizinhos: enche-silencio 19/19, visual-contract 330, cinematic-timeline 44, primeira-pessoa 21, scene-truth rc 0, cinematic-speech 184, regua-unica 74, variety-axis rc 0.
- Casos fechados preservados (não reabertos): Lituya, I see/Eu sobrevivi/Yo recuerdo/nurse, "I'd escape"/"I'd seen", negação removida, village intact × collapsed, filho sem ficha do pai, descrição conflitante, estado global, apara 7×10 s×21 palavras, "Only his son…; the fisherman is absent".
- Pendências técnicas: (a) limitação declarada: papel separado do predicado por vírgula ("The fisherman, however, is absent") ou por mais de 4 palavras não é reconhecido como ausência — fica no prompt e a cobertura segue pela ação (declarada, não removida por suspeita). (b) QUESTÃO PENDENTE do R1 (nao_suportada mantém 1ª pessoa) segue como registrado no R2: tratamento = narração intacta + estado no claim; validação só em filme.
- Limite financeiro: zero. Nenhum render pago ou dry-run pago. Veo 5b2dc929 segurado.
- Próximo do executor (sem pedido novo do Board): testes offline independentes dos motores clássicos (Kineo 1, Seedance 1.5, Kling 2.5) sobre o SHA da main; nada de código de produto novo enquanto o R4 está em revisão.

## CLASSICOS-R1 — PRONTO_PARA_REVISAO (responde_a: FID-V4-R5)

- Timestamp UTC: 2026-09-14T16:28:00Z (rodada automática motor-auto-20260914-claude) · dono: Claude · motor(es): Kineo 1, Seedance 1.5, Kling 2.5 (caminho clássico; Veo 3.1 aparece só onde o mesmo guardião o cobre de graça — 5b2dc929 segue SEGURADO)
- Recebido: FID-V4-R5 GO_TECNICO para 83aeef34f445cfc3c83610c5997e43909bbce6b7. Estado do candidato aqui: GO_TECNICO · SEGURADO (trava 8.2). Publicação depende exclusivamente do fundador ('publica fidelidade 83aeef34'); registrado no PEDIDOS nesta mesma leva. Nenhum código de produto foi tocado nesta rodada.
- Branch: nenhuma de código. Reconciliação feita em worktree DETACHED no SHA IMUTÁVEL da main: c6cac456c6c92c355b48634e004c1e0a4ac293e8 (C:/kineo-wt/classicos-c6cac456, junction de node_modules, removida ao fim). Base = o próprio origin/main.
- Arquivos: nenhum alterado. Só docs/coordination/motores/CLAUDE.md (esta outbox) e docs/PEDIDOS-ENTRE-PISTAS-2026-09-03.md (pedido de publicação atualizado para o SHA aprovado).
- Hipótese e critérios definidos ANTES de rodar: (H1) os guardiões existentes do caminho clássico, executados no SHA da main sem edição, continuam verdes — vermelho novo = regressão a abrir; vermelho já presente em docs/SUITE-MOTORES-2026-09-14.md (baseline 12:40 em bcff1868) = herdado, registrar causa e NÃO reabrir aqui. (H2) a matriz motor × entrada × duração declara NÃO VALIDADO onde nenhum guardião executa o caso — não se soma suíte sobreposta como cenário exclusivo, e nada offline vira validação audiovisual.
- Comandos (16:24–16:27Z, em c6cac456): 43 guardiões `node scripts/<g>.mjs` com rc capturado; `npx tsc --noEmit -p tsconfig.json` → rc 0.
  · 37 verdes (rc 0): kineo1-cinema 23 · qualidade-classicos 29 · dryrun-classico 18 · contrato-duracao-classico 13 · duracao-que-nao-existe · duration-floor-39 102 · regua-unica-e-entrega-medida 74 · narration-ruler · preflight-que-nao-acusa 28 · velocidade 23 · vigia-palavras-por-cena 26 · tres-modos-clipe 30 · brief-e-ideia-colada 44 · motores-r2-r6 · caption-chunker · music-direction 112 · scene-retry-mutex 258 · roteiro-longo-nao-e-erro · silencio-na-cena 16 · cinematic-speech 184 · cinematic-timeline 44 · scene-truth 63 · variety-axis 23 · primeira-pessoa 21 · enche-silencio 19 · visual-contract 330 (38 simulações do chamador clássico real: SEEDANCE/VEO/KLING/SORA) · entrega-noite 23 · entrega-noite-r5 · entrega-noite-r6 55 · dispatch-entry 24 · tentativa-perdida-ve-o-fast · rescue-composed-films 19 · stranded-authorized-urls 13 · capacidade-em-filmes 45 · voz-na-boca 39 · chatgpt-script-handoff 171 · recusa-com-nome 61.
  · 6 vermelhos, TODOS já listados como herdados no SUITE-MOTORES-2026-09-14 (nenhum novo): multiformato 43/45 (2 âncoras envelhecidas: `buildFalInput` ganhou `classicVisualMode` como último parâmetro em 11/09; a PROPRIEDADE segue válida — route.ts:4900 despacha `aspectRequested` no clássico) · motores-d1 34/35 (aspect literal do S25, não é clássico) · leva-confiabilidade 35/39 · guardiao-2026-08-28 19/20 (painel) · recusa-nao-e-tente-de-novo 26/32 (copy da recusa `narration_too_short`, âncoras de constante) · roteiro-perdido (morre no import: `export type`). Não reabertos; ficam nomeados para a leva de higiene de guardiões.
- MATRIZ OFFLINE (o que cada célula PROVA por guardião no SHA c6cac456; entrada = ideia / roteiro próprio verbatim / brief; durações do produto 35·60·90):
  · KINEO 1 (fast + compose, Pixabay, voz clássica 3,1 pal/s) — ideia 35/60/90: escritor entrega a faixa de palavras 105/186 (qualidade-classicos), 35→100-115 · 60→175-195 · 90→265-290 (contrato-duracao, regua-unica §1), 3ª passada coerência fala×legenda (regua-unica §6b), b-roll por cena e formato (kineo1-cinema, multiformato, tres-modos-clipe), legenda sem perder palavra (caption-chunker), música (music-direction), entrega grava duração MEDIDA e método (regua-unica §5/§7) — VERDE offline. Roteiro próprio 35/60/90: "as is" literal no compose (qualidade-classicos §3), roteiro curto → recusa com saída ANTES do gasto, nunca encurtado em silêncio (regua-unica §4/§6, duracao-que-nao-existe, duration-floor), >12 blocos recusa explícita, dry-run só autorizado (regua-unica §9, dryrun-classico) — VERDE offline. Brief: brief-e-ideia-colada 44 — VERDE. Idioma: voz na língua do texto EN/PT/ES executada (qualidade-classicos §4, "Tomás" não vira "ás") — VERDE offline. Confiabilidade: dispatch-entry, tentativa-perdida-ve-o-fast, rescue/stranded, capacidade — VERDE. NÃO VALIDADO: sincronia de legenda no ARQUIVO, escuta de voz/música, cota grátis com corte de 15 s medida em arquivo.
  · SEEDANCE 1.5 (cinematic clássico, clipes de 10 s, voz clássica 3,1) — ideia 35/60/90: faixa de palavras 74/186 (qualidade-classicos), portão/degrau/dry-run com a MESMA régua (regua-unica §3), dry-run $0 no clássico com resposta nomeando motor + modo visual + contrato de cena (dryrun-classico), silêncio por cena >1,5 s / >8 s reprova (silencio-na-cena), estilo travado e fecho sem repetição (motores-r2-r6), contrato visual executado no chamador real SEEDANCE_MODEL (visual-contract), scene-truth/variety-axis no gate — VERDE offline. Roteiro próprio 35/60/90: verbatim não reescrito, roteiro longo não é erro (roteiro-longo-nao-e-erro, chatgpt-script-handoff 171), régua de silêncio idem — VERDE offline. Brief: brief-e-ideia-colada — VERDE. Idioma: PT/ES na voz (qualidade-classicos); prompt visual em EN por regra do escritor — declarado, não testado em filme. Confiabilidade: retry/mutex por cena (scene-retry-mutex 258), recusa com nome (recusa-com-nome 61), estorno/claim (dispatch-entry) — VERDE. NÃO VALIDADO: qualquer célula em vídeo; 90 s só na aritmética do plano (contrato-duracao, duration-floor), sem arquivo medido.
  · KLING 2.5 (mesmo caminho clássico, 10 s por clipe, âncora i2v opcional) — ideia/roteiro/brief 35/60/90: TUDO que vale para o Seedance acima (mesmas funções, mesmo portão) + âncora: clipe real usa o still (i2v), 422 do i2v cai para t2v, 500 ambíguo não duplica POST (visual-contract linhas KLING_MODEL/KLING_I2V_MODEL) — VERDE offline. Sem guardião com nome próprio do Kling 2.5: a cobertura é a do caminho compartilhado, dita como tal. NÃO VALIDADO: identidade/escala renderizadas, fala nativa (não se aplica: voz é TTS nosso), 90 s em arquivo.
  · Transversal (os três): formato 9:16/16:9/1:1 no despacho (multiformato, propriedade conferida no código apesar da âncora velha) · legendas: chunker preserva toda palavra falada; posição/leitura no arquivo NÃO VALIDADAS · música: direção 112/112 offline; escuta NÃO VALIDADA · tsc 0 no SHA.
- Pendências técnicas: (a) 6 vermelhos herdados nomeados acima — higiene de guardiões (âncoras envelhecidas; `export type` fora de módulo), não regressão de produto; abrir só com pedido. (b) Nenhum guardião mede 90 s em ARQUIVO para clássico — a prova de 90 continua na aritmética do plano + entrega medida (regua-unica §5 grava a duração real quando existir filme). (c) Kling 2.5 sem guardião nominal; se o Board quiser célula própria, o caminho é estender visual-contract/dryrun-classico com caso nomeado, não biblioteca nova.
- Limite financeiro: zero. Nenhum render pago, nenhum dry-run pago, nenhuma escrita em banco. Veo 5b2dc929 segurado.
- Próximo do executor (sem pedido novo): aguardar a revisão da matriz; se o Board apontar célula sem prova, acrescentar caso nomeado ao guardião existente correspondente (offline). Publicação de fidelidade e novo H3 Lituya: só com a palavra do fundador.

## CLASSICOS-R3 — PRONTO_PARA_REVISAO (responde_a: CLASSICOS-R2)

- Timestamp UTC: 2026-09-14T17:05:00Z (rodada automática motor-auto-20260914-claude) · dono: Claude · motor(es): Kling 2.5 (evidência nominal nova), Kineo 1 e Seedance 1.5 (matriz reclassificada) · Veo 3.1 só onde o guardião compartilhado o cobre (5b2dc929 segue SEGURADO)
- Branch: codex/classicos-kling25-0914 · SHA COMPLETO: b681b0d84ad01bb5cf8bad47cad216178f738e5e · base origin/main 6b5cccea3b67a6fcf5e4b5327987b4a1e6ea5c74 · SEGURADO (só docs vão pela fila nesta rotina). Matriz medida no SHA imutável c6cac456 (worktree C:/kineo-wt/classicos-c6cac456, removida ao fim); reconciliação de vermelhos também em bcff1868 (worktree C:/kineo-wt/classicos-bcff1868, removida).
- Arquivos (só o delta): scripts/test-visual-contract-2026-09-11.mjs (+131/−3). Nenhum arquivo de produto tocado; fidelidade 83aeef34 intacta e separada.
- Hipótese e critérios definidos ANTES de editar (pedido 2 do Board): (H) o guardião existente prova cenas→payload do Kling 2.5 mas não entrada→cenas; a ligação que falta é o bloco de decisão + o construtor de cenas da rota. Critérios: (a) os 3 casos a 60 s executam o CÓDIGO REAL da rota localizado por AST — bloco `parsedScript…verbatim` (route.ts:1439-1462), `clipCountForDuration` (route.ts:840) + re-dimensionamento verbatim (route.ts:2853-2864), construtor `if (verbatim) {resolveVerbatimSegments} else {generateScenes}` (route.ts:2892-2919) — e depois o chamador clássico real (`contratoRelatoClassico`…`submitScene`, route.ts:4870-4910) até `buildFalInput` (route.ts:651-660); (b) mocks só em OpenAI/fal e em speechRateFor/writeServerEvent (não é a propriedade); (c) ideia → planner 1×, 0 split, 7 cenas, payload t2v `fal-ai/kling-video/v2.5-turbo/pro/text-to-video` com duration '10', aspect_ratio '9:16', cfg 0.6, seed 17, negative_prompt, sem image_url; (d) roteiro próprio (186 palavras, faixa 175-195) → verbatim, 0 planner, 1 split, voiceovers concatenados = texto do autor palavra por palavra, clipCount acima do botão; (e) brief (ficha + 2 instruções, script_mode verbatim) → rebaixado a IA com evento `brief_detected_ai_mode{engine:'kling'}`, brief inteiro chega ao planner, nenhuma linha de ficha narrada; (f) mesma ideia com âncora → i2v com still. Sem biblioteca nova: `run()` ganhou `scenesOverride` (todas as chamadas antigas inalteradas).
- Comandos/resultados (16:55–17:03Z, C:/kineo-wt/classicos-c6cac456 @ b681b0d8):
  · node scripts/test-visual-contract-2026-09-11.mjs → 365 ok (era 330; +35), 42 simulações do chamador real (era 38), rc 0.
  · falsificação por mutante NA ROTA (aplicado, provado por grep, restaurado por git checkout): (A) `(userSaysVerbatim && !briefDetected)` → `(userSaysVerbatim)` = vermelho "Brief: … demoted to AI mode"; (B) resolveVerbatimSegments alimentado com texto do planner = vermelho no roteiro; (C) `aspect_ratio: '16:9'` literal NO RAMO Kling (a 1ª tentativa mutou a linha 517 de outro motor e ficou verde — mutante mal endereçado, refeito na 655) = vermelho "Kling 2.5 payload carries duration/aspect/cfg/seed"; (D) `const sized = clipCount` = vermelho "186 words re-size the clip count above the 60 s button (7 → 7)"; (E) `looksLikeBrief(prompt)` → `false` = vermelho no locator (a propriedade do brief já morde pelo A).
  · npx tsc --noEmit -p tsconfig.json → rc 0.
  · vizinhos no SHA: enche-silencio 19, cinematic-timeline 44, primeira-pessoa 21, scene-truth rc 0, cinematic-speech 184, regua-unica 74, variety-axis rc 0, dryrun-classico 18, qualidade-classicos 29, contrato-duracao 13, kineo1-cinema 23, tres-modos-clipe 30, brief-e-ideia-colada 44, silencio-na-cena 16 — todos rc 0.
- ACHADOS DECLARADOS pela cadeia real (não escondidos na asserção; nenhum é regressão nova, ambos estavam no código): (1) o laço de âncoras tem `MAX_ANCHORED_SCENES = 6` (route.ts:4810) — um filme de 60 s no Kling 2.5 planeja 7 cenas e a 7ª vai t2v sem still (o teste afirma 6 stills / 7 cenas); (2) o re-dimensionamento verbatim usa `WORDS_PER_SECOND = 2.5` (route.ts:2855), não a régua da família (3,1) — viés declarado para MAIS clipes (186 palavras → 8), nunca abaixo do botão; a régua única de 14/09 não passou por ali. Registro, não conserto (fora do delta pedido).

### Pedido 3 — vermelhos herdados RECONCILIADOS (mesmo teste, mesmo ambiente, dois SHAs)
Rodei os 6 em bcff1868 (base do SUITE-MOTORES) e em c6cac456, mesmo node/node_modules: modo de falha IDÊNTICO nos dois SHAs. multiformato = 43 ok / 2 FAIL nos dois (`buildFalInput recebe o formato como ÚLTIMO parâmetro`; `o caminho clássico manda o formato no despacho`). A linha 70 do SUITE ("morre no import") é ARTEFATO DO RUNNER, não outro modo de falha: o SUITE captura a primeira linha com `✗` e este guardião imprime `FAIL` (linha 19 do teste: `console.log('  FAIL ' + n)`), então caiu no rótulo padrão; o arquivo tem um único import (`node:fs`, linha 12). Os outros cinco: guardiao-2026-08-28 19/1 (`live: a coluna soma as 4 fontes`) · leva-confiabilidade 35/4 (relinked no cron; free_duration_clamped via composeAdmin; POST só {source}; snapshot_time) · motores-d1 34/1 (`aspect_ratio 9:16 explicito no t2v` — S25, não clássico) · recusa-nao-e-tente-de-novo 26/6 (B2/B3/B5/C1/C2/C10, constante da recusa) · roteiro-perdido 0/0 (`SyntaxError: Unexpected token 'export'`, morre antes da 1ª verificação — este sim é import). Baseline fechado: nenhum é regressão entre os dois SHAs; nenhum prova defeito de produção; correção fica para a leva de higiene, só com pedido.

### Pedido 1 — MATRIZ RECLASSIFICADA (SHA c6cac456; classes: ESTRUTURAL = lê o fonte e confere texto/aritmética · HELPER EXECUTADO = função real transpilada e chamada isolada · CAMINHO REAL COM MOCKS = fatia real da rota executada, I/O mockado · ARQUIVO/AUDIO = mede filme/áudio real). "Compartilhado" = a mesma prova vale para os três clássicos; NÃO significa que cada combinação motor×entrada×duração rodou.

**Kineo 1 (generate-video-fast + compose)**
· ideia 60 s — CAMINHO REAL COM MOCKS: regua-unica §6 (linhas 78-127) executa a fatia real do portão/dry-run/degrau do fast (`dryRunAutorizado`…`return scenes`) com segmentos sintéticos de 5×10 palavras e speechRateFor REAL (SRX, linha 27); §6b (128-146) executa a fatia real da terceira passada com expansor mockado (180→279 palavras, legenda acompanha, erro não toca a cena). Parâmetros: verbatim/ai, EN, durações 35/60/90 pela função `largestFittingDuration` mockada. HELPER EXECUTADO: qualidade-classicos 25-32 (detectNarrationLanguage/resolveNarrationLanguage reais em ES/PT/EN — "detecção e encaminhamento de idioma testados", NÃO voz executada); 44-61 expansão com resposta MOCKADA (prova a colagem, não que um escritor real entregou a faixa); regua-unica §1-2 (speechRate/narrationFit reais). ESTRUTURAL: contrato-duracao 7-23 (strings + autofit REIMPLEMENTADO no teste), kineo1-cinema (só readFileSync: letterbox/fade/ranking/corte 4,5 s), duration-floor-39, multiformato (propriedade do `aspectRequested` conferida no fonte), qualidade-classicos 37-40 e 65-68 ("as is" literal = regex sobre compose/fast, não execução).
· roteiro próprio 35/60/90 — CAMINHO REAL COM MOCKS: regua-unica §4 (47-61) e §6 (recusa curta ANTES do gasto; >12 blocos recusa explícita; dry-run só autorizado — executado). HELPER EXECUTADO: brief-e-ideia-colada 24-50 (parseUserScript/looksLikeBrief reais; três modos). ESTRUTURAL: qualidade-classicos 65-68 (claimVerbatim/escalador pulado — regex).
· brief — HELPER EXECUTADO: brief-e-ideia-colada (looksLikeBrief real + espelho Lite; 44 verificações). Caminho da rota fast com brief: ESTRUTURAL apenas.
· legenda — caption-chunker: algoritmo REIMPLEMENTADO no teste (funções `chunk`/`normalizeCaptionWord` copiadas, linhas 54-98), NÃO executa lib/compose — rebaixado de "VERDE" para ESTRUTURAL-cópia. · música — music-direction: HELPER EXECUTADO (função real do provedor com fetch mockado, linha 99+; inspeciona o POST real) · entrega medida — regua-unica §5 (mp4Duration real sobre bytes sintéticos, linha 72) e §7 (fatia real do evento de entrega, 147-183) = CAMINHO REAL COM MOCKS.
· ARQUIVO/AUDIO: NENHUMA célula. NÃO VALIDADO em arquivo: voz, sincronia de legenda, música, cota grátis de 15 s.

**Seedance 1.5 (generate-video-cinematic, caminho clássico)**
· ideia/roteiro/brief 60 s — CAMINHO REAL COM MOCKS: visual-contract 88-160 (fatia real `contratoRelatoClassico`…`submitScene` com SEEDANCE_MODEL × 3 modos visuais × frame 16:9; payload sem negative_prompt, seed 17, sem áudio nativo — linhas 139-155); 228-262 (generateScenes REAL com OpenAI mockado, hash byte-idêntico do payload legado; contrato de escritor só no clássico, 271-289). NOVO em b681b0d8: a cadeia entrada→payload (visual-contract 300-421) roda no Kling 2.5 — a fatia de decisão/construtor é a MESMA para Seedance (só `model` muda), mas eu NÃO a rodei com SEEDANCE_MODEL: cobertura compartilhada DECLARADA, não executada. Durações 35/90: ESTRUTURAL (contrato-duracao, duration-floor).
· portão/degrau/dry-run — regua-unica §3 (40-46) = ESTRUTURAL (regex sobre a rota; a execução do portão é só no fast, §6); dryrun-classico 20-37 = HELPER EXECUTADO (`classicDryRunReport` real em cenários sintéticos), 41-63 = ESTRUTURAL (ligação às rotas). §9 (184-215) = ESTRUTURAL + fatia de rejeição executada.
· silêncio por cena — silencio-na-cena: HELPER EXECUTADO (timelineContract real, 16). · estilo/fecho — motores-r2-r6: HELPER EXECUTADO. · retry/mutex — scene-retry-mutex 258: HELPER EXECUTADO + 1 import de lib. · recusa com nome — recusa-com-nome 61: HELPER EXECUTADO + ESTRUTURAL.
· idioma — qualidade-classicos 25-32 (detecção/encaminhamento testados; prompt visual em EN por regra do escritor = ESTRUTURAL). · ARQUIVO/AUDIO: NENHUMA.

**Kling 2.5 (mesmo caminho; âncora i2v opcional)**
· ideia 60 s — CAMINHO REAL COM MOCKS (NOVO, b681b0d8): visual-contract 351-360: decisão real → 7 clipes (`clipCountForDuration(60)`) → generateScenes real com OpenAI mockado (1 chamada, a ideia chega ao request) → chamador real → payload `fal-ai/kling-video/v2.5-turbo/pro/text-to-video` {duration:'10', aspect_ratio:'9:16', cfg_scale:0.6, seed:17, negative_prompt, sem image_url}, prompt com sujeito e orientação ≤7000.
· roteiro próprio 60 s — CAMINHO REAL COM MOCKS (NOVO): 362-374: 186 palavras → verbatim (não brief), 0 planner, 1 split real, voiceovers = texto do autor palavra por palavra, clipCount 7→8, payload idem, prompt sem aspas.
· brief 60 s — CAMINHO REAL COM MOCKS (NOVO): 376-384: rebaixado a IA pelo bloco real, evento com engine 'kling', ficha nunca narrada, payload idem.
· âncora — 386-395 (NOVO) + 161-190 (existente): i2v `fal-ai/kling-video/v2.5-turbo/pro/image-to-video` com image_url, duration '10', sem aspect param; still 9:16 com o sujeito; 6 stills para 7 cenas (teto real); 422 do i2v cai para t2v (185-187), 500 ambíguo não duplica POST (188-189), 400/422 uma retentativa segura (171-176).
· 35/90 s — ESTRUTURAL/compartilhado (contrato-duracao, duration-floor); não executado. · ARQUIVO/AUDIO: NENHUMA.

**Veo 3.1** — visual-contract 134-156 executa o chamador real com VEO_MODEL (safety_tolerance '5', seed) de graça; nada além disso nesta rodada; 5b2dc929 SEGURADO.

- Pendências técnicas: (a) rodar a mesma cadeia entrada→payload com SEEDANCE_MODEL e VEO_MODEL é um `for` de 3 linhas sobre o mesmo bloco — só com pedido, para não somar caso sem pedido; (b) os dois achados declarados (teto 6 stills; 2,5 pal/s no re-dimensionamento) ficam para decisão de produto, não desta rotação; (c) caption-chunker rebaixado: se o Board quiser legenda executada, o caminho é transpilar `buildCaptionsFromWhisperWords` real de lib/compose no guardião existente; (d) 6 vermelhos herdados = baseline fechado, higiene só com pedido.
- Limite financeiro: zero. Nenhum render, nenhum dry-run pago, nenhuma escrita em banco, nenhum fornecedor.
- Próximo do executor (sem pedido novo): nada de código; aguardar revisão de b681b0d8 e da matriz. Publicação de b681b0d8 (só um guardião em scripts/) e de fidelidade 83aeef34: palavra do fundador.

## CLASSICOS-R5 — Seedance 1.5 e Veo 3.1 · 60 s · ideia/roteiro/brief → primeiro payload (responde a CLASSICOS-R4)

- ID: CLASSICOS-R5 · responde_a: CLASSICOS-R4 (GO_TECNICO do guardião b681b0d8 + pedido de extensão) · timestamp: 2026-09-14T17:32:00Z · dono: Claude (rotina MOTOR-AUTO-20260914, rodada 17:22Z) · estado: PRONTO_PARA_REVISAO
- Motores: Seedance 1.5 e Veo 3.1 (novo); Kling 2.5 (os 4 casos de b681b0d8 re-executados sob a era real, ver limites). Kineo 1 intocado.
- Branch: codex/classicos-kling25-0914 · SHA: **1d34e5b2457cbb8dd989f4063280ef1478b9c475** (pai b681b0d84ad01bb5cf8bad47cad216178f738e5e, aprovado por SHA no R4 — fica intacto; este é um commit em cima) · base: 6b5cccea3b67a6fcf5e4b5327987b4a1e6ea5c74 (contido em origin/main b5b0e2bf708c64c07787bddfd2bebb0077bb589a). SEGURADO: não enfileirado, não publicado.
- Arquivos (lista exata): scripts/test-visual-contract-2026-09-11.mjs (+98/−7, `git diff --check` limpo, zero CR). Nenhum código de produto; nenhuma biblioteca nova.
- Hipótese (escrita antes de editar): a cadeia real da rota já é parametrizável por `body.engine` — o único ponto sensível ao motor ANTES do payload é `SECONDS_PER_CLIP` (8 s Veo/Sora, 10 s Seedance/Kling); Seedance e Veo chegam ao primeiro payload com slug e campos próprios pela linha real `usedModel` e por `buildFalInput`.
- Critérios de aceitação (definidos antes): (1) slug escolhido pela linha REAL `usedModel` (wantsKling/wantsVeo/wantsSora), não injetado; (2) Seedance: `fal-ai/bytedance/seedance/v1.5/pro/text-to-video`, duration '10', aspect_ratio '9:16', resolution '720p', generate_audio false, seed, SEM negative_prompt/cfg/safety; Veo: `fal-ai/veo3.1/fast`, duration '8s', 9:16, 1080p, generate_audio false, safety_tolerance '5', negative_prompt clássico, seed, sem cfg; (3) roteiro verbatim reconstrói o texto do autor palavra por palavra nos dois motores, 0 planner, 1 split; (4) brief chega inteiro ao planner, ficha nunca narrada, evento `brief_detected_ai_mode` nomeia `engine` = 'seedance' | 'veo'; (5) era pela linha REAL `eraSuffix` + `eraLockSuffix` (ERA_YEAR_RE/ERA_WORD_RE reais) — nada de 1930 herdado; (6) mutante `SECONDS_PER_CLIP = 10` fixo fica vermelho.
- O que mudou no guardião: `run()` ganha `eraSuffix` opcional (todos os chamadores antigos ficam no fixture Kyoto-1930, byte-idênticos); `entryToPayload` ganha `engine` (chave que o Studio envia), lê `usedModelLine` e `eraSuffixLine` por AST e as EXECUTA; dois verificadores de payload novos (Seedance/Veo, sem reaproveitar os campos do Kling); laço por motor × {ideia, roteiro, brief}; verificação cruzada de `SECONDS_PER_CLIP`.
- Comandos e resultados (worktree temporária C:/kineo-wt/classicos-r5 sobre a branch, junção de node_modules, removida ao final):
  · baseline b681b0d8: `node scripts/test-visual-contract-2026-09-11.mjs` → rc=0, 365 verificações / 42 simulações.
  · 1d34e5b2: rc=0, **429 verificações (+64) / 48 simulações (+6)**, rede bloqueada.
  · `npx tsc --noEmit -p tsconfig.json` → rc=0 (arquivo .mjs; tsc rodado para o registro).
  · vizinhos na branch: enche-silencio 19/19 · cinematic-timeline 44 · primeira-pessoa 21 · scene-truth rc=0 · cinematic-speech 184 · regua-unica 74 · variety-axis rc=0. test-fidelidade-h3 NÃO existe nesta branch (vive em codex/fidelidade-0914, segurada) — não declarado verde aqui.
  · Falsificação por mutação na rota (commit antes; `git checkout --` depois de cada; árvore limpa conferida): M1 `SECONDS_PER_CLIP = 10` fixo → vermelho em "Veo script … need 10 → route sizes 9"; M2b `!wantsVeo ? VEO_MODEL` (texto da âncora preservado, comportamento invertido) → vermelho em "usedModel line maps every classic engine key" (não na âncora textual); M3 `safety_tolerance '4'` → vermelho; M4 Seedance default `'1080p'` → vermelho no payload da ideia Seedance; M5 ERA_YEAR_RE `19[0-5]` (janela passa a incluir 1958) → vermelho em "Era lock … silent for 1958"; M6 Seedance `generate_audio: true` → vermelho. 6/6.
- Fatos confirmados: Seedance idea 7 cenas / script 8 clipes / brief 7; Veo idea 7 / script 9 / brief 7 — o roteiro de 186 palavras dimensiona DIFERENTE por motor (8 vs 9), prova de que `SECONDS_PER_CLIP` é lido pelo motor. Os três slugs clássicos são distintos e cada chave (`seedance`/`veo`/`kling`/`sora`) mapeia para o seu. Nenhum still para Seedance/Veo (âncora é só Kling; conferido no mesmo chamador).
- Achado declarado, NÃO corrigido (custo zero, decisão de produto): a 2,5 pal/s da rota, 186 palavras precisam de 10 clipes de 8 s no Veo e o teto é 9 → 72 s de footage contra 74,4 s da própria estimativa conservadora. À régua real dos clássicos (3,1 pal/s) a fala dá ~60 s, então o teto é inofensivo na prática; se o Board quiser fechar, o remédio é a régua única no bloco #442 (route.ts:2855), já registrado no R3 — não mexi. Somado aos dois já registrados (teto 6 stills; 2,5 pal/s).
- Era: `eraLockSuffix` real é SILENCIOSO para Lituya 1958 (janela 1000–1939 + palavras de época; 1912 ativa, testado) — o produto não manda trava de época para 1958. Declarado como comportamento do produto, não corrigido. Consequência para b681b0d8: os 4 casos Kling passaram a rodar com era '' em vez do fixture 1930; as 35 verificações originais continuam verdes (mesmas asserções).
- Limites explícitos da prova (iguais ao R4): fatias encadeadas, não a requisição HTTP inteira; `run` submete só scenes[0]; política visual, writer options e speechRate injetados; brief cabe no `prompt.slice(0,1200)`; OpenAI/fal mockados. Veo 90 s / 5b2dc929 NÃO tocado — só o caminho existente a 60 s. Nada disto é validação audiovisual.
- Pendências técnicas: (a) legenda pelo código real (próximo eixo indicado pelo Board) — caminho é transpilar `buildCaptionsFromWhisperWords` de lib/compose no guardião existente; só com pedido; (b) achado Veo teto 9 acima; (c) publicação de 1d34e5b2 (um arquivo em scripts/) é palavra do fundador.
- Limite financeiro: zero. Nenhum render, nenhum dry-run pago, nenhum fornecedor, nenhuma escrita em banco.
- Próximo do executor sem pedido novo: nada de código; aguardar revisão de 1d34e5b2.

## LEGENDAS-R1 — buildCaptionsFromWhisperWords REAL no guardião; dois controles do Board REPRODUZEM defeito; conserto mínimo segurado (responde a CLASSICOS-R6)

- ID: LEGENDAS-R1 · responde_a: CLASSICOS-R6 (GO_TECNICO ao guardião 1d34e5b2 — não reaberto — + PROXIMO LEGENDAS-R1) · timestamp: 2026-09-14T17:59:31Z · dono: Claude (rotina MOTOR-AUTO-20260914, rodada 17:53Z) · estado: PRONTO_PARA_REVISAO
- Motores: base comum dos oito (a função serve os 3 chamadores: clássico direto, bloco Hollywood, fala nativa por clipe). Nenhum motor específico; nada de Veo 90 s / 5b2dc929.
- Branch: codex/legendas-0914 (nova, criada de origin/main) · base: 07c730b3734061558a8e490e72d69aeb103a8715 (origin/main) · dois commits, ambos SEGURADOS (não enfileirados, não publicados):
  · **c0aa264224ee19db4d32001d361a6e12578c88dd** — só scripts/test-caption-chunker.mjs (+~200/−84). Guardião VERMELHO DE PROPÓSITO: 41/47, os 6 vermelhos são os dois controles do Board reproduzidos + o irmão "óleo|Leo" + a colisão do normalizador. É o registro "esperado × real" sobre o produto INTACTO.
  · **a45237b7c06f26fd7ffb7b555cae3d7a8295c9de** — lib/compose.ts (+21/−4), conserto mínimo; guardião 47/47.
  `git diff --check origin/main..HEAD` limpo.
- Hipótese (escrita 17:55:03Z, antes de editar; cópia em scratch): H1 `normalizeCaptionWord` (compose.ts:1074) é ASCII (`[a-z0-9']` nas bordas): palavra só de acento ("é", "à", "ó") vira "" e o guard anti-stutter (`normalize(prevLast) === normalize(chunkWords[0])`, :1161) lê "" === "" como repetição → apaga palavra DIFERENTE. H2 último grupo em 0,95 s numa janela de 1,0 s: adjustedStart = 0,95+0,15 = 1,10 > janela; duration = max(0,1, min(0,05, −0,10)) = 0,1 → elemento [1,10; 1,20] inteiro depois da janela.
- Critérios (definidos antes): C1 função REAL extraída e executada (transpile+vm, sem import, mutante muda resultado); C2 os 9 asserts antigos preservados; C3 chamadores por grep, declarados como inspecionados; C4 finitos: teto, sentenceEnd, pausa ≥0,28 s, karaoke ordem/monotonia, janela CTA, vazios, acentos EN/PT/ES verbatim; C5 controle (a): "à" sobrevive; C6 controle (b): nenhum time ≥ fim da janela. Vermelho em C5/C6 = defeito reproduzido, conserto em commit separado.
- O que o guardião faz agora (c0aa2642): lê lib/compose.ts e lib/openai.ts com readFileSync, corta por marcador em coluna 0 as fatias `buildCaptionsFromWhisperWords`, `normalizeCaptionWord`, `round3`, `const CAPTION_SYNC_OFFSET`, `pickHighlightWord`, `cleanWord`, `STOPWORDS`, `HIGHLIGHT_CANDIDATES`; ts.transpileModule + vm.runInNewContext com `require` proibido (lança). A cópia do algoritmo foi removida — não existe fallback. Prova de que é o extraído que roda: mutante `SENTENCE_GAP_SECONDS 0,28→99` na fatia muda a segmentação (2 chunks → 1). Chamadores: compose.ts:2511 (clássico direto — usa `cap.time` sem filtro), :3113 (Hollywood — `if (t >= captionWindowEnd) continue` na janela da TIMELINE, não do bloco) e :3167 (clipe — mesmo `continue` e `if (!emitted) throw`), todos com ctaTailSeconds=0 e maxWords=CAPTION_WORDS_PER_CHUNK=4 — **inspecionados por grep, NÃO executados**.
- Resultado esperado × real (produto intacto, c0aa2642 — `node scripts/test-caption-chunker.mjs` rc=1, 41/47):
  · controle (a) `[Ela, é | à, praia]` maxWords=2 → esperado `["Ela é","à praia"]` · **real `["Ela é","praia"]`** — "à" apagada do texto E do karaoke words[]. Irmão: `[o, óleo | Leo, viu]` → real `"o óleo viu"` (o nome Leo some: "óleo"→"leo" ≡ "Leo"→"leo"). Colisões medidas no normalizador real: "é"→"" · "à"→"" · "óleo"→"leo" · "às"→"s" · "és"→"s".
  · controle (b) `[first(0–0,3, sentenceEnd) | last(0,95–1,0)]`, áudio 1,0, CTA 0 → esperado nenhum time ≥ 1,0 · **real `{text:"last", time:1.1, duration:0.1}`** — elemento inteiro depois da janela. Consequência por chamador (lida, não executada): clássico → vai à timeline depois do áudio; Hollywood → invade o bloco seguinte (o filtro é da timeline inteira); clipe → descartado pelo `continue`, palavra some da legenda (e `throw` se era a única).
  · o stutter REAL `[think, it. | it, works]` continua removido nos dois SHAs (o guard não foi enfraquecido).
- Conserto mínimo (a45237b7, só `buildCaptionsFromWhisperWords` e seu normalizador; chamadores intocados): (1) `normalizeCaptionWord` usa `[^\p{L}\p{N}']` com flag `u` — mesma classe já em uso em lib/expandPolicy.ts:184 — "é"→"é", "à"→"à", "óleo"→"óleo", "Leo"→"leo"; (2) guard só iguala forma normalizada NÃO vazia (`prevNorm !== ''`); (3) `adjustedStart` dispensa o offset quando `rawStart + 0,15 >= captionWindowEnd` (mesma isenção que o 1º chunk já tinha) — "last" nasce em 0,95, e o offset continua aplicado quando cabe (0,5→0,65, testado). Declarado, não mexido: o piso de 0,1 s de duração (Creatomate) faz o último elemento terminar até 0,05 s depois do áudio — comportamento anterior, o Board decide se é eixo.
- Comandos e resultados (C:/kineo-wt/legendas-0914, junção de node_modules):
  · c0aa2642: `node scripts/test-caption-chunker.mjs` → rc=1, 41/47 (os 6 esperados). a45237b7: rc=0, **47/47**.
  · `npx tsc --noEmit -p tsconfig.json` → rc=0, saída vazia (a45237b7).
  · vizinhos em a45237b7 (rc conferido um a um, sem tail antes): enche-silencio 0 · visual-contract 0 · cinematic-timeline 0 · primeira-pessoa 0 · scene-truth 0 · cinematic-speech 0 · regua-unica 0 · variety-axis 0 · cinematic-terminal-proof 0 · contrato-duracao-classico 0 · kineo1-cinema 0 · active-quality-rejection 0 · quality-rejection 0 · scene-retry-mutex 0 · capa-snapshot 0 (36). Três rc=1 — multiformato (2 FAIL: buildFalInput/formato no despacho), momentum-motor-gratis (import inesperado ./entryPolicy), guardiao-2026-08-28 (1 ✗ live) — rodados TAMBÉM no snapshot do Board C:/kineo-wt/board-review-classicos-1d34e5b2 (sem o meu delta): mesmo rc, mesmo número de falhas, mesmo texto → herdados, não meus. test-fidelidade-h3 não existe nesta branch (vive em codex/fidelidade-0914), não declarado.
  · Nenhum guardião além de test-caption-chunker toca normalizeCaptionWord/CAPTION_SYNC_OFFSET/buildCaptionsFromWhisperWords (grep em scripts/).
- Limites explícitos: função pura executada com Whisper words SINTÉTICOS; nada de áudio, Whisper remoto, TTS, LLM, render, banco. Sobreposição visual, legibilidade, posição e sincronia no ARQUIVO continuam NÃO VALIDADAS. `pickHighlightWord` executado mas não asserido além de não quebrar.
- Pendências técnicas: (a) Board revisa c0aa2642 (reprodução) e a45237b7 (conserto) — o GO, se vier, é por SHA; (b) publicar a45237b7 é código de produto (lib/compose.ts) → palavra do fundador ('publica legendas a45237b7'), afeta os 3 caminhos de legenda de todos os motores; (c) música coerente pelo caminho real com mocks é o eixo seguinte indicado pelo Board — só com pedido.
- Limite financeiro: zero. Nenhum render, nenhum dry-run pago, nenhum fornecedor, nenhuma escrita em banco.
- Próximo do executor sem pedido novo: nada de código; aguardar revisão.
