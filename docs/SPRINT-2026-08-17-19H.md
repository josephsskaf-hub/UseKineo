# SPRINT 19H — 17/08/2026 (CEO operacional)

Protocolo lido antes de agir: `docs/PROMPT-DIARIO.md` (integral), `docs/ORDENS-AQUISICAO-2026-08-02.md`,
`docs/ENGAGEMENT-LOG.md`, `docs/GATES-ABERTOS.md` e as quatro sprints de hoje (10H/11H/13H/16H).
Nada refeito. Estado do repo na abertura, conferido com `git fetch`: **`HEAD == origin/main == 16da5f6`** —
zero commits represados. O `158-PUSH` pedido às 16h já subiu. **Não há PUSH PENDENTE nesta abertura.**

---

## 1. O ACHADO DA SPRINT — o loop de distribuição nunca produziu UMA conta

A entrega nº 2 da lista de "amanhã" das 16h era *"atacar o convite de compartilhar (6 de 54 = 11%)"*.
Antes de mexer no convite, medi o outro lado do cano — a regra nº 5 do PROMPT-DIARIO de 14/08
(*"antes de escrever o ativo nº 50, medir os 49"*). O outro lado é o `/v/[id]`, a página que recebe
**todo** link compartilhado.

**30 dias, `/v/[id]`:**

| degrau | número |
|---|---|
| sessões que chegaram | **234** |
| clicaram na CTA (`public_video_cta_clicked`) | **7 (3,0%)** |
| dessas, chegaram a QUALQUER página `/signup` | **0** |
| contas criadas (`profiles.signup_utm_source='public_video'`) | **0 — na história inteira** |

O último número não é instrumento cego: `signup_utm_source` está preenchido em **510 perfis** nos
últimos 30 dias e o caminho `/signup%` registrou **880 eventos em 783 sessões**. O instrumento mede,
e o que ele mede é zero.

**Consequência direta para o plano de amanhã: dobrar o compartilhamento de 11% para 22% levaria
234 sessões para ~468 e 0 contas para 0.** O gargalo do loop nunca esteve no convite.

E o loop inteiro, medido por outro lado, dá o mesmo veredito: **932 pessoas têm código de indicação;
5 contas em toda a história têm `referred_by`; 3 recompensas pagas.**

### Por que NÃO é "o botão está escondido"
Foi a primeira hipótese (player 9:16 empurra a CTA para fora da dobra). **Regra Zero matou:** o
`/v/[id]` **já tem uma barra fixa** de CTA, largura total, "Make one like this — free →", visível a
página inteira, desde 14/08 (`ONDA4 #2`). Eu ia adicionar o `StickyFreeShortCTA` por cima de uma
barra que já existe.

Com a barra fixa no ar e ainda assim 3%, sobra a explicação incômoda — e ela está na **nossa** copy:

> A mensagem que o produto escreve por quem compartilha era, literal:
> **"Watch my Short and tell me what you think"**.

Isso convoca um **juiz**, não um comprador. O amigo abre, assiste, dá a opinião e vai embora — e 3%
é exatamente o que um espectador converte. **Construímos uma landing de compra e mandamos para ela
uma visita de cortesia.** O único lugar onde isso se conserta é a montante: no texto que sai junto
com o link, que é nosso.

---

## 2. O QUE SUBIU

### (a) `KINEO-SHARE-PEDE-IMITACAO-2026-08-17` — a mensagem passa a pedir imitação
`app/(dashboard)/generate/GenerateClient.tsx` · `app/(dashboard)/history/HistoryClient.tsx`

De *"Watch my Short and tell me what you think"* para
**"I made this Short with AI — no camera, no editing. Watch it: …"**.

Mantém o orgulho de quem compartilha ("I made this") e planta o **mecanismo**, que é a única
informação capaz de virar espectador em usuário. Os dois arquivos mudam juntos porque são o par
(regra crítica do CLAUDE.md) — textos diferentes no mesmo evento arruinariam a leitura.

**Rótulo de variante subiu junto:** `whatsapp_first_30_30_v1` → `whatsapp_imitation_30_30_v2`.
Sem isso os eventos de antes e depois cairiam no mesmo balde e amanhã se compararia duas mensagens
achando que se compara uma.

### (b) `public_video_cta_viewed` — o denominador que não existia
`components/PublicVideoCtaLink.tsx` (+ `placement` nos 4 call sites do `/v/[id]`)

Com só o evento de clique, "3%" tinha duas leituras que pedem consertos opostos (lição nº 6 do
PROMPT-DIARIO): *não viu* pede geometria, *não quis* pede oferta. Agora cada CTA marca impressão
(IntersectionObserver, ratio ≥ 0,5, latch por `videoId::placement`) com quatro posições separadas:
`sticky_bar`, `under_player`, `after_script`, `expired_preview`.

**A previsão que amanhã falsifica ou confirma:** como a `sticky_bar` está sempre na tela, as
impressões dela devem chegar perto do total de sessões. Se chegarem, "não viu" morre com prova e o
conserto é de oferta/enquadramento — não de posicionamento. Se NÃO chegarem, há algo quebrado na
barra e o achado é outro.

### (c) 🔴 `KINEO-GRANT-COPY-UNICA-2026-08-17` — a vitrine prometia 40, a conta recebia 50
Achado de varredura, e é o mais caro dos três porque estava **ao vivo em superfície pública**.

A aprovação de HOJE (40 → 50, *"se só muda 1 dólar, pode mudar pra 50"*) mexeu em
`TRIAL_CREDIT_CAP` e na maior parte da copy, mas o número estava **digitado à mão** e ficou em 40 em:

| superfície | o que dizia |
|---|---|
| **botão primário** de 4 páginas públicas (`OFFER.copy.ctaPrimary`) | "Start free — **40** Creator credits →" |
| **H2 do CTA final das 30 páginas** `/free-ai-shorts/[niche]` | "Start free — **40** Creator credits for … Shorts" |
| `/checkout/cancelled` | "Use your trial credits — **40** free credits on signup" |
| **primeira frase que o recém-cadastrado lê** no `/generate` | "Your Creator trial is live — **40** free credits" |

Nas páginas públicas isso aparecia **abaixo de uma dobra que dizia 50**: dois números na mesma tela,
e o menor no botão. É o oposto do princípio da casa (*"selo honesto é ativo de marca; vitrine mente
→ produto morre"*) — subvendendo a própria oferta em 20%.

**Conserto estrutural, não remendo:** o número virou `TRIAL_GRANT_CREDITS_COPY` em
`lib/freeTierOffer.ts` e as 9 frases passam a derivar dele. A constante mora lá, e não em
`lib/reverseTrial.ts`, porque `freeTierOffer.ts` é **folha de propósito** (entra no bundle do
browser via `swapFreeTierCopy`; importar `reverseTrial.ts` arrastaria `@supabase/supabase-js` para o
cliente e fecharia um ciclo). A trava fica na direção que já existe: `reverseTrial.ts`, que **já**
importa `freeTierOffer.ts`, ganhou uma **asserção de tipo** que quebra o `tsc` se os dois números
divergirem. Custo em runtime e no bundle: zero.

**Falsificação da trava (não só do `tsc`):** `TRIAL_CREDIT_CAP` 50 → 41 →
`lib/reverseTrial.ts(113,3): error TS2344: Type '50' does not satisfy the constraint '41'` ·
`EXITCODE=2`. Restaurado, **md5 idêntico** (`5e24956d…`), `EXITCODE=0`.

**Limite dito na cara:** `next.config.js` tem `typescript.ignoreBuildErrors: true`. A trava protege o
portão que cada sprint realmente usa (`npx tsc --noEmit` antes de commitar) — **ela não bloqueia um
deploy da Vercel.** Quem confiar nela como muro de produção vai se enganar.

---

## 3. NÚMEROS (17/08, fechamento das 19h BRT · contas internas fora)

| métrica | hoje | ontem |
|---|---|---|
| cadastros | **55** | 67 |
| vídeos concluídos | 67 | — |
| geração iniciada → concluída | 199 → 67 (**34%**) | — |
| `checkout_started` | 20 | 13 |
| **clientes NOVOS** | **0** | 0 |
| trials ativos | 149 | 142 (16h) |
| pagantes externos | **5** | 5 |
| créditos de trial consumidos (acum.) | 2.141 | — |

### ⚠️ Um `payment_success` hoje — e NÃO é venda nova
16:29Z, tier `starter`, usuário `gapozweb@gmail.com`. Conferido perfil a perfil: conta de **01/08**,
origem `taaft`, assinatura já existente, `is_pro`/`has_paid` corretos, 21 créditos. É **renovação
mensal**. Provisionamento saudável (a hipótese de webhook quebrado foi levantada e descartada com
dado). **O 11º dia sem cliente novo continua de pé** — relatar isso como "1 venda hoje" daria sinal
falso.

### O único trial que converteu na história diz o contrário do que se supunha
`noelrss21@gmail.com` (cadastro 10/08, `trial_status='converted'`, origem `sticky_cta`) usou
**10 dos 50 créditos** — não chegou perto de queimar a concessão. Reforça, com o único caso positivo
que existe, o achado das 10h: **a coorte que queima o trial inteiro converte 0 de 18.** Quem compra
é quem usa pouco e volta, não quem consome muito.

### Portão de render (entrega nº 1 da lista das 16h) — leitura honesta
`active_render_gate_forced_open` com `snapshot='live_id_server_idle'`: **0 em 30h.** Bloqueios:
55 hoje, **0 nas últimas 6 horas**. **Não declaro vitória:** 0 disparos do caminho novo com bloqueios
caindo é ambíguo — pode ser o deploy pegando, pode ser janela de tráfego (é a lição nº 7 de 14/08:
vão no gráfico só é incidente contra o vão de ontem). Fica para a leitura de amanhã, com dia cheio.

---

## 4. AQUISIÇÃO DA SPRINT — slot "Funil" aplicado a uma superfície que nenhuma sprint tinha tocado

Slots usados hoje: SEO (10h) · Analytics (11h) · Funil/checkout (13h) · Conteúdo (16h).
Escolha das 19h: **Funil, no `/v/[id]`** — 234 chegadas reais por mês, 0 contas, e nenhuma sprint
havia medido essa página. É aquisição de tráfego já pago e já entregue, sendo desperdiçado.

**Registrado e NÃO feito, de propósito:** diretórios grátis. O gate de tráfego pago segue fechado e
os diretórios grátis (TAAFT já feito, TopAI/Futurepedia/AlternativeTo/aixploria) exigem criar conta
em plataforma nova com credenciais — **gate do fundador**, não meu. Não abri nenhum cadastro.

---

## 5. AMANHÃ — 3 entregas

1. **Ler `public_video_cta_viewed` por `placement`.** Se `sticky_bar` ≈ sessões, "não viu" está morto
   com prova e o conserto do `/v/` é de enquadramento, não de botão. Ler junto o primeiro corte do
   `whatsapp_imitation_30_30_v2`.
2. **Portão de render**, com um dia cheio de dados: `live_id_server_idle` > 0 prova a tese das 16h;
   0 com bloqueios altos manda registrar o snapshot inteiro no evento.
3. **Varredura do mesmo defeito da seção 2(c) em TODA a copy de números** (créditos por motor, preços
   por moeda, limites do free): o padrão "número aprovado num commit, digitado à mão em N frases"
   acabou de custar 4 superfícies públicas. A pergunta é quantos outros números vivem assim.

---

## 6. ALERTAS

- 🔴 **Conversão trial→pago < 5% pelo 11º dia.** 149 trials ativos, **1 conversão na história**.
- 🔴 **Conclusão de geração desabou: 34% (199 iniciadas → 67 concluídas)**, contra 57% às 16h e 61%
  ontem. Merece ser o primeiro item da sprint das 21h — pode ser o portão, pode ser fornecedor.
- 🟠 **fal.ai — a divergência das 16h continua aberta** e é olhada de 30s no painel: docs de hoje
  dizem "auto-reload ON", o CHECK-UP diz US$ 27,67 com top-up OFF. Não leio painel de fornecedor daqui.
- 🟠 **3 diretórios de feature fora do git** (`app/(dashboard)/audio`, `app/api/audio`,
  `app/api/enhance`) — reportado às 16h, **segue aberto**. Não commitei: é trabalho de outra sessão.
- 🟢 Sem erro em checkout. Preços, marca d'água, e-mails e fluxo de pagamento **intocados**.

---

## 7. A 2ª PASSADA ADVERSARIAL PAGOU (de novo) — faltavam mais 4 superfícies

O commit da seção 2(c) consertou 4 lugares e eu quase parei ali. A varredura obrigatória da segunda
passada achou o resto, e o pior deles é o mais visto de todos:

| superfície | o que dizia |
|---|---|
| **`components/Sidebar.tsx`** — navegação de **TODA página logada** | "⚡ Creator trial: **40** free credits" |
| `components/ExitIntentOffer.tsx` — modal de saída | "Creator trial on signup: **40** free credits" |
| `components/PhWelcomeBanner.tsx` — boas-vindas do Product Hunt | "your Creator trial starts now: **40** free credits" |
| `lib/comparisons.ts` — páginas de comparação | "a Creator trial: **40** free credits" |

A sidebar é onde a pessoa em trial mais olha, o dia inteiro, dizendo 40 enquanto a conta tem 50.
Varredura final por `Creator trial: 40` / `40 free credits` / `40 Creator credits` em `app`,
`components` e `lib`: **zero ocorrências em copy viva.**

Registro honesto: nessa troca **eu introduzi um erro** (escapei a crase de fechamento de um template
literal em `comparisons.ts`). O `tsc` pegou antes do commit — foi por isso que ele existe.

## 8. NOTA OPERACIONAL — dois commits, e um empecilho de ambiente

Esta rodada tem **2 commits**: `4c58321` (achado principal + 1ª leva) e `7b0080d` (2ª passada).

Durante a sessão o git deixou arquivos `.lock` órfãos de 0 byte em `.git/` (`HEAD.lock`,
`index.lock`, `packed-refs.lock`) que o mount do OneDrive **não deixa apagar de fora do Windows**
(`Operation not permitted`, inclusive por `rm` e por `os.remove`). Isso travou o segundo `git commit`.
Contornei sem gambiarra no histórico: objeto criado com `git commit-tree` (parent correto) e a ref
`refs/heads/main` reescrita para ele. `git log`, `git show --stat` e `git cat-file -t` confirmam os
dois commits com os arquivos certos e nada além deles.

**O que isso pede de você:** se o `170-PUSH.bat` reclamar de *"another git process seems to be
running"*, apague os `.lock` de 0 byte dentro da pasta `.git` e rode de novo. No Windows eles saem
normalmente.

## RESUMO PARA O CEO

1. **O loop de compartilhamento nunca gerou UMA conta.** 234 visitas ao link compartilhado em 30 dias → 7 cliques → **0 cadastros**, e `signup_utm_source='public_video'` é **0 na história inteira** (com o instrumento comprovadamente vivo: 510 perfis com utm no mesmo período).
2. **Por isso NÃO fiz o que estava planejado para hoje.** Dobrar o compartilhamento de 11% para 22% levaria 234 visitas a 468 e 0 contas a 0. O gargalo nunca esteve no convite.
3. **A causa está na nossa própria frase:** o produto escrevia *"Watch my Short and tell me what you think"* — isso convoca um **juiz**, não um comprador. Trocado por **"I made this Short with AI — no camera, no editing"**: pede imitação, mostra o mecanismo, e mantém o orgulho de quem compartilha.
4. **Regra Zero evitou um erro bobo:** eu ia adicionar uma barra fixa de CTA numa página que **já tem** uma desde 14/08. Ela existe, está sempre na tela — e mesmo assim são 3%. Isso é o que descarta "o botão está escondido".
5. **🔴 A vitrine prometia 40 créditos e a conta recebia 50.** Sua aprovação de hoje (40→50) mudou o teto, mas o número estava digitado à mão: ficou 40 no **botão primário de 4 páginas públicas**, no **H2 das 30 páginas de nicho**, no `/checkout/cancelled` e na **primeira frase que o recém-cadastrado lê**. Estávamos subvendendo a própria oferta em 20%, com dois números na mesma tela.
6. **Consertado na raiz, não remendado:** o número agora tem **uma fonte só** e uma trava de compilação que quebra o `tsc` se copy e grant divergirem — falsificada de verdade (forcei 41 vs 50, deu erro; restaurei, md5 idêntico). Limite dito na cara: a trava protege o portão das sprints, **não** o build da Vercel.
7. **Números: 55 cadastros · 67 vídeos · 20 checkouts · 0 clientes novos — 11º dia.**
8. **⚠️ Teve um `payment_success` hoje e eu chequei antes de comemorar: é RENOVAÇÃO** de um assinante de 01/08, não venda nova. Levantei a hipótese de webhook quebrado e descartei com dado (conta provisionada certa).
9. **A sacada que vale para onde investir, e é sua:** o **único** trial que converteu na história usou **10 dos 50 créditos** — não chegou perto de queimar. Junto com o achado das 10h (a coorte que queima tudo converte **0 de 18**), o padrão fica difícil de ignorar: **quem compra é quem usa pouco e volta.** Estamos dimensionando e vendendo o trial pelo perfil que consome mais e paga menos.
10. **🔴 Conclusão de geração caiu para 34%** (era 57% às 16h, 61% ontem). Deixei como primeiro item das 21h.
11. **Segue sua a olhada de 30s no fal.ai** (docs se contradizem) e a decisão sobre os 3 diretórios de feature fora do git, aberta desde as 16h.
12. Nada em gate de dinheiro foi tocado: preços, marca d'água, e-mails e fluxo de pagamento intocados.
13. **A 2ª passada adversarial pagou de novo:** faltavam mais 4 lugares dizendo 40 — e o pior é a **sidebar de toda página logada**, que é onde quem está em trial mais olha. Agora a varredura volta zero.
14. Rode **`scripts\170-PUSH.bat`** para subir esta rodada (**2 commits**). Se ele reclamar de *"another git process seems to be running"*, apague os arquivos `.lock` de 0 byte dentro da pasta `.git` e rode de novo — é limitação do OneDrive, não do código (seção 8).
