# SPRINT 06/08/2026 — REVERSE TRIAL FASE 1 [KINEO-REVERSE-TRIAL-P1-2026-08-06]

Sessão dedicada (prioridade nº1 do fundador, spec aprovada 06/08). Fundação do
reverse trial construída INTEIRA atrás de flag — produção não muda em nada até
`KINEO_REVERSE_TRIAL_ENABLED=true` ser setada na Vercel.

## O que foi entregue

1. **Migração aditiva APLICADA** no Supabase (projeto cqqukkvjjrguayiyjvhh,
   migração `reverse_trial_p1_profiles_columns`, verificada por
   information_schema pós-apply): `profiles.trial_status` (text, null),
   `trial_ends_at` (timestamptz), `trial_credits_used` (int not null default 0),
   `trial_extended` (bool not null default false), `trial_variant` (text) +
   índice `idx_profiles_trial_ends_at`. Espelho em `docs/SQL-REVERSE-TRIAL.sql`.
   Migração ANTES do deploy — a ordem certa (o select novo do cinematic já pode
   ler as colunas).

2. **Flag única server-side**: `KINEO_REVERSE_TRIAL_ENABLED === 'true'` lida SÓ
   em `lib/reverseTrial.ts` (mesmo idioma dos crons de lifecycle). Default OFF.
   `isTrialActive()` retorna false com a flag OFF para QUALQUER perfil — mesmo
   um perfil com colunas populadas à mão não ganha nada.

3. **Ativação no signup** (`app/api/track-signup-source/route.ts` → o único
   touchpoint server-side que TODOS os fluxos de signup já chamam: signup page,
   login page, mount do /generate): `maybeActivateReverseTrial()` com guardas —
   perfil NOVO (<24h), 1 trial por conta PARA SEMPRE (trial_status não-nulo
   nunca reativa; o UPDATE carrega `is('trial_status', null)` contra corrida),
   domínios descartáveis bloqueados (mailinator, guerrillamail, 10minutemail,
   tempmail, yopmail, sharklasers, minitts.net, dysonc.com), conta paga pulada.
   A/B 50/50 determinístico (FNV-1a do user_id) → trial_variant '3d'/'7d',
   trial_ends_at = now()+3d/7d. SEM tocar em Stripe.

4. **Entitlement Creator no trial**: `app/api/generate-video-cinematic/route.ts`
   — Seedance liberado quando `isTrialActive(profile)`; Kling/Veo/Hollywood
   CONTINUAM exigindo conta paga (trial NUNCA os satisfaz; 402 com copy
   "Studio engines — not included in your trial"). UI: `/api/credits` devolve
   `trialActive` (query extra SÓ com flag ON) e o ModeSelector do
   GenerateClient destrava Seedance e mostra "🔒 Studio" nos três motores
   premium. Sem redesign.

5. **HARD CAP 60 no BACKEND**: novo ponto único de débito
   `lib/credits/debit.ts` (`debitVideoCredits`) — os 8 call sites do RPC
   `debit_video_credits` (compose/status, cinematic, generate-avatar,
   avatar/voice, avatar/scene, gesture-clip, animate, render legado) agora
   passam por ele; ZERO chamadas cruas restantes (grep confirmado). Débito
   confirmado + conta em trial → soma em `trial_credits_used`
   (`lib/reverseTrial.ts.recordReverseTrialDebit`, update otimista com retry);
   ao atingir 60 grava `trial_status='expired'` NA MESMA escrita — expira na
   hora, mesmo antes do dia 3/7.

6. **Expiração passiva**: `isTrialActive()` = status 'active' AND relógio antes
   de trial_ends_at AND used < 60 — usada em todos os checks; nada depende de
   cron.

## QA
- `tsc --noEmit` EXITCODE=0.
- Flag OFF: wrapper de débito chama o MESMO RPC com os mesmos parâmetros e
  devolve o mesmo shape; ativação/entitlement/cap retornam antes de qualquer
  leitura; /api/credits não faz query extra (os 2 campos novos da resposta são
  constantes false/null e o cliente gateia por typeof). EOL conferido no HEAD
  por arquivo; diff limpo sem ruído de CRLF (track-signup-source e
  animate/service renormalizados para LF = HEAD).

## Fluxo com a flag ON (raciocínio, para o dia da virada)
signup → track-signup-source ativa trial ('3d' ou '7d') → generate mostra
Seedance destravado + cadeado Studio → cada débito soma no cap → 60 atingido OU
relógio vence → isTrialActive()=false na request seguinte → direitos voltam ao
free (3 Fast/24h, intocado) → fase 2 converte/comunica.

## ⚠️ DECISÃO ABERTA ANTES DE LIGAR A FLAG (dinheiro — não implementei)
A ativação NÃO concede video_credits (perfil novo nasce com 0). Trial dá o
DIREITO de Creator, mas sem saldo nada debita. Sugestão natural: grant de 60
(o próprio cap; pior caso de COGS = 3 Seedance = ~$7,02/trial em 60 créditos a
$0,117/cr worst case). É decisão do CEO/fundador, não de fundação.

## FASE 2 (não feito, por spec)
Cron de downgrade formal ('expired' → limpeza/plan) · modal de downgrade ·
e-mails D0–D10 · paywalls contextuais · troca atômica do free tier + copy
(3/24h fica como está até lá) · fingerprint de device (anti-abuso além de
e-mail) · webhook Stripe gravar trial_status='converted' na compra (hoje um
convertido que continua gastando pode virar 'expired' no cap — inofensivo para
entitlement, mas suja o A/B) · grant de créditos do trial (decisão acima) ·
Fast limpo/sem marca d'água durante o trial (exigiria mexer em compose, que
esta fase congela por ordem expressa).

## RESUMO PARA O CEO
- Reverse trial FASE 1 pronto e commitado atrás de KINEO_REVERSE_TRIAL_ENABLED
  (OFF). Produção inalterada; tsc verde; migração já aplicada no banco.
- Cap de 60 é do SERVIDOR: ponto único de débito novo (lib/credits/debit.ts)
  cobre os 8 caminhos de cobrança; expira o trial na hora ao bater 60.
- Studio (Kling/Veo/Hollywood) NUNCA no trial — server + cadeado na UI.
- 1 trial por conta para sempre + bloqueio de e-mail descartável na ativação.
- GATE antes de ligar a flag: decidir o grant de créditos do trial (sugestão
  60) e executar a fase 2 (cron, e-mails, copy do free tier).

---

# 🕝 SPRINT 14:30Z (11h BRT) — A REVISÃO ADVERSARIAL MATOU A MINHA CORREÇÃO

## PASSO 0
Sem replay: `select now()` = 14:30:23Z, bate com o `env`. **`origin/main` == `HEAD` == `a7d61bb`
no início: os 3 commits que estavam represados JÁ SUBIRAM** — o gate do push da madrugada foi
fechado pelo fundador. O deploy `a7d61bb` está READY em produção, então a correção do Autopilot
(`737e1fb`) está no ar desde ~12:42Z.

## SAÚDE — LIMPA
Zero erros nas últimas 6h. Único registro do dia às 08:00Z: 11 `generate_blocked_server_active_render`
(2 pessoas) e 4 `generate_script_threw` (2 pessoas). Não é apagão, não é fornecedor.

## PLACAR (14:35Z) — NADA PIOROU
| | 00:47Z | 14:35Z |
|---|---|---|
| perfis ext | 940 | **942** |
| cadastros 7d | 236 | 237 |
| ativados | 340 | **341** |
| **taxa 2º vídeo** | 24,1% | **24,6%** |
| has_paid / ATIVOS | 6 / 3 | 6 / **3** |
| posted_shorts | 2 | **2** |
| baixaram 7d | 48 | 48 |

## AS PREVISÕES
✅ **BATEU — `post_nudge_sent`:** 13 eventos, **13 usuários distintos, ZERO duplicados**, em 5
rodadas (00:50, 01:50, 02:50, 10:50, 12:50). A reserva atômica segue de pé no segundo dia.
⏸️ **NÃO COBRÁVEL — `autopilot_page_viewed` com `on_paid_plan`:** **0 visitas** à página do
Autopilot desde o deploy. Não é falha do código, é ausência de tráfego. Fica para a próxima sprint,
e é um dado por si: a página que queimou 2 dos 3 assinantes não recebe ninguém há 12h.

## 🎯 A IDEIA DA SPRINT — E O QUE ACONTECEU COM ELA
Fui atrás do item 2 da fila: **"devolver a cota de reserva morta (11,7%)"**.

**A medição confirmou o cheiro.** 270 reservas free na história, 235 viraram vídeo, **35 não
(13,0%)**, em 33 pessoas. E a query que decide: **13 pessoas já levaram HTTP 402 "you've hit
today's free limit (3)". OITO delas (62%) tinham uma reserva sem entrega na janela de 24h daquele
exato momento.** Não é abuso — o máximo por pessoa é **2** em três semanas, média 1,06; abuso teria
concentração.

Escrevi a correção óbvia: reserva que não virou vídeo devolve a vaga após um período de graça de
45min, **derivado da medição real** de render (p50 189s · p90 300s · p99 1110s · **max 1436s**),
e não de `STALE_GENERATION_MS` (15min), que é menor que o p99 daqui.

### ❌ E A REVISÃO ADVERSARIAL DERRUBOU TUDO
Com uma frase que **já estava no código**, escrita por outra pessoa, **duas linhas acima de onde eu
mexi**:

> *"`videos` is written only after the client polls a successful render"*

**A linha em `videos` é escrita pelo CLIENTE.** Quem não polla nunca a escreve — e mesmo assim fica
com o MP4. Existe até uma rota autenticada, `/api/render/[id]`, que devolve a URL do vídeo sem
produzir essa escrita. Ancorar cota nisso transformaria **3 por 24h em 3 a cada 45 minutos ≈ 96/dia**.

**E a mesma revisão desmontou a minha própria medição:** das 35 reservas "mortas", **9 têm download
ou visualização nas 2h seguintes** — parte delas ENTREGOU vídeo, e eu estaria devolvendo vaga para
quem recebeu o produto. **Os 13% são um TETO da vítima, não a vítima.**

### ✅ O QUE FOI COMMITADO NO LUGAR (`KINEO-DEAD-RESERVATION-2026-08-06`)
Não a correção — **o instrumento**:
1. **`lib/freeFastQuota.ts`:** a contagem sai de duas cópias (compose + send-credits-back) e vira
   fonte única, com a **regra INTACTA** (paridade conferida caso a caso: dedupe por `render_id`,
   previews legados pré-PUSH19, claim sem `render_id`, ordem de processamento).
2. **Instrumento inerte:** `undelivered_reservations` no evento de recusa e
   `blocked_by_undelivered` no payload do cron. **Aditivos** — `reason`, `used` e
   `with_free_credits` não mudaram de significado.
3. **Terceira cópia do número 3 eliminada:** `send-cap-hit` tinha `FREE_CAP = 3` hardcoded, com um
   comentário apontando para uma constante que este mesmo commit acabara de mover. Mais 3 ponteiros
   defasados corrigidos (`kineoFacts`, cabeçalho do `send-credits-back`).

### A SEGUNDA PASSADA PEGOU O DEFEITO QUE EU INTRODUZI CONSERTANDO O PRIMEIRO
Adicionei um `throw` "por segurança" para linha sem `user_id`. Mas **`events.user_id` é
`ON DELETE SET NULL`**: apagar uma conta deixa reservas órfãs, a query do cron varre a base inteira
sem filtrar isso, e o throw **derrubaria o job de lifecycle inteiro a cada rodada** até a linha sair
da janela de 24h. Virou parâmetro explícito `onUnknownUser`: compose lança, cron pula.

## 🚨 ACHADO DE SEGURANÇA — PRIORIDADE 1 DA PRÓXIMA SPRINT
`app/api/render/[id]/route.ts` verifica **apenas que existe um login**, e nunca que o render
pertence àquele usuário. Consequências:
- devolve a **URL do vídeo** de qualquer `render_id` para qualquer usuário logado;
- chama `refundRenderCredits('legacy-<id>')` — ou seja, **um usuário pode disparar refund no ledger
  de crédito de outro**.

Não corrigi nesta sprint por deliberação: mexe em dinheiro e merece revisão adversarial dedicada,
não os minutos finais de uma janela.

## 💡 IDEIA CEO — **O RECIBO QUE COBRA POR NÓS**
De primeiro princípio: hoje a empresa só sabe que entregou um vídeo **se o navegador da pessoa
avisar**. Isso não é um detalhe técnico — é a razão de a cota estar errada, de o e-mail de resgate
não saber quem resgatar, e de `posted_shorts` estar em 2. A entrega é o evento mais valioso do
funil e ele é *terceirizado para uma aba que a pessoa fecha*.

**Registrar a conclusão do render no SERVIDOR** (webhook do Creatomate ou varredura por `render_id`)
não é dívida técnica: é o que destrava, de uma vez, (a) a cota justa — as 8 pessoas barradas
indevidamente; (b) o e-mail "seu vídeo ficou pronto" para quem fechou a aba, que hoje simplesmente
não recebe nada; e (c) o convite do Post to Earn no momento certo. Um único registro server-side
conserta três alavancas que estão sendo tratadas como três problemas separados.
**Métrica:** entregas registradas sem participação do cliente / entregas totais. **Falsifica-se
se:** a taxa de 2º vídeo não se mexer em 7 dias depois de ligado.

## MUDANÇAS NO PROMPT
1. **"NÃO É ABUSO" NÃO AUTORIZA A CORREÇÃO — só descarta uma objeção.** Medi que não havia
   concentração e tratei isso como sinal verde. A pergunta que faltava não era *quem se beneficia*,
   era **quem escreve o registro em que eu estou me apoiando**.
2. **PROVA DE ENTREGA ESCRITA PELO CLIENTE NÃO É PROVA.** Antes de usar uma tabela como evidência
   numa decisão de cota/dinheiro, perguntar **quem faz o INSERT** — servidor ou navegador. Se for o
   navegador, ausência de linha não prova nada.
3. **A RESPOSTA PODE JÁ ESTAR COMENTADA NO ARQUIVO QUE ESTOU EDITANDO.** A frase que derrubou minha
   correção estava duas linhas acima do meu diff. Ler os comentários do bloco que se altera é parte
   da Regra Zero, não cortesia.
4. **INSTRUMENTO É ENTREGÁVEL LEGÍTIMO.** Quando a medição não distingue as duas hipóteses,
   commitar o medidor e não o remédio é o resultado certo da sprint — não um fracasso.

## PARA AS PRÓXIMAS SPRINTS, NA ORDEM
1. **`/api/render/[id]` sem checagem de dono** (vazamento de vídeo + refund no ledger alheio).
2. **Registro de entrega server-side** (a IDEIA CEO acima) — destrava cota, e-mail e Post to Earn.
3. Cobrar `autopilot_page_viewed` com `on_paid_plan` assim que houver tráfego.
4. Ler `undelivered_reservations` / `blocked_by_undelivered` em produção e cruzar com o status real
   do render no Creatomate — só então decidir a correção da cota.
5. ORDEM R-v2 · ORDEM M · GO da L · G2 (lança 10/08) · afiliado ponta a ponta · `video_ready_viewed`.

## GATE DESTA SPRINT
**`scripts\53-PUSH.bat`** — `request_access` devolveu a mesma mensagem de sempre (uma tentativa só).
Sobe o commit da cota **e** o REVERSE TRIAL fase 1 da sessão paralela.
