# SPRINT 2026-08-12

Arquivo do dia. Cada sprint acrescenta a sua seção; ninguém refaz o que já está aqui.

---

## Sprint da madrugada (01h–03h) — já entregue antes das 10h

| commit | entrega |
|---|---|
| `c834b3c` | **LICENÇA COMERCIAL VISÍVEL** `[KINEO-COMMERCIAL-LICENSE-2026-08-12]` — a primeira pergunta de qualquer agência agora tem resposta na página de preço |
| `5335ce5` | **B2B CÓPIA E COLA** `[KINEO-B2B-WEDGE-2026-08-12]` — 3 mensagens por balde para 32 alvos Fiverr/Upwork + a aritmética que vende |
| `c932c11` | **UI 1% #1** `[KINEO-UI-NORTH-STAR-2026-08-12]` — os 6 vídeos da galeria passam sozinhos em viewport + backlog de 20 dias |

---

## Sprint 10h — A PAREDE DO FUNIL TEM NOME E FOI MEDIDA HOJE

### O achado

Não é opinião, é consulta em produção (12/08 ~10:15Z):

| número | valor |
|---|---|
| trials ativos | **98** |
| nunca geraram um vídeo | **50 (51%)** |
| desses 50, quantos TENTARAM e falharam | **0** |
| desses 50, quantos NUNCA TENTARAM | **50** |
| créditos concedidos parados nessas contas | **1.990** |
| desses, expiram em 48h | **17 contas** |
| receberam o `d0_welcome` | **44 dos 50** |
| efeito do `d0_welcome` (93 envios, 76 maduros >24h) | **6,5%** |
| efeito do `d0_welcome` entre quem nunca gerou | **0%** |

**A leitura que muda a prioridade da empresa:** metade do funil não morre por bug
de render, por preço, nem por falta de tráfego. Morre porque **ninguém clicou a
primeira vez**. Zero falhas entre os 50 — não é produto quebrado, é produto não
tocado.

E o e-mail feito exatamente para isso chegou (44 de 50) e não moveu ninguém.
Ele não falhava na entrega nem no texto: falhava no **destino**. O CTA levava
para `/generate` puro — uma caixa de texto vazia pedindo uma ideia. Quem não tem
ideia não digita. Essa é a parede inteira.

### O que foi entregue

**1. `a384733` — o d0_welcome passa a levar 3 temas de 1 clique**
`[KINEO-D0-ONE-CLICK-2026-08-12]`

REGRA ZERO pagou de novo: **o trilho de 1 clique já existia**.
`/generate?prompt=<texto>&create_intent=fast` prefila e dispara sozinho, e já é
usado pelas 28 páginas de SEO e pela `/checkout/success`. O `d0_welcome` era a
**única superfície de aquisição que ainda não usava**. Nada novo foi construído.

Agora o e-mail leva 3 temas de **verticais distintas** (do mesmo
`VIRAL_TOPICS_POOL` que `/viral-now` serve — fonte única, sem cópia local), o
melhor de cada vertical por `viralScore`, girados por FNV-1a do id da conta.

Duas restrições do código, **conferidas e não supostas**:

- o autostart faz `consumeAndSkip('empty_prompt')` sem `prompt=` com texto —
  mandar só `viral_topic=` seria um clique que não faz nada, **exatamente o modo
  de falha do item 7**;
- `prompt` é cortado em 1000 chars e os prompts do pool têm ~1.400 de roteiro
  estruturado — sairiam truncados no meio de uma cena. O link leva o **título
  curto** e o AUTO-STRUCTURE (v2.5) monta o roteiro.

Caminho traçado ponta a ponta antes de escrever: `generatePath()` preserva os
params no login (clique deslogado sobrevive); `create_intent=fast` derruba
`trialDefaultsToCreatorEngine` e cai em `setMode('fast')` (1 crédito, não 20); o
handshake do primeiro minuto pago só gateia `utm_source=checkout_success`, não
este rail; e um scanner de e-mail corporativo faz GET sem JS nem sessão, então
não dispara geração.

**2. `05f2461` — o ending_soon para de pedir compra a quem nunca viu o produto**
`[KINEO-STALLED-ENDING-2026-08-12]`

O commit anterior **não salva nenhuma das 50 pessoas já paradas**: o
`d0_welcome` tem claim permanente na `PK(user_id,email_kind)` de
`trial_emails_log` — elas já receberam e nunca receberão outro. Ele só serve
para as próximas.

Quem ainda alcança essas 50 é o `ending_soon`. E o `ending_soon` dizia:

> "If Kineo's been working for you, keep everything exactly as it is"
> [botão] **Keep Creator** → `/pricing`

Para quem nunca gerou nada a frase é **literalmente falsa** e o pedido é o
errado: pedir para comprar um produto que a pessoa nunca viu funcionar uma vez.
**A conversão não é o próximo passo dessas 17 pessoas — a primeira execução é.**
A única conversão da história do produto veio de uma conta que tinha vídeo pronto.

Agora, quando `creditsUsed <= 0`, o `ending_soon` vira o mesmo bloco de 1 clique.
O link para `/pricing` **continua no e-mail**, só deixa de ser o único caminho
oferecido a quem ainda não tem o que avaliar. Quem já gerou recebe o e-mail de
sempre, byte a byte.

**3. `scripts/71-PUSH.bat` — o push estava preso desde ontem por EOL**

O `70-PUSH.bat` nasceu **em LF, na raiz do repo**, e o histórico não tem exceção:
65/66/69 em CRLF rodaram; 67/68/70 em LF não. Ele ainda dizia "5 commits" quando
havia 1. Foi renomeado para `.LF-NAO-RODA-USE-scripts-71` para ninguém clicar
nele de novo. O 71 nasce em CRLF, não cita contagem de commit nenhuma (o passo 3
imprime a lista real na hora) e não faz `add`/`commit`/`reset` — só apaga locks
órfãos e empurra.

### Revisão adversarial — 3 passadas, 3 defeitos, todos MEUS

O padrão registrado no PROMPT-DIARIO se repetiu pela 6ª vez: **a passada seguinte
acha o defeito que a correção anterior criou.**

1. **1ª passada:** o comentário do seed dizia que ele servia para "sobreviver a
   reenvio". Reenvio não existe — a PK de `trial_emails_log` é claim permanente.
2. **2ª passada:** a correção da 1ª citou `MAX_PER_KIND`, **símbolo que não
   existe** (a linha 296 é `KIND_PRIORITY`, ordem de prioridade, não teto).
   Símbolo inventado dentro de uma correção de comentário falso.
3. **3ª passada — a única em código:** eu escrevi `worth doing before ${when}` e
   `after ${when}`. `when` vem de `endingSoonTiming()` e tem **quatro** valores,
   três preposicionados: `in less than an hour`, `in about N hours`, `tomorrow`,
   `in N days`. "before **in 2 days**" e "after **in less than an hour**" são
   gramática quebrada — sairiam em **3 dos 4 casos**, e o caso mais urgente
   (menos de uma hora) é um dos quebrados. `when` só lê bem depois de VERBO
   ("ends in 2 days"), nunca depois de preposição. Trocado por "before then" /
   "after that", e **as quatro variantes foram renderizadas uma a uma** antes do
   commit.

Confirmações mecânicas: `tsc` **falsificado** (erro plantado → `EXITCODE=2` →
restaurado → `EXITCODE=0`); nenhum dos 35 títulos do pool contém `& < > "`, então
nenhum quebra o HTML; URLs ~200 chars, bem abaixo do corte de 1000; fallback de
pool vazio devolve o e-mail anterior byte a byte em vez de sair sem CTA.

Commits feitos com índice temporário em `$HOME` + `commit-tree` + escrita direta
do ref, porque **o índice do repo segue com 28 deleções preparadas** — um
`git commit` normal apagaria `lib/firstWinHandshake.ts`, os docs de coorte e mais.
`diff-tree --numstat` conferido nos dois: **só os arquivos pretendidos**. Presença
de `lib/firstWinHandshake.ts`, `lib/indexnow.ts`, `docs/SPRINT-2026-08-11.md` e
`docs/B2B-COPIA-E-COLA-2026-08-12.md` reconferida no HEAD depois de escrever o ref.
A escrita direta do ref foi autorizada por **parentesco verificado** (tree e
parent conferidos contra o esperado, `merge-base --is-ancestor` OK) — nunca pelo
conteúdo do `.lock`, que não é prova de nada.

### O que NÃO foi feito e por quê

- **Nenhum e-mail foi disparado.** O código muda o que o cron manda daqui para
  frente; disparo de lote (`send-stalled-rescue`, 231 pessoas) continua
  dependendo do push chegar em produção.
- **Nada de tráfego pago.** Gate segue fechado e o achado de hoje é argumento a
  mais: não faz sentido comprar entrada enquanto metade de quem já entrou não
  clicou uma vez.

---

## RESUMO PARA O CEO

1. Metade do funil tem uma causa única e medida: **50 dos 98 trials ativos nunca geraram um vídeo — e os 50 nunca sequer tentaram.** Zero falhas de render entre eles. 1.990 créditos parados; 17 contas vencem em 48h.
2. O `d0_welcome` chegou para 44 desses 50 e moveu **0**. Não falhava na entrega nem no texto: o botão levava para uma **caixa de texto vazia** pedindo uma ideia.
3. **Entregue:** os dois e-mails que ainda alcançam essa gente agora levam **3 temas de 1 clique** — clicou, o vídeo começa a escrever e renderizar sozinho. O trilho já existia (28 páginas de SEO e checkout já usam); o e-mail era a única superfície que não usava.
4. **Entregue:** o `ending_soon` parou de pedir **compra** a quem nunca viu o produto rodar. É o último e-mail que as 17 contas expirando ainda recebem, e ele agora pede a **primeira execução**, não o cartão. `/pricing` continua no e-mail, só não é mais o único caminho.
5. **Push estava preso desde ontem por causa de EOL**, não de git: o `70-PUSH.bat` nasceu em LF (67/68/70 em LF nunca rodaram; 65/66/69 em CRLF rodaram). Criei `scripts/71-PUSH.bat` em CRLF e neutralizei o 70.
6. **PUSH PENDENTE — mande qualquer mensagem nesta conversa ou rode `scripts\71-PUSH.bat`.** São 3 commits, e eles destravam também a rota `send-stalled-rescue` (231 pessoas), a licença comercial B2B e o handshake do primeiro minuto pago.
7. Números agora: 1.084 signups (12/24h) · 98 trials ativos · 121 trials totais · 24 vídeos/24h por 17 pessoas · 8 `has_paid` · **1 conversão** · 0 erros de checkout.
8. Revisão adversarial rodou 3 vezes e achou 3 defeitos, **todos meus** — o terceiro era gramática quebrada em 3 dos 4 formatos de prazo, incluindo o mais urgente. Nenhum foi para produção.
9. **Nada a decidir.** Nenhum e-mail foi disparado, nenhum preço tocado, nenhum dólar gasto.

---

# Sprint 13h — 12/08/2026

## A EXTENSÃO AUTOMÁTICA ESTAVA APONTADA PARA O LADO ERRADO DO FUNIL

### O achado (medido antes de tocar em código)

A extensão automática do trial escolhia gente por `trial_credits_used < 10` —
literalmente **"mal tocou no produto"**. O placar dela em produção:

| medida | valor |
|---|---|
| e-mails `trial_extended` enviados | **25** (3º kind mais enviado da casa) |
| geraram ≥1 vídeo **depois** da extensão | **0** |
| converteram | **0** |
| média de créditos usados na coorte | **2,6 de 40** |
| nunca tinham gerado 1 vídeo na vida | **10 dos 25** |

E o efeito colateral é pior que a ineficácia. A extensão reescreve
`trial_status` para `'active'`, então ela **tira a pessoa da coorte
expired/downgraded** — a única que recebe o e-mail da perda e o COMEBACK50 do
D5/D10. O instrumento selecionava quem não usou o produto e o **removia da única
sequência que pede dinheiro**.

**Correção de número para o relatório:** 24 dessas contas estão AGORA dentro do
número "trials ativos", com saldo médio de **39,0 de 40** intactos. O número de
trials ativos está inflado por elas.

### O que NÃO é verdade (e a primeira versão deste doc afirmava)

`expired_offer_d5` e `expired_lastcall_d10` têm **0 envios na história** — mas a
causa é **calendário, não bug**. O fim de trial mais antigo da base é 09/08; o D5
dele abre em **14/08**. A extensão atrasa o D5 em +3 dias para a metade de baixo
do funil, o que é real, mas não explica o zero. Afirmar que explicaria seria o
tipo de causalidade inventada que a revisão adversarial existe para derrubar.

### `dcb1944` — o critério inverteu

`[KINEO-TRIAL-EXTENSION-INVERTED-2026-08-12]`

Critério novo, o da ordem do fundador de 06/08: **+2 dias se 3+ vídeos
concluídos e não assinou**. Aponta para o lado oposto do funil.

**Terceira condição, exigida pela verdade do e-mail e não pela ordem:** ≥1
crédito utilizável **depois** da restauração. Durante o trial o Fast custa 1
crédito de verdade (`compose/route.ts` #1252 — o trial segue o caminho pago
justamente para o render aparecer no teto de 40). Dias sem crédito não são
extensão, são promessa falsa. Quem fez 3+ vídeos e queimou os 40 cai **de
propósito** no `downgraded_loss` e depois no COMEBACK50: para quem esgotou o
teto o pedido certo é o cartão, não o calendário.

Efeito na coorte real de hoje, conferido linha a linha:

- **antes:** 2 extensões, para contas com **1 vídeo** cada (`used=1`);
- **depois:** 2 extensões, para contas com **4 e 3 vídeos** (`used=23` e `22`);
- as 3 contas com 3+ vídeos e `used=40` deixam de receber dias inúteis e vão
  para a perda → COMEBACK50;
- as 2 de baixa intenção entram na sequência que pede dinheiro.

Mesmo volume, pessoas opostas. **Nenhum teto foi tocado:** a restauração
continua sendo `granted − used`, ≤40 por construção.

Contagem de vídeos: leitura nova e **paginada** de `videos` (status completed),
50 contas por bloco, **ordenada por id**. `null` = leitura falhou ⇒ **falha
fechada** na coorte pós-trial inteira (o cron é horário e a janela da perda é de
48h — adiar uma hora não custa e-mail nenhum), com `video_counts_degraded` nas
**três** respostas JSON, para um 200 com a coorte adiada não se parecer com um
200 normal.

### `ee01c20` — e o destino dela também estava mentindo

`[KINEO-LOSS-NEVER-RAN-2026-08-12]`

Corrigir a extensão **traz** os 0-vídeo para o `downgraded_loss`. E o
`downgraded_loss` dizia, para todo mundo:

> "The videos you already made are yours — they stay in your account."

Para quem nunca gerou um vídeo a frase é **falsa**, e o destinatário confere em
um clique no `/history`. Mudar o endereço de um e-mail falso não é consertá-lo.
**Coorte que passa a chegar lá: 51 trials ativos com ZERO vídeo.**

Para `videosMade === 0`: some a frase falsa; a afirmação sobre o que a casa
mandou usa a **única forma provada segura** (ENGAGEMENT-LOG 11/08, 3 tentativas
falsas antes dela) — fala do **resultado**, não de quais jobs dispararam; e o
primeiro caminho oferecido deixa de ser `/pricing` e passa a ser o **trilho de 1
clique que já existe** (`oneClickBlocks`, o mesmo do `d0_welcome` e do
`ending_soon` — REGRA ZERO, nenhuma cópia nova). `/pricing` **continua** no
e-mail, só deixa de ser o único caminho. Quem tem 1+ vídeo recebe o e-mail de
antes, byte a byte.

### Revisão adversarial — 6 passadas no total, 6 defeitos, todos meus

O padrão do PROMPT-DIARIO se repetiu pela 7ª vez: **a passada seguinte acha o
defeito que a correção anterior criou.**

No commit da extensão:

1. a **paginação** (que foi a 1ª correção, contra truncamento silencioso) não
   tinha `ORDER BY`. `.range()` sobre ordem instável **duplica linha numa página
   e pula na outra** — e o erro sairia como número errado de vídeos **dentro do
   e-mail**;
2. o comentário da leitura afirmava "blocos de CHUNK_SIZE" quando o bloco é de
   **50**, não 200. Número afirmado em comentário entra na revisão como código;
3. a copy dizia "You made N videos **on your Creator trial**" enquanto a
   contagem vem de `videos` inteira — creditava ao trial vídeos **anteriores** a
   ele.

No commit da perda:

4. `videosMade` só descia no retorno da extensão. O `base` carrega **0**, e 0 é
   exatamente o valor que o e-mail lê como "nunca gerou nada" — a versão "você
   nunca rodou" iria para **100% da coorte**, inclusive para quem fez 12 vídeos.
   Corrigido com `postBase` em todos os retornos pós-trial;
5. `starterTopics(c.id)` devolveria **os mesmos três temas** que o `d0_welcome` e
   o `ending_soon` já mandaram para a mesma pessoa. Ela não clicou nos dois
   primeiros; mostrar os mesmos três pela terceira vez é o pedido de menor
   rendimento possível. Semente `${id}:loss` — offset diferente em **4 de 4** ids
   reais testados;
6. o assunto era "Your free video is still waiting", que **afirma cota
   disponível**. `reserveFreeFastPreviewSlot` reserva **antes** do render, então
   um render que falhou consome a cota e não deixa vídeo concluído — existe conta
   com 0 vídeos **e** 0 cota. Trocado por "Your first video is one click away",
   que afirma só o que o código garante.

Confirmações mecânicas: `tsc` **falsificado** (erro plantado → `EXITCODE=2` →
restaurado → `0`) e `prove-trial-clock-monotonic` **falsificado** (critério
afrouxado → exit 1 → restaurado → 0), repetidos depois de cada passada; as 4
variantes da copy da extensão renderizadas uma a uma.

O script de prova ganhou o **gate sintático** contra o retorno do critério
invertido e a **seção 9**, que prova que o ramo da extensão segue inerte na
fixture de relógio (9 linhas × 9 instantes = **0 disparos**) — ou seja, nenhuma
conclusão das seções 1-6 mudou por causa da troca. Ele já tinha avisado sozinho:
`EXTENSION_MAX_CREDITS_USED` sumiu do route.ts e a seção 7 quebrou, que é
exatamente para isso que aquele gate existe.

Commits feitos com índice temporário em `$HOME` + `commit-tree` + escrita direta
do ref (o índice do repo segue com 28 deleções preparadas). `diff-tree --numstat`
conferido nos dois: só os arquivos pretendidos. Presença de
`lib/firstWinHandshake.ts`, `lib/indexnow.ts`, `docs/SPRINT-2026-08-11.md`,
`docs/B2B-COPIA-E-COLA-2026-08-12.md` e `scripts/71-PUSH.bat` reconferida no HEAD
depois de escrever o ref. Escrita autorizada por **parentesco verificado**, nunca
pelo conteúdo do `.lock`.

### O que NÃO foi feito e por quê

- **Nenhum e-mail disparado.** O código muda o que o cron manda daqui para
  frente; nada sai antes do push chegar em produção.
- **Nenhum diretório novo submetido.** Submeter formulário e publicar conteúdo
  público são ações que exigem autorização explícita do fundador na conversa, e
  várias exigem criar conta com senha — fora do que eu posso fazer sozinho. O
  gate de tráfego pago segue fechado, e o achado de hoje é argumento a mais:
  não se compra entrada enquanto metade de quem já entrou não gerou um vídeo.
- **Nada de preço, teto, marca d'água ou dinheiro.**

---

## RESUMO PARA O CEO — sprint 13h

1. **PUSH PENDENTE — mande qualquer mensagem nesta conversa ou rode `scripts\71-PUSH.bat`.** São **6 commits** represados (4 de ontem/hoje + 2 desta sprint).
2. Achei o instrumento mais caro do funil apontado para o lado errado: a **extensão automática do trial premiava quem NÃO usou o produto** (critério: "gastou menos de 10 dos 40 créditos"). Placar dela: **25 envios, 0 vídeos depois, 0 conversões**, média de 2,6 créditos usados.
3. Pior: a extensão devolve a pessoa para `trial_status='active'`, o que a **tira da única sequência que pede dinheiro** (e-mail da perda + COMEBACK50). **Correção de número:** 24 contas assim estão dentro do seu "trials ativos", com 39 de 40 créditos intactos — o número está inflado.
4. **Entregue:** o critério virou o da sua ordem de 06/08 — **+2 dias para quem fez 3+ vídeos e não assinou**. Na coorte de hoje isso troca 2 extensões para contas de 1 vídeo por 2 extensões para contas de 4 e 3 vídeos. Mesmo volume, pessoas opostas.
5. **Entregue:** quem esgotou os 40 créditos **não** ganha dias que não pode gastar — vai direto para o e-mail da perda e o COMEBACK50. Para quem estourou o teto o pedido certo é o cartão, não o calendário. Nenhum teto foi tocado.
6. **Entregue:** o e-mail da perda parou de dizer *"os vídeos que você já fez são seus"* para quem tem **zero** vídeo, e passou a levar **3 temas de 1 clique** em vez de só um link para `/pricing`. **51 trials ativos com zero vídeo** caem nesse e-mail.
7. O COMEBACK50 (D5) nunca foi enviado, mas a causa é **calendário, não bug**: o fim de trial mais antigo é 09/08 e o D5 dele abre em **14/08**. Depois deste push ele sai para a coorte certa.
8. Números agora: **1.068 perfis · 13 signups/24h · 102 trials ativos (24 são reciclados) · 51 deles com ZERO vídeo · 9 vídeos hoje · 16 pessoas geraram em 24h · 7 has_paid · 1 conversão · 0 erros de checkout.**
9. Revisão adversarial rodou **6 vezes** e achou **6 defeitos, todos meus** — inclusive um que mandaria o e-mail "você nunca rodou nada" para 100% da coorte, gente com 12 vídeos incluída. Nenhum foi para produção.
10. **Nada a decidir.** Nenhum e-mail disparado, nenhum preço tocado, nenhum dólar gasto. O gate de tráfego pago segue fechado — e hoje ficou mais fácil de defender.

---

# Sprint 16h — 12/08/2026

## O NÚMERO QUE ORIENTOU O DIA INTEIRO ESTAVA ERRADO — E AS DUAS FONTES QUE O PRODUZIRAM CONCORDAVAM

### A correção

A sprint das 10h publicou, e as 13h herdaram, este achado:

> "dos 50 [trials sem vídeo], quantos TENTARAM e falharam: **0** · quantos
> NUNCA TENTARAM: **50** … não é produto quebrado, é produto não tocado."

Medido hoje às ~19:1xZ na tabela `events` (contas internas fora), para os
**52** trials ativos com zero vídeo concluído:

| medida | pessoas |
|---|---:|
| chegaram a `/generate` | **52 de 52** |
| clicaram `analyze_idea` | **43** |
| chamaram `generate_started` | **35** (83 tentativas) |
| têm falha registrada (`generate_failed`) | **23** |
| falha classificável como **NOSSA** (5xx nosso ou símbolo de apagão) | **22** |
| dessas, com falha **só** dentro do apagão de 09–10/08 | **21** |
| créditos de trial queimados por essas tentativas | **0** |
| vencem nas próximas 24h | **11** |

Não é "produto não tocado". É **produto tocado e quebrado por nós**: 135
respostas `Render service rejected the job` durante as 30 horas em que o
Creatomate esteve fora, até 8 tentativas por pessoa, zero sucessos.

### Por que as duas fontes da sprint das 10h concordavam

`videos` vazio e `trial_credits_used = 0` medem a mesma coisa: **quando a
falha é nossa, o débito não acontece**. O crédito não sai, o vídeo não nasce,
e a pessoa que tentou cinco vezes fica, nas duas tabelas, idêntica à que nunca
abriu o app. A evidência de que ela tentou existe num lugar só — `events`, a
tabela que o PROMPT-DIARIO já mandou colocar no placar em 30/07 justamente por
ser "a única superfície onde falha de entrega paga aparece".

**"Não gastou crédito" não é "não tentou".** Toda coorte definida por ausência
(não gerou, não comprou, não usou) tem de passar por `events` antes de virar
decisão — senão a vítima de um incidente nosso é tratada como desinteressada,
e a copy escrita para ela começa pedindo o que ela já tentou fazer.

### O e-mail que existia para isto nunca saiu

`send-blackout-winback` — cuja copy é literalmente *"The failure was ours"* —
tem **0 envios desde 01/08**, medido. Não é bug de código: ele só age enquanto
houver marcador de apagão nas **últimas 48h**, e o símbolo `creatomate_rejected`
só passou a ser escrito no deploy de 10/08, **depois** que o apagão acabou. O
detector nasceu para o incidente seguinte e a coorte deste ficou órfã para
sempre. As 23 pessoas nunca receberam uma palavra.

## O que foi entregue

**1. `57ac670` — os dois e-mails que ainda alcançam essa gente passam a dizer
de quem foi a culpa** `[KINEO-FAILED-BY-US-2026-08-12]`

`lib/lifecycle/ourFailure.ts` — predicado único de "a falha foi nossa".
**Allowlist**, falha **fechada** em símbolo desconhecido: o erro caro aqui não
é perder uma vítima, é pedir desculpa a quem bateu num paywall. Duas cláusulas,
cada uma com contraexemplo medido: `http_status >= 500` (cobre as 106 linhas de
`compose_not_ok`/502; deixa de fora as 6 de `compose_not_ok`/**400** e todos os
402 de cota) e símbolo de apagão de fornecedor (`openai_hang` não tem status —
a lambda morre antes de existir resposta).

No cron: leitura paginada e ordenada de `events` para a coorte, campo
`failedOnUs`, e as duas copies. **Falha ABERTA**, ao contrário da contagem de
vídeos, e a diferença é o tipo de afirmação: `videosMade` vira número impresso,
este campo só acrescenta um pedido de desculpas. Conjunto parcial é descartado
inteiro — metade classificada e metade não vira "não foi vítima", que é a
afirmação errada com cara de resposta.

**Alcance medido, sem otimismo:** 12 das 22 ainda podem receber o
`ending_soon`; as outras 10 já queimaram o claim permanente da PK (o mesmo
efeito que o `d0_welcome` teve de manhã). As **23** inteiras ainda podem
receber o `downgraded_loss`, que ninguém recebeu — por isso os **dois** ramos
foram corrigidos no mesmo commit. Próximo vencimento: **hoje 22:48Z**.

**2. `02e9a33` — a tela parou de mandar tentar de novo contra a parede**
`[KINEO-REJECTION-COPY-2026-08-12]`

"Render service rejected the job. Please try again." era descrição do nosso
log entregue a quem acabou de escrever um tópico, e o conselho errado repetido
com autoridade. A frase nova diz de quem foi a culpa e o que **não** adianta
mexer. Extraída para `lib/render/rejectionMessage.ts` porque a mesma string
estava em **três** call sites.

O que ela deliberadamente **não** afirma: não promete e-mail de recuperação
(o win-back teve 0 envios — prometer na tela o que a coorte anterior não
recebeu é dívida assinada) e não diz "we're on it" (os dois call sites de
`compose` disparam `alertCreatomateDown`; o de `compose/unlock` **não**).

## Revisão adversarial — 2 passadas, 4 defeitos, todos meus

1. **duas origens de UTM no mesmo e-mail** — texto herdando
   `trial_ending_stalled`, HTML com `trial_ending_failed_by_us`. O CTR desta
   copy, a única pergunta que ela existe para responder, ficaria dividido entre
   dois nomes, com a metade texto somando no balde do ramo que ela substitui;
2. **corrigi a linha de crédito só na metade TEXTO.** A metade HTML seguiu
   dizendo "0 trial credits are still sitting in your account, untouched" e
   "if you want to spend one". Corrigir uma das duas metades de um e-mail é o
   mesmo defeito de corrigir uma de três telas;
3. **o comentário que EU escrevi era falso** — afirmava que
   `send-blackout-winback` "consome esta MESMA constante". Pior que falso: um
   convite para alguém torná-lo verdadeiro. As duas listas diferem em
   `creatomate_unverified` **de propósito**, porque lá o símbolo dispara
   campanha em massa e um soluço de 1s do provedor não pode disparar isso;
4. **"you tried and it NEVER finished" é falso para quem tem vídeo pronto** — e
   o ramo que hospeda a frase filtra por CRÉDITO, não por vídeo. A inferência
   "0 créditos usados ⇒ 0 vídeos" só vale enquanto o trial for concedido no
   cadastro (medido: 0 contas em 51 a contradizem hoje). `failedOnUs` passa a
   conferir `videoCounts`.

Confirmações mecânicas: `tsc` **falsificado** duas vezes (erro plantado →
`EXITCODE=2` → restaurado → `0`), `prove-trial-clock-monotonic` e
`prove-ending-soon-timing` em 0, e as **8** variantes da copy nova (4 formatos
de prazo × com/sem crédito) renderizadas uma a uma — inclusive
"in less than an hour", o formato que quebrou a gramática na sprint das 10h.

## Ideia medida e MORTA antes de virar código

O funil do onboarding foi medido (14 dias, 373 pessoas): 59,8% saem da tela
sem emitir evento, o que parecia instrumento cego digno de correção. **Não é.**
93,7% desse grupo tinha `activation_autostart` — a tela nunca foi a superfície
decisória para eles. Controlando por isso, quem clica converte 37,5% × 28,2%
de quem pula (n=56 e 39). O número bruto diz o **oposto** (44% × 66%) por
paradoxo de Simpson. Achado colateral: `viral_onboarding_primary_clicked` e
`first_video_started_from_viral_onboarding` são o **mesmo clique** — 104 × 104
pessoas, 0,060 s de intervalo médio.

## Dívidas registradas (não corrigidas, de propósito)

- `app/api/compose/unlock/route.ts` recusa com 502 **sem** disparar
  `alertCreatomateDown` — um apagão que só atinja esse caminho é invisível;
- o ramo `neverUsed` do `ending_soon` (das 10h) diz "the **0** credits in your
  account expire with it" se `creditsLeft = 0`. Hoje são 0 contas ativas nesse
  estado; a guarda foi posta **só** na copy nova;
- `tsconfig.sprint16h.json` ficou na raiz: o sandbox não consegue apagar
  (OneDrive). **Apagar pelo Windows** — não entrou em commit nenhum;
- a inferência "0 créditos de trial usados ⇒ 0 vídeos" vira falsa no dia em que
  um free antigo ganhar trial.

## O que NÃO foi feito e por quê

- **Nenhum e-mail disparado, nenhum lote.** O código muda o que o cron manda
  daqui para frente; nada sai antes do push chegar em produção.
- **Nenhuma extensão de trial para as vítimas.** Tentador e errado: extensão
  reescreve `trial_status='active'` e **tira a pessoa** da coorte
  expired/downgraded, a única que recebe o e-mail da perda e o COMEBACK50 —
  o achado estrutural das 13h de hoje. Compensar com dias cancelaria em
  silêncio a única sequência que pede dinheiro.
- **Nada de tráfego pago, preço, teto ou dinheiro.**

## RESUMO PARA O CEO — sprint 16h

1. **PUSH PENDENTE — mande qualquer mensagem nesta conversa ou rode `scripts\71-PUSH.bat`.** São **9 commits** represados.
2. **O número que orientou o dia estava errado, e eu corrigi a fonte:** não é que 50 trials "nunca tentaram". **35 tentaram e 22 foram derrubados por NÓS** — 21 deles dentro do apagão de 30h de 09–10/08. As duas fontes das 10h (`videos`, `trial_credits_used`) concordavam porque **falha nossa não debita**: quem tentou 5 vezes fica idêntico a quem nunca abriu o app.
3. **O e-mail que existe para pedir desculpa por isso nunca saiu**: `send-blackout-winback` tem 0 envios desde 01/08 — ele só age com marcador de apagão nas últimas 48h, e o símbolo passou a ser escrito **depois** do apagão acabar. Essas 23 pessoas nunca ouviram uma palavra nossa.
4. **Entregue:** os dois e-mails que ainda as alcançam passam a dizer de quem foi a culpa, que os créditos estão intactos, e levam o trilho de 1 clique. Alcance real medido: **12** ainda pegam o `ending_soon` (10 já queimaram o claim), e as **23** pegam o `downgraded_loss`. Primeiro vencimento: **hoje 22:48Z**.
5. **Entregue:** a tela parou de dizer "Please try again" para quem já tentou 8 vezes contra uma parede — a mensagem de recusa agora diz que a falha foi nossa e que mexer no roteiro não resolve. Uma string em um lugar só, no lugar de três cópias.
6. **Nada de dias de trial de brinde para as vítimas** — parece justo e cancelaria em silêncio a única sequência que pede dinheiro (a extensão devolve para `active` e tira a pessoa do COMEBACK50). Se você quiser compensá-las, é decisão sua e eu monto.
7. Números agora: **1.075 perfis · 15 signups/24h · 139/7d · 21 vídeos/24h por 15 pessoas · 419 ativados · 108 trials ativos · 7 has_paid · 4 planos pagos · 1 conversão · 0 erros de checkout.**
8. Revisão adversarial rodou 2 vezes e achou **4 defeitos, todos meus** — incluindo um e-mail que sairia com o CTR dividido em dois nomes e outro que afirmaria "0 créditos intactos". Nenhum foi para produção.
9. **Nada a decidir hoje**, exceto o item 6 se você quiser.
