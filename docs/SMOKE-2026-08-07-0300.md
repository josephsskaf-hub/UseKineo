# SMOKE TEST — 2026-08-07 ~03:10Z (última rede antes da madrugada)

Deploy sob teste: `1f1bcd5` / `dpl_4ASRUMvFXZMNZ9tCuQetQkzEBUwy` — READY,
criado 02:57:06Z, pronto 02:57:58Z, alias de produção (`www.usekineo.com`,
`usekineo.com`, `shortsforgeai.com`).
Executado por agente de QA. Nada foi escrito em produção (só leitura).

---

## VEREDITO: **VERDE COM UMA RESSALVA VERMELHA**

O produto público está inteiro, sem 5xx, sem erro novo, sem risco de dinheiro.
**Um defeito confirmado:** o cron `trial-downgrade` rodou às 02:55Z com uma
linha ELEGÍVEL na base e **não a processou**. Esta noite isso não custa nada
(o saldo já é 0), mas se um signup orgânico ativar trial e o relógio vencer,
o crédito não gasto **não será revogado**. Detalhe no caso 4c.

---

## 1. Rotas públicas (12 URLs, `curl` em www.usekineo.com)

Checagem: status HTTP, tempo total, e ausência das strings `3 free Shorts
every 24h` e `[object Object]` no HTML servido.

| Rota | Status | Tempo | Bytes | `3 free Shorts every 24h` | `[object Object]` | Resultado |
|---|---|---|---|---|---|---|
| `/` | 200 | 0,590 s | 173 106 | 0 | 0 | OK |
| `/pricing` | 200 | 0,098 s | 70 518 | 0 | 0 | OK |
| `/signup` | 200 | 0,285 s | 57 776 | 0 | 0 | OK |
| `/login` | 200 | 0,424 s | 57 398 | 0 | 0 | OK |
| `/forgot-password` | 200 | 0,398 s | 44 688 | 0 | 0 | OK |
| `/wall` | 200 | 0,733 s | 62 540 | 0 | 0 | OK |
| `/scripts` | 200 | 0,527 s | 178 279 | 0 | 0 | OK |
| `/vs` | 200 | 0,614 s | 237 185 | 0 | 0 | OK |
| `/facts` | 200 | 0,501 s | 89 048 | 0 | 0 | OK |
| `/free-ai-shorts/money` | 200 | 0,435 s | 87 905 | 0 | 0 | OK |
| `/sitemap.xml` | 200 | 0,429 s | 28 108 | 0 | 0 | OK |
| `/llms.txt` | 200 | 0,388 s | 19 065 | 0 | 0 | OK |

**12/12 OK.** Nenhuma rota acima de 0,74 s. Nenhuma promessa velha de cota
free sobrou no HTML. Zero `[object Object]`.

`/generate` responde **307 → `/signup?redirect=%2Fgenerate`** para anônimo —
o middleware de auth está de pé (esperado, e é o que impede a prova por bundle
abaixo).

---

## 2. Os 2 commits desta leva estão no ar?

### (a) `1f1bcd5` — modal imediato — **PROVADO POR COMPORTAMENTO EM PRODUÇÃO**

Este é o método mais forte que existe, e ele apareceu sozinho nos dados.
A mudança de `1f1bcd5` (`lib/reverseTrial.ts`) foi exatamente esta:

```
-  showDowngradeModal: phase === 'downgraded' && !isPayingProfile(profile) && creditsGranted > 0,
+  showDowngradeModal:
+    (phase === 'downgraded' || phase === 'ending') && ...
```

Evidência (tabela `events` do Supabase):

| evento | horário | user |
|---|---|---|
| `trial_downgrade_modal_shown` | **02:58:21,824Z** | 84c9ddee |
| `trial_downgrade_modal_dismissed` (`how: stay_free`) | 02:58:30,224Z | 84c9ddee |

E o perfil, AGORA: `trial_status = 'expired'`, `trial_downgraded_at = NULL`.

Ou seja: `phase === 'ending'` (venceu por teto, cron ainda não passou) e o
modal **apareceu**. Com o código anterior a `1f1bcd5` esse modal seria
impossível — a guarda exigia `phase === 'downgraded'`. O deploy ficou READY
às 02:57:58Z e o evento é de **23 segundos depois**. Não é inferência: é o
código novo rodando.

### (b) `f9a4a9b` — etiqueta "1 credit" no card do Fast — **PROVADO POR SHA + LINHAGEM**

Tentei primeiro o bundle, como pedido, e **não deu**:
- `/generate` devolve 307 no middleware para anônimo → o RSC da rota nunca é
  renderizado, então os chunks de `app/(dashboard)/generate/page-*.js` não são
  referenciados em lugar nenhum público e o hash não é adivinhável;
- `app-build-manifest.json` → 404;
- o `webpack-8c8963a80b505b41.js` servido tem 4 226 bytes e **não** carrega
  mapa de chunks (`s.u` devolve um literal único), então não há como derivar
  o nome do arquivo;
- `fastCostsCredit` é `const` local → o minifier renomeia de qualquer jeito.

Método usado no lugar (o fallback previsto): **SHA do deploy READY + linhagem
no HEAD**.
- `get_deployment(dpl_4ASRUM…).meta.githubCommitSha` = `1f1bcd55a1c889c87a4bdda97dba18eccd7004ff`;
- `alias` desse deploy inclui `www.usekineo.com`, `usekineo.com`, `shortsforgeai.com` → é ele que serve produção;
- `git rev-parse 1f1bcd5^` = `f9a4a9b132e5f1ddc78022aa35b59d28e8a24627`;
- `git merge-base --is-ancestor f9a4a9b 1f1bcd5` → **YES**.

`f9a4a9b` é o pai direto de `1f1bcd5`. Se `1f1bcd5` está no ar, `f9a4a9b`
está no ar. **OK** (por SHA, não por bundle — declarado).

Observação honesta: a etiqueta não pode ser observada agora nem com sessão,
porque a única conta de trial já está `expired` → `trialActive === false` → o
card volta a dizer `FREE` (correto). A prova comportamental dessa linha só
existe com um trial ATIVO.

---

## 3. Logs de runtime (Vercel, janela de 45 min: 02:16Z → 03:01Z)

| Métrica | Valor | Resultado |
|---|---|---|
| `group_by=statusCode` | 200: 471 · 304: 21 · 404: 17 · 307: 4 · 401: 3 | OK |
| Filtro `statusCode=5xx` | **"No logs found"** — zero 5xx | OK |
| `get_runtime_errors` / exceções novas | nenhuma | OK |
| `cap update failed` | 0 ocorrências | OK |
| `ANTI-ABUSE INACTIVE` | 0 ocorrências | OK (salt está setado — ver 4d) |
| `[reverse-trial]` | 1 linha: `EXPIRED user=84c9ddee used=41/40` às 02:47:05Z | OK, esperado |

Os 3 `401` são o `/api/cron/trial-downgrade` às 02:17:42Z e afins — cron auth
fail-closed funcionando contra chamada sem `CRON_SECRET`. Não é incidente.

---

## 4. Banco (Supabase `cqqukkvjjrguayiyjvhh`)

### 4a. Perfil de teste `84c9ddee-1404-48d0-b7a6-163c860fad0a`

| Campo | Valor | Esperado | Resultado |
|---|---|---|---|
| `email` | josephsskaf+t1@gmail.com | — | — |
| `trial_status` | `expired` | expired/downgraded | OK |
| `video_credits` | **0** | 0 | OK |
| `trial_credits_used` | **41** | 41 | OK |
| `trial_credits_granted` | 40 | 40 | OK |
| linhas em `trial_debit_ledger` | **3** | 3 | OK |
| `plan` / `is_pro` / `has_paid` | free / false / false | não-pagante | OK |
| `trial_downgraded_at` | `NULL` | — | ver 4c |

Ledger (a contabilidade fecha, e isto **exercita** o fix `115985d`):

| render_id | custo | horário |
|---|---|---|
| `cinematic-0ea18752…` | 20 | 01:31:47,264Z |
| `95089b6d-…` (Fast) | **1** | 02:38:12,351Z |
| `cinematic-a60347de…` | 20 | 02:47:06,387Z |

20 + 1 + 20 = **41 = `trial_credits_used`**. Três débitos reais, três linhas,
zero replay. O bug do teto contando o mesmo débito duas vezes (`115985d`)
estava deployado-e-não-exercitado no QA anterior (`cb927d8`); **agora está
exercitado e correto**. OK.

Nota lateral (não é defeito): 41 > 40. O último débito custou 20 quando
faltavam 19 — o teto é "não começa se já bateu", não "não ultrapassa". Não há
saldo negativo (`video_credits = 0`) e a própria função comenta esse caso
(`used` pode ultrapassar `granted`). Só registrando para não virar susto.

Nota lateral 2: o evento `trial_downgrade_modal_shown` gravou
`credits_used: 40` (clampado no cap) enquanto o real é 41. Cosmético,
telemetria; não afeta dinheiro nem tela.

### 4b. Contagem de trials na base

| Consulta | Valor |
|---|---|
| `trial_status = 'active'` | **0** |
| `trial_status = 'expired'` | **1** |
| `trial_status = 'downgraded'` | **0** |
| `trial_status IS NOT NULL` (total histórico) | **1** |
| `trial_signup_fingerprints` | **1** |
| `trial_emails_log` | **0** |
| `events` `trial_downgraded` (todo o histórico) | **0** |

`trial_emails_log` vazio é **esperado**: o único cron que escreve nele é
`trial-lifecycle-emails`, que roda 16:30Z e ainda não rodou desde o go-live.

### 4c. 🔴 Perfis criados nas últimas 2h sem trial + o achado real

Perfis criados nas últimas 2h: **1** — o próprio `84c9ddee` (01:18:41Z), e ele
**recebeu** o trial (`trial_credits_granted` às 01:28:22Z, variante `7d`).
**Nenhum perfil das últimas 2h ficou sem trial.** OK.

Últimas 24h, para contexto (4 perfis sem trial, todos com motivo exato):

| email | criado | trial | motivo |
|---|---|---|---|
| josephsskaf+t1@ | 07/08 01:18Z | ✅ 7d | — |
| humansecret007@ | 07/08 00:02Z | ❌ | criado **antes** de `cd750ea` (ativação server-side, deploy 01:26:52Z). A ativação de então dependia de chamada do CLIENTE com trava de sessão. |
| tanaewilliams123@ | 06/08 18:56Z | ❌ | criada **antes do go-live do Reverse Trial** — o deploy `028463d` ("GO-LIVE") ficou pronto às **01:03:37Z de 07/08**. Não havia trial a conceder. |
| tepperson098@ | 06/08 15:59Z | ❌ | idem (anterior a 01:03:37Z de 07/08) |
| chibuikesamuel546@ | 06/08 04:27Z | ❌ | idem (anterior a 01:03:37Z de 07/08) |

Todos os 4 têm `video_credits = 0`. **Não é perda de dinheiro** — é 4 contas
que não ganharam brinde. E `maybeActivateReverseTrial` é idempotente e roda no
servidor no `/generate`: se qualquer uma delas voltar e abrir o gerador, ela
ganha o trial ali (comportamento correto, não requer ação).

**🔴 O ACHADO QUE IMPORTA — o cron `trial-downgrade` não processou uma linha devida.**

Cronologia com evidência:

| horário | fato | fonte |
|---|---|---|
| 02:47:05Z | `[reverse-trial] EXPIRED user=84c9ddee used=41/40` → `trial_status = 'expired'` | log de runtime + evento `trial_expired` 02:47:06,526Z |
| **02:55:07Z** | `GET /api/cron/trial-downgrade` → **200**, `dep=dpl_DuVk…`, **nenhuma linha de console** | log de runtime |
| agora (03:10Z) | perfil segue `trial_status = 'expired'`, `trial_downgraded_at = NULL` | query no banco |

Aplicando `trialNeedsDowngrade()` (lib/reverseTrial.ts:259) à linha como ela
estava às 02:55:07Z: `status = 'expired'` ✅ passa o primeiro guard, e
`trialCapReached` = `41 >= 40` = **true** → a função devolve **true**. A linha
**era da coorte**. O cron devia ter escrito `downgraded` e logado
`[trial-downgrade] {…}` + `[reverse-trial] DOWNGRADED …`. Não escreveu nem
logou (o `console.log` do payload só acontece com `ids.length > 0`; não houve).
E `events.trial_downgraded` tem **0 linhas em todo o histórico**.

Duas confirmações independentes (banco não mudou + nenhum log) apontam para
`due.length === 0` numa rodada em que devia ser 1. **Causa raiz não
determinada** — não consigo invocar a rota (fail-closed por `CRON_SECRET`) e
os logs não trazem o corpo. Fica registrado como fato observado, não como
diagnóstico.

**Impacto ESTA noite: nenhum centavo.** O único perfil elegível já está com
`video_credits = 0`, e `revoke = max(0, granted − used) = max(0, 40−41) = 0` —
não há saldo a revogar. O entitlement já está correto (a expiração é passiva:
`isTrialActive` devolve false desde 02:47Z) e o modal de upsell já aparece
sozinho graças ao `1f1bcd5`.

**Impacto se um signup orgânico ativar trial esta noite:** se ele vencer pelo
RELÓGIO com saldo não gasto (ex.: usou 12 dos 40), esses ~28 créditos **não
serão revogados** e viram crédito vitalício por endereço de e-mail — que é
literalmente "a dívida #1" descrita no cabeçalho do próprio cron. Probabilidade
esta madrugada é **baixíssima**: os trials são de 3d/7d, então ninguém que se
cadastrar hoje vence pelo relógio antes de 10/08. O caminho por TETO exige
gastar 40 créditos (2 vídeos cinematográficos) na mesma noite.

**Teste falsificável já marcado (custa zero e resolve a dúvida):**
> Às **03:55Z** o cron roda de novo, agora em `dpl_4ASRUM`. Se ele estiver são,
> o perfil `84c9ddee` vira `trial_status = 'downgraded'` com
> `trial_downgraded_at ≈ 03:55Z` e `credits_revoked = 0`.
> **Checar às ~04:00Z.** Se continuar `expired`, o cron está quebrado e vira
> item 1 do próximo sprint.

### 4d. Anti-abuso

`trial_signup_fingerprints` tem **1 linha** para **1 trial concedido**. Se o
`TRIAL_FINGERPRINT_SALT` não estivesse setado, o código emitiria
`[reverse-trial] ANTI-ABUSE INACTIVE: … every signup is getting a trial with no
device/IP check` (lib/reverseTrial.ts:634) — **essa linha não aparece nos logs
e a tabela está sendo escrita**. Anti-abuso **ATIVO**. OK.

---

## 5. Crons das próximas 8 horas (03:10Z → 11:10Z), lidos de `vercel.json`

| UTC (próximas execuções) | Rota | O que faz | Toca crédito? | E-mail? | Risco |
|---|---|---|---|---|---|
| :05 e :35 de toda hora | `send-blackout-winback` | winback de quem ficou sem vídeo | não | **sim** | baixo — trava de reenvio por reserva atômica |
| :10 e :40 de toda hora | `send-video-ready` | avisa vídeo pronto | não | **sim** | baixo — carimbo `video_ready_sent_at` |
| :15 e :45 de toda hora | `send-cap-hit` | avisa quem bateu no teto | não | **sim** | baixo — índice único parcial em `events` (fix de 05/08) |
| :40 de toda hora | `send-activation-nudge` | cutuca quem não gerou | não | **sim** | baixo |
| :50 de toda hora | `send-post-nudge` | convida a postar (Post to Earn) | não | **sim** | baixo — reserva atômica antes do Resend |
| **:55 de toda hora** | **`trial-downgrade`** | **revoga saldo não gasto de trial vencido + carimba `downgraded`** | **SIM (escreve `video_credits`)** | não | **⚠️ ver 4c** |
| :00 de toda hora | `autopilot-generate` | passe 1 publica render em voo (LIQUIDA crédito), passe 2 dispara render novo | **SIM (gasta)** | não | médio-baixo — só toca contas com Autopilot agendado; unique `(schedule_id, date, slot)` impede render duplo |
| 04:20, 06:20, 08:20, 10:20 | `send-recovery` | recuperação de checkout | não | **sim** | baixo |
| 05:15 | `refresh-viral-now` | repopula os 3 cards trending | não | não | nenhum |
| 06:45 | `wall/refresh` | atualiza o /wall | não | não | nenhum |
| 09:30 | `refund-sweep` | **devolve** crédito de render travado/abandonado | **SIM (credita)** | **não** (comentário do arquivo: nenhuma das 2 funções toca a Resend) | baixo — só credita, direção segura |
| 10:00 | `send-reminders` | lembretes | não | **sim** | baixo |

**Fora da janela (confirmado):**
- **`trial-lifecycle-emails` — `30 16 * * *` = 16:30Z = 13:30 BRT. NÃO roda de
  madrugada.** ✅ Confirmado. É o cron que dispara os 5 e-mails do trial
  (`d0_welcome`, `ending_soon`, `expired_offer_d5`, …). A primeira execução
  desde o go-live acontece **com o fundador acordado**, o que é a ordem certa —
  `trial_emails_log` está em 0 e o primeiro disparo será observável.
- `refresh-niche-trends` 12:00Z, `send-video-rescue` 14:00Z, `send-credits-back`
  15:25Z (este ESCREVE crédito) — todos após 11:10Z.
- `reset-cinematic-tokens` `0 0 1 * *` — dia 1 do mês. Hoje é 7. Não roda.

**Quem pode escrever em crédito sem supervisão nas próximas 8h:**
`trial-downgrade` (:55, revoga), `autopilot-generate` (:00, gasta),
`refund-sweep` (09:30, credita). Os três têm `CRON_SECRET` fail-closed, teto
por rodada e idempotência declarada. O único com defeito observado é o
`trial-downgrade`, e o defeito dele é **omissão** (não revoga), não escrita
indevida — falha para o lado do usuário, não contra ele. **Não recomendo
desligar nada.**

---

## 6. Sanidade de dinheiro

| Checagem | Evidência | Resultado |
|---|---|---|
| Perfis pagantes alterados nas últimas 3h | 9 perfis pagantes; nenhum aparece em `credit_debits`, `events` ou `trial_debit_ledger` na janela | OK |
| Débitos inesperados nas últimas 3h | `credit_debits` = **3 linhas**, todas do perfil de teste `84c9ddee` (20 · 1 · 20), `refunded_at = NULL` | OK |
| Saldos negativos | nenhum (`video_credits ≥ 0` em toda a base) | OK |
| Perfis pagantes com trial aberto | 0 (`trial_status` é NULL nos 9) | OK |
| `has_paid`/`is_pro` mexido | nada nas últimas 3h | OK |

Os 9 pagantes seguem intactos, incluindo os 3 com `plan='free'` + `has_paid=true`
e o 1 com `plan='pro'` sem `has_paid` — a razão de `isPayingProfile` ser
denylist invertida. Nenhum deles é elegível a nada que os crons da noite façam.

---

## Resumo executivo

**PASSOU (14 casos):** 12 rotas públicas, zero 5xx, zero exceção nova, zero
`cap update failed`, zero `ANTI-ABUSE INACTIVE`, anti-abuso ativo, perfil de
teste exatamente no estado esperado (expired / 0 créditos / used 41 /
ledger 3), fix do double-count `115985d` **agora exercitado de verdade**,
`1f1bcd5` provado por comportamento em produção, `f9a4a9b` provado por SHA +
linhagem, `trial-lifecycle-emails` confirmado fora da madrugada, e dinheiro
intocado.

**PREOCUPA (1 caso):** `trial-downgrade` não processou a única linha devida em
02:55Z. Custo esta noite: zero. Custo se repetir com um trial que sobre saldo:
crédito vitalício. **Ação: checar o perfil `84c9ddee` às ~04:00Z** — se ele
virou `downgraded` às 03:55Z, foi ruído; se continuar `expired`, é o item 1 do
próximo sprint.

**Nada precisa ser desligado.** A flag `KINEO_REVERSE_TRIAL_ENABLED` deve
continuar ON: desligá-la agora não conserta o cron (ele é deliberadamente
independente de flag) e apagaria o único canal de ativação que está
funcionando. Pode dormir.

---

## ADENDO 03:40Z — a RESSALVA VERMELHA foi diagnosticada e corrigida
`[KINEO-DOWNGRADE-CRON-FIX-2026-08-07]`

**A ressalva era pior do que o smoke conseguiu ver: não foi 1 rodada, foram 2.**

| rodada | estado do perfil `84c9ddee` naquele instante | elegível? | processou? | console |
|---|---|---|---|---|
| 00:55:22Z | trial ainda não existia (ativado 01:28:22Z) | não | — | vazio (correto) |
| 01:55:17Z | `expired`, used 40/40 desde 01:31:47Z, prazo 14/08 | **SIM** | **NÃO** | **vazio** |
| 02:55:07Z | `expired`, used 41/40 desde 02:47:06Z, prazo 14/08 | **SIM** | **NÃO** | **vazio** |

Evidência de que o console vazio é REAL e não um artefato da busca de logs: no
mesmo intervalo, `GET /api/cron/send-post-nudge` às 02:50:42Z — mesmo
deployment `dpl_DuVkMA9WZya7ugxwbj1DQZvpPZaj`, mesmo padrão de cliente
service-role, mesma tabela `profiles` — logou `[lifecycle-suppression] 3/3
suprimido(s)`. Logo: leitura de `profiles` com service-role funcionava, e o
`trial-downgrade` chegou ao fim do handler com `ids.length === 0`.

**Hipóteses eliminadas com evidência (nenhum chute):**
- *coorte só olhava `active`* — **falso**: o SQL era `.in('trial_status',
  ['active','expired'])`, e o arquivo não muda desde `89a235d`, ancestral do
  SHA deployado `115985d`. `git diff 115985d HEAD` na rota: vazio.
- *filtro `trial_ends_at < now()` na coorte* — **falso**: não existe predicado
  de tempo no SQL; o autor tirou de propósito.
- *guarda de flag/segredo saindo cedo* — **falso**: 200, não 401/500; e o cron
  não é gateado por `KINEO_REVERSE_TRIAL_ENABLED`.
- *`isPayingProfile` classificando errado* — **falso**: ela só roda DEPOIS da
  coorte, e classificaria como `converted`, que escreveria `trial_downgraded_at`
  e um evento. Banco intacto.
- *`trial_credits_granted` ausente no SELECT da coorte* — **falso**: a coorte não
  precisa dessa coluna; o juiz usa `trial_credits_used` (41), que está no SELECT.
- *RLS / chave errada* — **falso**: `send-post-nudge` leu `profiles` 5 min antes.

**O que sobrou, e é a causa raiz:** a coorte era a única GET **sem parâmetro
variável** do projeto (idêntica byte a byte em toda rodada), servida a partir do
cache de `fetch` do App Router com o `[]` das horas em que não havia trial; e a
linha `if (ids.length > 0) console.log(...)` fazia o cron ficar mudo
**exatamente** no caso de falha. Corrigido: `cache: 'no-store'` explícito,
coorte larga (SQL só exclui os terminais, e pela mesma constante que o juiz
usa), log incondicional, `skip_reasons` agregado, e um `cohort_count` do próprio
banco que transforma "leitura vazia com banco cheio" em **500
`cohort_read_mismatch`** em vez de 200 silencioso.

**Teste falsificável, para conferir DEPOIS do deploy (não vale antes):**
na primeira virada de :55 posterior ao deploy READY,
`84c9ddee-1404-48d0-b7a6-163c860fad0a` deve ficar
`trial_status='downgraded'` + `trial_downgraded_at` preenchido + 1 evento
`trial_downgraded`. `credits_revoked` = 0 é o esperado (saldo já é 0). Se
continuar `expired`, o log do cron agora traz `cohort_rows`, `cohort_count`,
`due`, `processed` e `skip_reasons` — a causa vem escrita.

**E-mails D5/D10:** a coorte de `trial-lifecycle-emails` está CERTA (já inclui
`expired`), mas o relógio dela para quem morreu por TETO com prazo futuro é
`trial_downgraded_at` — que só o cron de downgrade escreve. Com o cron parado,
`endedAt` ficava 0 e o D5/D10 **nunca sairia** para o lead mais quente. Sem
alteração de lógica lá (um segundo relógio criaria duas verdades sobre "quando o
trial acabou"); a dependência está registrada em comentário na própria rota.
