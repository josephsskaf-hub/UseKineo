# SPRINT 10/08/2026 — CEO operacional

> Sessão executada ~03:40–06:55 BRT (06:40–09:55Z). Fundador ausente por desenho.
> **Dia 09/08 não teve sprint registrada** — não existe `SPRINT-2026-08-09.md`.
> O commit `d122fa2` leva a data 09/08 mas foi entregue na sprint das 21h de 08/08.

---

## 🔴 PUSH PENDENTE — mande qualquer mensagem nesta conversa ou rode `scripts\52-PUSH.bat`

`origin/main` = **`6fcc83b`** — conferido AGORA por `git ls-remote origin refs/heads/main`.
`HEAD` local = **`541f5bf`**. Tudo à frente de `6fcc83b` = **5 commits**.

⚠️ A regra que vale é *tudo o que estiver à frente de `6fcc83b`* — a contagem
envelhece a cada sprint, o SHA não.

**Os dois bloqueadores de dinheiro de 08/08 continuam vivos em produção** e só
saem do ar com o push (compra avulsa resgatada com link de assinatura; clique
mensal resgatado em sessão anual — $4,90 prometidos contra $99,00). **E agora o
e-mail de hoje também depende do push:** sem ele, o `downgraded_loss` não
existe em produção e a pessoa das 18:22Z morre muda.

---

## 1. O ACHADO DA SPRINT — o trial morre e o produto não diz nada por 5 dias

Hoje são os **dois primeiros vencimentos reais da história** (17:57:20Z e
18:22:14Z). Fui conferir se a cadência de e-mails cobria o caso e **ela tinha um
buraco no meio**.

`dueKind()` tinha três saídas para uma linha morta: extensão (exige
`used < 10`), oferta D5 (a partir de **5 dias**) e D10. Quem foi rebaixado tendo
usado **10 créditos ou mais** caía num `return null` implícito:

> **CINCO DIAS DE SILÊNCIO — e o primeiro contato pós-morte era um cupom.**

O instante de maior aversão à perda do funil inteiro (o crédito acabou de ser
revogado) não tinha e-mail nenhum.

**Medido, não deduzido.** Dos **40 trials que vencem até 12/08**: 33 ganham
extensão, **7 caíam no buraco**, com **153 créditos revogados sem uma linha de
aviso**.

**E o caso de hoje é o pior possível:** `e934461f` usou **11 créditos** — um
acima do teto da extensão — e é a **MESMA conta que produziu o único clique em
COMPRAR da história da empresa** (sprint 21h de 08/08). Sem esta correção, o
lead mais quente que a empresa já teve morre mudo até 15/08.

### Entregue — commit `541f5bf`

Kind novo **`downgraded_loss`**, janela `[0, 48h)` após o fim, assunto
**"Here's what you just lost access to"**.

**Prova contra as linhas reais** (simulação no run das 22:25Z, o primeiro depois
da supressão de 24h do `ending_soon`):

| conta | usou | antes | depois |
|---|---|---|---|
| `5f0f607e` | 1 | extensão | **extensão — idêntico, zero regressão** |
| `e934461f` | 11 | **NADA por 5 dias** | **`downgraded_loss`, creditsLost=29** |

Acima de 48h o comportamento também é exatamente o de antes (silêncio até o D5):
**esta mudança só preenche vazio, não altera nenhuma linha que já tinha saída.**

### Três decisões de NÃO fazer

1. **SEM CUPOM.** `COMEBACK50` é do D5/D10 e de mais lugar nenhum (ordem do
   fundador, 06/08). Descontar no minuto da perda queima margem exatamente em
   quem tem a maior intenção e ensina a coorte a esperar pelo cupom.
2. **SEM PREÇO LITERAL.** Este cron não sabe a moeda da pessoa. Existe
   `signup_country`, mas ele **não é a entrada de `lib/checkoutPricing.ts`**, e
   derivar moeda aqui criaria uma **segunda verdade de preço fora da fonte
   única** — o bug de 08/08 ($4,90 × $99) por outra porta. Como os outros quatro
   e-mails do arquivo, o link vai para `/pricing`, que resolve a moeda.
3. **SUPRESSÃO CRUZADA DE 24h NÃO FOI TOCADA.** Ela atrasa o e-mail de hoje de
   ~19:25Z para ~22:25Z (o `ending_soon` saiu 09/08 22:25). 4h de atraso não
   justifica mexer numa guarda que protege TODOS os kinds. **Uma variável por vez.**

### Honestidade da copy (a parte que a revisão adversarial apertou)

A frase dos créditos **só é afirmada sobre linha `'downgraded'`** — a única em
que a revogação comprovadamente aconteceu (o cron carimba status e saldo na
MESMA escrita atômica). Em `'expired'` o saldo ainda está na conta e a pessoa
abriria o app para conferir: **o e-mail se cala sobre créditos em vez de
mentir.** E quem queimou os 40 não vê a linha — perda de zero crédito num rol de
perdas só enfraquece as perdas reais. O residual do free tier vem de
`getFreeTierOffer().copy.residual`, nunca de literal.

### Provas

- `tsc --noEmit` do projeto **INTEIRO**: `EXITCODE=0`.
- **O `tsc` foi FALSIFICADO**: erro proposital na linha 516 → `EXITCODE=2` com
  `TS2322` apontando este arquivo; restaurado byte-idêntico.
- Os kinds **não têm consumidor fora deste arquivo** (grep) — a renumeração de
  prioridade não alcança ninguém.
- **EOL LF conferido no blob gravado** (0 CR).
- Nenhuma migração, nada de dinheiro, marca d'água, preço, checkout ou flag.

### De passagem

`granted` era calculado dentro do ramo da extensão e agora dois ramos precisam
dele — *hoisted* para uma conta só. E o comentário de `KIND_PRIORITY` afirmava
"o teto de 200": **`MAX_PER_RUN` é 40 desde que existe** (lição 5 da sprint 21h
de 07/08 — comentário que afirma número é afirmação verificável).

---

## 2. Números (10/08, 06:40Z)

| métrica | valor |
|---|---|
| Signups 09/08 | **39** (melhor dia desde 31/07) |
| Signups 10/08 até 06:40Z | 8 |
| Contas com trial (história) | **90** |
| Trials ativos | **82** |
| Trials vencidos não processados | **0** ✅ |
| Vencem hoje / amanhã | **2 / 20** |
| Conversões trial→pago | **0** 🔴 |
| MRR novo hoje | **R$ 0** |
| A/B 3d × 7d | **45 / 45** — split saudável |
| Ativação (usou ≥1 crédito) | 7d: 31/45 · 3d: 27/45 |

**A onda real é 11/08: 20 pessoas.** Hoje são 2 — é o ensaio, não o show.

---

## 3. Aquisição concluída — o canal nº1 mudou de dono, e desta vez está medido

Usei as colunas de **primeiro toque** que já existiam em `profiles`
(`signup_utm_source` / `signup_referrer`) — **Regra Zero, não construí nada.**

**`chatgpt` passou o `taaft` como maior canal EXTERNO de entrada:**

| dia | chatgpt | taaft |
|---|---|---|
| 08/08 | 7 | 2 |
| 09/08 | **13** | 4 |
| 10/08 (parcial) | **4** | 0 |

Contra 65 cadastros do TAAFT num único dia em 31/07 — **o canal pago-de-graça
virou o canal principal enquanto o diretório secava.**

### ⚠️ Um número que eu ia reportar e derrubei antes

Minha primeira query dizia **"TAAFT ativa 4,0%"**. Falso: ela media
`trial_credits_used > 0`, e **a maioria dos 150 do TAAFT se cadastrou ANTES do
trial existir** — o zero era por construção, não por comportamento. Medindo por
**vídeo gerado** (válido nas duas eras) e contando PESSOAS:

| fonte | pessoas | ativaram | % | pagaram |
|---|---|---|---|---|
| taaft | 150 | 97 | **64,7%** | **2** |
| homepage¹ | 39 | 35 | 89,7% | 0 |
| chatgpt | 35 | 17 | 48,6% | 0 |
| (direct) | 25 | 8 | 32,0% | 0 |

¹ `homepage` é etiqueta interna (cadastro pelo formulário da home), não canal externo.

**A nuance que impede a conclusão errada:** o TAAFT é o **único canal que já
produziu pagamento** (2). O `chatgpt` traz volume e ativação decente e tem
**0 pagamentos em 35 pessoas**. "ChatGPT é o nº1 em entrada" **não** é "desligar
o TAAFT" — é "a entrada mudou de dono e a saída ainda não seguiu".

### Superfícies de máquina conferidas POR FETCH DO ARQUIVO SERVIDO

`/llms.txt` e `/api/facts` estão **corretos e atuais** — o trial de 40 créditos
aparece na oração principal, `videosPer24h: null` + `freeVideosPerWindow: 1` em
janela de 720h. **A correção de 08/08 segurou.** Nada a refazer (Regra Zero).

---

## 4. Amanhã — 3 entregas

1. **Ler o resultado real do `downgraded_loss`** nas 2 pessoas de hoje e nas 20
   de 11/08: abriu? clicou? É a primeira medição do e-mail de perda.
2. **Fechar a razão entre `d0_welcome` e cadastro.** 46 welcomes para 90 trials —
   **2 pessoas de 11/08 têm `NENHUM E-MAIL`** (`170c49ce`, `eeab29a4`, esta com
   20 créditos gastos). Suspeita: quem se cadastrou antes do 1º run de 08/08
   16:30Z ficou fora da janela para sempre.
3. **Atacar a saída do `chatgpt`**: 35 pessoas, 48,6% de ativação, **0
   pagamentos**. O canal que mais cresce nunca converteu uma vez.

---

## RESUMO PARA O CEO — sprint 10/08

1. 🔴 **PUSH PENDENTE — 5 commits à frente de `6fcc83b`.** Além dos dois bugs de
   dinheiro de 08/08, agora o e-mail de hoje também depende do push.
2. **Achei e fechei um buraco que ia estourar hoje:** quem é rebaixado tendo
   usado 10+ créditos não recebia **NADA por 5 dias**, e o primeiro contato
   pós-morte era um cupom. São **7 pessoas e 153 créditos revogados em silêncio**
   até 12/08.
3. **A vítima de hoje é a pior possível:** a única conta da história que clicou
   em COMPRAR morre às 18:22Z. Agora ela recebe "Here's what you just lost
   access to" — sem cupom e sem preço literal, de propósito.
4. **Entregue com prova:** `tsc` do projeto inteiro em 0 **e falsificado**;
   comportamento das linhas reais simulado antes de commitar (a de 1 crédito
   segue igual, zero regressão).
5. **Aquisição: o ChatGPT passou o TAAFT como maior canal externo** (13 × 4
   ontem). Medido nas colunas de primeiro toque que já existiam.
6. ⚠️ **Derrubei um número meu antes de reportar:** "TAAFT ativa 4%" era artefato
   de denominador (gente de antes do trial). O real é **64,7%**.
7. 🔴 **0 conversões em 90 trials.** O limiar ("<5% após 20") segue vencido há
   dias. **A onda real é amanhã: 20 vencimentos.**
8. ⚪ **Gate TAAFT $347: mantenho ADIAR.** O canal que cresce é de custo zero e o
   funil ainda não converteu ninguém.
