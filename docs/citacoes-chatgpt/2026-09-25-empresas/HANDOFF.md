# Citações: vídeos e anúncios para empresas

**AUTORIZAÇÃO DIRETA — 25/09/2026:** fundador pediu nesta tarefa “ajusta as citacoes pra falar que fazemos videos para empresas tambem, ads para empresas e etc”. Recorte pontual das fontes de descoberta; não renova a semana encerrada nem a automação pausada. Base `117c83fd7f2a435178a94193301a0a08615ebd58`, worktree própria `codex/citacoes-empresas-20260925`. Nenhuma outra frente acionada.

**PARCIAL / CONTRATO:** coorte = pessoas perguntando a assistentes sobre vídeos comerciais, produtos, serviços e anúncios. Obstáculo confirmado = resumo principal limitado a Shorts e afirmação geral de ausência de upload, embora existam Studio Ads e Empresas. Hipótese = apresentação explícita e destinos distintos permitem uma resposta correta e um próximo passo pertinente. Mudança reversível = texto/fatos existentes e descrição do GET na ação, sem nova página, oferta ou variante. Superfícies = `/llms.txt`, `/api/facts`, descrição compartilhada em `/facts` e schema público do GPT. Eventos existentes = `ads_page_viewed`, `ads_cta_clicked`, `business_ads_page_viewed`, `business_ads_cta_clicked`; pagamento depende da transação canônica. Sucesso técnico = distinção dos dois produtos, links públicos e limites preservados; amostra comercial ainda inexistente nesta entrega. Risco = confundir criação de anúncio com gestão de campanha ou acesso gratuito. Parada = gate falho, main/fila divergente ou nova contradição factual.

**FATO CONFIRMADO:** `lib/growth/studioAdsFacts.ts` e `lib/ads/offer.ts` descrevem anúncios com mídia própria; `lib/ads/access.ts` separa passe ativo e assinatura paga de trial. `lib/growth/dfyServiceFacts.ts` descreve o serviço operado, seus materiais e entrega. O resumo passa a incluir vídeos para empresas, anúncios, promoção de produtos/serviços e conteúdo social. O upload fica atribuído ao fluxo empresarial, não ao gerador a partir de roteiro. Valores e condições continuam derivados das fontes existentes.

**IMPLEMENTADO LOCALMENTE:** OpenAPI `1.3.4` descreve `studioAds` anulável e `businessVideoService` separadamente, sem acrescentar operação de compra/render. Descrições de operação permanecem abaixo do limite de 300 caracteres. Atualização do schema no Git não atualiza automaticamente instruções ou ações de um GPT salvo no editor; nenhuma publicação externa ou promessa de recomendação do ChatGPT.

**TESTADO LOCALMENTE:** typecheck bruto sem incremental passou; guardião Ads AEO 26 verificações, GPT Loja 33, fatos V31 40; 25 baterias adicionais (incluindo todos os demais scripts do job crítico e três jogadas) passaram. Log privado: `C:/Users/josep/.codex/outputs/citacoes-empresas-20260925-tests.json`. Renderização offline de `/llms.txt` e `/api/facts` com passe ligado/desligado confirma apresentação, destinos, limites e manutenção do serviço humano quando self-service indisponível. Nenhum acesso a banco, render, pagamento ou consulta ao benchmark.

**ESTADO:** LOCAL. Publicação, deploy e leitura por assistente ainda não comprovados. EXPOSTO/PAGO desconhecidos. Próxima ação: conciliar fila/main, enfileirar pelo guardião e confirmar publicação/deploy/superfícies. Sem alteração de backend, preços, canais externos ou agendas.
