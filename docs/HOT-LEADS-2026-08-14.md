# HOT LEADS — campanha de 14/08 [KINEO-HOTLEADS-2026-08-14]

Pedido literal do fundador: "usa os nossos hot leads, dá uma olhada na nossa
data e vamos mandar um email pra eles."

## O que existe agora (permanente)

`/api/admin/send-hotlead-blast` — rota no padrão da casa (dry-run default,
`?confirm=SEND&segment=X`), 3 segmentos que NENHUM job de lifecycle cobria:

| Segmento | Definição | Tamanho bruto (14/08) | Copy |
|---|---|---|---|
| `burned` | trial-era, créditos = 0, ≥2 vídeos completos, nunca pagou | 14 | "You used every single credit — quick question" → Creator $9.90 + pergunta aberta |
| `stalled` | trial-era, saldo 1–19, ≥1 vídeo, nunca pagou | 9 | "Your credits are still sitting there" → pergunta o que travou + Starter $4.90 |
| `power` | ≥10 vídeos completos no free (qualquer época) | 1 | "You make more videos than most paid users" → matemática do anual |

Proteções: supressão de lifecycle 24h (padrão da casa), flag idempotente por
evento `hotlead_emailed_v1` (ninguém recebe 2×, nunca), validação de e-mail
externo, List-Unsubscribe/CAN-SPAM via emailFooterHtml, pacing 600ms,
reply-to hello@ (cai na caixa do Joseph).

## Resultado do 1º disparo (14/08 ~21h BRT)

- burned: 4 enviados / 0 falhas
- stalled: 4 enviados / 0 falhas
- power: 0 elegíveis no momento
- **Total: 8 e-mails.** Os demais (~12) estavam dentro da janela de 24h da
  régua diária — ficam elegíveis conforme a janela rola. **Re-rodar o dry-run
  +SEND diariamente até esvaziar** (a flag garante que só pega os novos).

## Máquinas consertadas na mesma leva

1. `send-abandon-recovery` (cron diário 10:00 UTC) estava CEGO para os nomes
   novos de evento (`checkout_started`, `checkout_cta_clicked`,
   `inline_pricing_checkout_clicked`, `checkout_attempted`) — adicionados.
   Todo abandono recente agora entra no blast $4.90 automaticamente.
2. Exit-intent de cadastro (`variant="free"`) nas 2 páginas-ferramenta que
   convertem (67% e 41%).
3. IndexNow: backfill manual de 166 URLs submetido (HTTP 200) em 14/08.

## Próximos gatilhos recomendados (decisão do fundador)

- Promover o hotlead-blast a cron diário (1 linha no vercel.json) quando os
  segmentos provarem resposta — por ora, disparo manual controlado.
- Segmento futuro: "baixou watermark e nunca viu /pricing" (41 contas
  identificadas pela sprint de 21h de 13/08) — precisa de query por eventos
  de download cruzada com paths; fica para a leva 2.
