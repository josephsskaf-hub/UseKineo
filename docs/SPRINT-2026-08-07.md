# SPRINT 2026-08-07 — TROCA ATÔMICA DO FREE TIER + COPY [KINEO-TRIAL-SWAP-2026-08-07]

## RESUMO PARA O CEO
A promessa "3 free Shorts every 24h" saiu de TODAS as superfícies hardcoded e agora
mora num único lugar: `lib/freeTierOffer.ts` (`getFreeTierOffer()`), decidido pela
MESMA flag do reverse trial (`KINEO_REVERSE_TRIAL_ENABLED`). Com a flag OFF (estado
atual de produção) o site, os e-mails e o enforcement ficam byte a byte idênticos ao
que eram — provado por script: **187 strings legadas conferidas contra o HEAD, 100%
idênticas**, e `tsc --noEmit` EXIT=0. Com a flag ON, numa única virada:
- **Copy** (68 arquivos, ~190 pontos): headline aprovada — *"Start free — your first
  video is on us. New accounts get a full Creator trial: 40 credits, every engine
  except Studio."* — e residual *"1 free Fast video/month"*. Zero menção a
  desconto/50% em superfície pública (o meio-off do e-mail de cap foi inclusive
  REMOVIDO na versão ON).
- **Comportamento** (`/api/compose`): free pós-trial = **1 Fast por janela rolante de
  30 dias + máx. 15s + watermark** (watermark/end-card já existiam para o free e
  continuam). A mensagem 402 troca junto, da mesma fonte.

## O QUE FICOU PENDENTE (registrado, não inventado)
- **480p NÃO implementado**: o builder Creatomate (`lib/compose.ts` /
  `buildCreatomateSource`) não tem knob de resolução — layout fixo 1080×1920.
  Implementar exigiria pipeline novo (proibido nesta sprint). Free ON sai com
  1/mês + 15s + watermark; 480p fica para quando o builder ganhar o parâmetro.
- **"15s"** implementado como clamp `duration = min(duration, 15)` no caminho
  free-fast, ANTES da reserva de cota e do TTS (word count e captions escalam pelo
  mesmo número). 15 não está em SUPPORTED_DURATIONS, mas o pipeline já aceita
  duração arbitrária (caminho avatar usa 3–90s livremente).
- **"1/mês"** = janela ROLANTE de 30 dias (mesma mecânica de contagem por reservas
  do compose; sem calendário novo). Se o fundador quiser mês-calendário, é ajuste
  de `windowMs` → lógica própria.
- **Limite diário de thumbnails** (`ThumbnailGeneratorClient`) é outra cota, fora do
  escopo da decisão — intocado.

## DECISÕES DE ENGENHARIA (revisão adversarial 2×)
1. **SSG/cache**: as ~28 páginas de marketing continuam estáticas, SEM
   `dynamic`/`revalidate`. Na Vercel, mudar env var SÓ tem efeito com redeploy, e o
   redeploy reconstrói as páginas estáticas e invalida o cache do deployment — logo
   build e runtime leem o mesmo valor por deploy e a troca é atômica POR DEPLOY.
   **Procedimento de virada: setar a env E redeployar; a flag só conta como ligada
   quando o deploy novo está servindo.**
2. **Client components** (16 arquivos): a env da flag não existe no browser. O valor
   é resolvido no `app/layout.tsx` (server) e desce por contexto
   (`components/FreeTierOfferProvider.tsx` → `useFreeTierOffer()` /
   `<FreeTierCopy/>`). Fallback sem provider = versão OFF + warning em dev (falha
   para o lado que não promete o que não entrega). `getFreeTierOffer()` avisa no
   console se chamado no browser.
3. **Cauda longa**: ~190 frases usam `ft(OFFER, '<literal atual>', <copy ON>)` — o
   literal OFF fica no call site (auditável localmente, diff zero por construção); a
   copy ON canônica mora no helper (`headline/sentence/chip/planCardBody/...`);
   frases ON de contexto muito local (e-mails, comparações) são passadas explícitas
   ao `ft`, sempre gateadas pela mesma flag.
4. **E-mails**: send-welcome, send-reminders, send-activation-nudge, lead-capture e
   send-cap-hit trocam via helper. **send-credits-back é SKIPADO inteiro com a flag
   ON** (`reason: 'reverse_trial_free_tier'`) — a premissa "suas 3 grátis voltaram a
   cada 24h" não existe no free novo; a copy interna dele fica intocada porque só
   roda com OFF.
5. **Enforcement**: `reserveFreeFastPreviewSlot` agora lê `FREE_OFFER.limit` /
   `FREE_OFFER.windowMs` (OFF: 3/24h — idêntico às constantes antigas, que seguem
   exportadas em `lib/freeFastQuota.ts` para o TrialDowngradeModal e o cron OFF).
   Contas em trial ativo NÃO passam pelo caminho free (geram via crédito), então o
   clamp de 15s só atinge o free real pós-trial.
6. **lib/comparisons.ts / lib/kineoFacts.ts**: são importados só por código
   server-side (conferido: alternatives/vs/facts/llms/sitemap; as menções em
   affiliate/partners são comentários) — `getFreeTierOffer()` no module scope é
   seguro. `FREE_TIER` ganhou `allowance` (frase pronta) e os consumidores
   (facts/llms) usam a frase, não os números.
7. **EOL**: repo tem mix LF/CRLF (worktree Windows CRLF-ificado vs index LF). As
   edições preservaram o byte-EOL do worktree; no commit, cada arquivo foi
   normalizado para o EOL DO INDEX (i/lf → LF; os 2 i/mixed — AuthModal,
   DashboardClient — staged byte a byte), então o diff commitado contém só as
   mudanças de conteúdo.

## RISCOS CONHECIDOS
- Copy ON em superfícies de SEO muda titles/descriptions de páginas rankeadas
  (ex.: cheapest-ai-shorts-maker title) — esperado e aprovado pela ordem, mas vale
  monitorar CTR pós-virada.
- Qualquer client component NOVO que renderize a promessa precisa usar
  `useFreeTierOffer()`/`<FreeTierCopy/>`; ler `process.env` direto = copy velha
  silenciosa (guardas: warning no helper + fallback OFF no hook).
- O contador do chip no /generate ("X of N free …") depende de `freeUsedToday`
  derivado de recentVideos no cliente; com a flag ON a janela do contador local
  (24h) fica mais curta que a do servidor (30d) — o chip pode mostrar "0 of 1"
  com a cota do mês já gasta; o servidor continua sendo a verdade (402 correto).
  Ajuste fino do contador fica para o QA da flag.

## PROVAS
- `tsc --noEmit` → EXIT=0 (2 execuções, antes e depois da revisão).
- Verificação automatizada: 187 strings `legacy` extraídas dos call sites e
  conferidas contra `git show HEAD:` (normalizando whitespace de JSX) — todas
  presentes no HEAD → flag OFF = copy 100% atual.
- Sweep de órfãs: re-grep de "3 free|every 24|3 watermarked|three watermarked|
  free Fast previews|per 24" → restaram apenas: fatos de CONCORRENTES
  (comparisons), comentários de código, feature names ("Free Shorts Idea of the
  Day", "Your next 3 Shorts") e as rotas OFF-only documentadas acima.

---

# ANTI-ABUSO (FINGERPRINT) + PRIMEIRO MINUTO PAGO [KINEO-TRIAL-ABUSE-PMP-2026-08-07]

## RESUMO PARA O CEO
Duas entregas, ambas sem tocar no checkout e sem ligar nenhuma flag.

**1) Anti-abuso do reverse trial (fase 2).** 40 créditos grátis por conta era o alvo
óbvio; as guardas antigas (perfil <24h, 1 trial por conta, e-mail descartável) não
param quem abre 30 Gmails no mesmo notebook. Agora existe uma **guarda 7** de
device/IP: hash SHA-256 salgado de `(IP · user-agent · accept-language)` gravado em
`trial_signup_fingerprints`. **2 ativações por fingerprint em 30 dias**; a partir da
terceira a conta é criada **normalmente, sem trial e sem nenhum aviso**. O IP CRU NÃO
É GRAVADO em lugar nenhum — nem em log (só o prefixo de 12 chars do hash).

**2) Primeiro minuto pago.** Auditoria dos 60s pós-pagamento — o achado abaixo.

## O PRIMEIRO MINUTO PAGO — AUDITORIA (com evidência)
| Pergunta | Resposta encontrada |
|---|---|
| Créditos creditados na hora? | **Sim.** `app/api/stripe/webhook/route.ts` credita em `checkout.session.completed` (Path A packs ~l.603-640, Path B assinatura ~l.841-880) com guard de idempotência e `has_paid: true`. |
| Plano reflete na UI sem refresh? | **Não, e essa era a falha.** ver abaixo. |
| Para onde o usuário volta? | Depende do SKU — e é aí que estava o limbo. |
| Existe tela de "pagamento confirmado"? | Só para 4 dos 7 SKUs. |
| E-mail de boas-vindas do plano pago? | **NÃO EXISTE.** Zero envio de e-mail no webhook (grep `resend|emails.send|sendMail` em `app/api/stripe/webhook/route.ts` → **0 ocorrências**). |

**O BURACO (evidência literal).** Três SKUs redirecionam DIRETO para dentro do app:
- `app/api/stripe/checkout/route.ts:1697` topup → `/generate?success=true&topup=…`
- `app/api/stripe/checkout/route.ts:2023` bulk → `/generate?success=true&pack=…`
- `app/api/stripe/checkout/route.ts:1869` piloto → `/autopilot?success=true&pack=…`

E `grep -rn "searchParams.*'success'" "app/(dashboard)"` retorna **ZERO linhas**:
ninguém no app lê esse parâmetro. Cartão cobrado, Stripe redireciona, **o app não diz
nada**. O saldo do topo só muda se o webhook por acaso já tiver rodado quando o
`useEffect` de `/api/credits` montou; se não, o comprador vê o saldo ANTIGO e a única
saída é ele mesmo apertar F5 — no minuto mais caro da relação.

E `app/checkout/success/page.tsx` (os outros 4 SKUs + PayPal) **confessava o limbo por
escrito**: dizia "Your plan is being activated." e logo abaixo "If your credits do not
appear immediately, **refresh in a few seconds**" — a página nunca perguntava ao
servidor se o crédito havia chegado, e delegava o trabalho ao cliente.

## O QUE FOI CONSERTADO (barato e óbvio, sem refatorar checkout)
- `components/PaymentConfirmedToast.tsx` (novo), montado em `app/(dashboard)/layout.tsx`:
  lê `success=true`, confirma o pagamento na tela e repola `/api/credits` por ~20s
  (0/2/5/10/20s), disparando `creditsChanged` **só quando o número muda** — é isso que
  atualiza o saldo do resto da UI **sem refresh manual**. Montado no layout, e não no
  `GenerateClient`, porque os destinos são telas diferentes (`/generate`, `/autopilot`)
  e amanhã haverá uma terceira. Sem `success` na URL ele retorna no primeiro efeito:
  zero fetch, zero render.
- `app/checkout/success/page.tsx`: mesmo poll; a copy passa a afirmar o que foi LIDO
  ("N credits available"). O "refresh in a few seconds" só reaparece se, ao fim dos
  20s, o saldo realmente não tiver sido lido — deixou de ser a mensagem padrão.
- **Nenhuma copy afirma um DELTA** ("+120 créditos"). O baseline pré-compra não existe
  no cliente; a única frase honesta em qualquer ordem de chegada do webhook é o saldo
  atual, que sobe sozinho se o webhook atrasar.

## BURACO GRANDE — DOCUMENTADO, NÃO IMPROVISADO
**Não existe e-mail de boas-vindas do plano pago.** O webhook não manda e-mail nenhum;
o único "recibo" é o do próprio Stripe. Implementar exige template + supressão
(`lib/emailSuppression`) + provedor + decisão de copy por SKU (7 SKUs, planos e packs
com promessas diferentes) — fora do escopo "barato e óbvio" desta ordem. **Fica como
próximo item do primeiro minuto pago.**

## ARQUIVOS
Novos: `lib/trialFingerprint.ts`, `app/admin/trial-abuse/page.tsx`,
`components/PaymentConfirmedToast.tsx`.
Alterados: `lib/reverseTrial.ts`, `app/api/track-signup-source/route.ts`,
`app/(dashboard)/layout.tsx`, `app/(dashboard)/admin/ceo/CeoClient.tsx`,
`app/checkout/success/page.tsx`, `docs/SQL-REVERSE-TRIAL.sql`.
Migração `trial_signup_fingerprints` **APLICADA** em cqqukkvjjrguayiyjvhh e verificada
por `information_schema.columns` pós-apply (5 colunas, 2 índices, RLS ON sem policy =
só service-role).

## DECISÕES DE ENGENHARIA (revisão adversarial 2×)
1. **O hash é calculado na BORDA** (`app/api/track-signup-source`), e só o hash desce
   para `lib/reverseTrial.ts`. O módulo que fala com o banco **nunca tem o IP em
   escopo** — não dá para logar por engano o que não se recebeu.
2. **Sem `KINEO_TRIAL_FINGERPRINT_SALT` no ambiente, nada é gravado.** Um deploy sem o
   salt não produz hash sem sal (que seria uma rainbow table de IPs); ele desliga o
   sinal. Trocar o salt invalida a base inteira de propósito.
3. **Falha ABERTO em todo ramo**: sem salt, sem IP, tabela ausente, erro de query →
   **concede o trial** e grava `trial_fingerprint_check_failed`. Ordem do fundador:
   "é melhor errar concedendo do que barrar cliente real".
4. **N=2 e não N=1.** O fingerprint colide de verdade para um casal/família atrás do
   mesmo NAT com o mesmo Chrome e o mesmo idioma. Com N=2 a segunda pessoa da casa
   ainda ganha o trial; só a terceira em 30 dias não ganha — e ainda assim cria conta
   normalmente, sem mensagem.
5. **O slot só é queimado DEPOIS do trial existir.** Gravar 'activated' na expectativa
   faria toda ativação abortada (CAS perdido, instância congelada) consumir um slot — e
   slot consumido é falso positivo para a PRÓXIMA pessoa daquele IP. O perdedor do CAS
   retorna `lost_race` sem gravar; só o vencedor grava.
6. **Bloqueios são gravados (`outcome='blocked'`) mas NÃO contam** para o limite — eles
   só alimentam o painel.
7. **Ordem das guardas**: a guarda 7 é a última, porque custa uma query e não faz
   sentido pagá-la por quem já foi recusado por motivo mais barato.
8. **Lista de descartáveis (8 → 47 tokens)** em módulo único (`lib/reverseTrial.ts`).
   Todo token sem ponto foi testado por script contra 100 domínios de provedores reais:
   a 2ª passada pegou `'dropmail'` casando `raindropmail.com`/`dropmailbox.com` e ele
   virou `'dropmail.me'` (ancorado). Resultado final: **0 falso-positivo em 100 domínios
   reais, 0 escapes em 47 domínios descartáveis conhecidos**. Serviços de **ALIAS/RELAY**
   (`privaterelay.appleid.com`, anonaddy, simplelogin, duck.com, firefox relay) foram
   **deliberadamente deixados de fora**: são usados por clientes reais e pagantes.
9. **`/admin/trial-abuse` é 100% leitura** — nenhum caminho de escrita, de propósito.
   Mostra bloqueios **ao lado** dos permitidos: um painel que só mostra bloqueios
   convida a apertar o limite. E distingue "tabela ausente" de "tabela vazia" com uma
   pergunta direta ao schema — confundir os dois seria mentir sobre o anti-abuso estar
   ligado.
10. **Flag**: nenhum comportamento novo com `KINEO_REVERSE_TRIAL_ENABLED` OFF.
    `maybeActivateReverseTrial` continua retornando `flag_off` antes de qualquer
    leitura, então a guarda 7 nunca executa. O único código que roda com a flag OFF é
    `trialFingerprintFromHeaders()` (argumento da chamada): um hash em memória que é
    descartado — **zero escrita, zero log, resposta HTTP byte-idêntica**. Já o
    `PaymentConfirmedToast` **não** está atrás da flag do trial, e não deve estar: é
    correção de pagamento, ortogonal ao reverse trial.

## RISCOS CONHECIDOS
- **IPv6 móvel** troca de endereço por sessão: o sinal é fraco nesse tráfego (falso
  NEGATIVO, ou seja, abuso passa). É o lado certo de errar; o e-mail descartável e o
  "1 trial por conta" continuam valendo.
- **NAT corporativo/universitário**: 3+ pessoas com o mesmo Chrome e o mesmo idioma no
  mesmo IP em 30 dias → a partir da terceira sai sem trial. Visível no painel
  (fingerprints repetidos) para o fundador decidir se afrouxa o N.
- **PII pré-existente, fora do escopo desta ordem**: `app/api/credits/route.ts` grava
  `profiles.last_ip` com o **IP CRU** a cada chamada. Não foi tocado (não é reverse
  trial), mas é o único IP cru persistido no produto e vale uma decisão à parte.
- **`trial_signup_fingerprints` cresce sem TTL.** Uma linha por signup; nesse volume
  leva anos para incomodar, mas a limpeza >30d é candidata natural a um cron.

## PROVAS
- `tsc --noEmit` → **EXIT=0**, duas execuções (antes e depois da 2ª passada).
- Migração aplicada e verificada por `information_schema` (5 colunas na ordem esperada).
- Lista de descartáveis validada por script: 100 domínios reais / 47 descartáveis.
- `grep -rn "searchParams.*'success'" "app/(dashboard)"` → 0 linhas **antes** da
  correção (a prova do limbo).
- `grep -rn "resend|emails.send|sendMail" app/api/stripe/webhook/route.ts` → 0 linhas
  (a prova de que não há e-mail de boas-vindas pago).
- EOL: `lib/reverseTrial.ts`, `app/api/track-signup-source/route.ts`, `CeoClient.tsx`,
  `docs/*` são **i/lf**; `app/(dashboard)/layout.tsx` e `app/checkout/success/page.tsx`
  são **i/crlf** — cada edição preservou o EOL do HEAD daquele arquivo (verificado
  byte a byte após cada escrita).

---

# SPRINT 21h (06/08 local) — KINEO-TRIAL-FEATURE-GATES-2026-08-07

## O QUE FOI ENTREGUE

**1. Fase 2 — os dois últimos gates de feature que tratavam trial como free.**
`6dfad6a` fechou os gates de MOTOR (compose, cinematic, status, youtube/upload,
video-summary). Sobraram os gates de FEATURE, que o QA listou como "não alterados
de propósito, registrados para decisão". Dois deles são direito de Creator e
portanto do trial:

- `app/api/footage/route.ts` — upload de footage próprio. O gate virou
  `getEffectiveEntitlement(profile, { isPaidAccount: isPaid }).treatAsPaid`, com o
  predicado LOCAL passado explicitamente (invariante 1 de `lib/reverseTrial.ts`).
- `app/api/characters/route.ts` — `limit` virou
  `isTrialActive(profile) ? Math.max(planLimit, 3) : planLimit`. O 3 não é número
  novo: é o que `characterLimitFor` já devolve para starter/basic. `Math.max`
  para nunca REBAIXAR um Studio.

Com a flag OFF, `isTrialActive()` retorna false na PRIMEIRA instrução (antes de
ler o perfil), logo `treatAsPaid === isPaid` e `limit === planLimit` byte a byte —
**diff de runtime zero por construção**, a mesma invariante do resto da fase 2.

**2. Decisões de NÃO fazer (registradas para a próxima varredura não "consertar"):**
- `api/autopilot/schedules` — Autopilot é plano próprio (US$ 299), não é Creator.
  Trial NÃO recebe. Correto por spec.
- `api/generate-video` / gate `isPro` do Cinematic — invariante 2: motores Studio
  nunca no trial. Ficar de fora é o comportamento certo, não um esquecimento.
- `stripe/checkout` top-up (`isCreatorPlus`) — trial não compra pacote de crédito;
  a conversão desejada é assinatura, e o trial já tem 40 créditos.

## O QUE A REVISÃO ADVERSARIAL DERRUBOU (e é o valor real desta sprint)

A primeira passada achou **4 defeitos na minha própria correção**, três deles
importantes. Todos corrigidos antes do commit:

1. **O `error` do `.single()` estava sendo descartado nas duas rotas.** O SELECT
   ganhou 3 colunas que vêm de uma MIGRAÇÃO que a flag não controla. Num ambiente
   sem elas o PostgREST devolve 42703, `data` vem null, e as duas rotas dariam
   **402 "feature paga" para TODO usuário, pagante inclusive** — falha silenciosa
   do lado caro. Agora as duas espelham o 503 de `app/api/compose/route.ts`, com
   `PGRST116` (0 linhas) EXCLUÍDO do 503 de propósito: perfil inexistente sempre
   caiu no 402, e mudar isso seria mudança de comportamento com a flag OFF.
2. **Eu escrevi uma afirmação falsa e verificável.** O comentário novo dizia
   "FREE = 0, Starter/Creator = 3, Studio (`pro`) = 10". `characterLimitFor` NÃO
   tem ramo para `creator`, `studio` nem `autopilot` — as três caem em
   `hasPaid ? 3 : 0`. Ou seja um `plan='studio'` recebe **3 personagens, não 10**.
   Eu troquei um número errado (o antigo "1 e 12") por outro errado, e ainda
   carimbei como "os números reais". Corrigido para os valores por STRING de
   plano, com o bug pré-existente nomeado.
3. **`getEffectiveEntitlement(profile, { isPaidAccount: planLimit > 0 })` em
   characters era inerte e virava armadilha**: só `ent.isTrial` era consumido, e
   `ent.treatAsPaid` estaria ERRADO nessa rota para um assinante (porque
   `characterLimitFor` devolve 0 para creator/studio). Trocado por `isTrialActive`
   direto, com o motivo escrito no import.
4. **CRLF de arquivo inteiro.** Meus edits reescreveram os dois arquivos em CRLF
   contra um HEAD que é LF: o diff saiu com **674 linhas para uma mudança de 52**.
   Normalizado com `sed -i 's/\r$//'`; diff final = 98 inserções, 8 remoções.

## AQUISIÇÃO DO DIA (rotação: Funil/Analytics) — O FUNIL REAL, E O NÚMERO QUE MENTE

Coorte: **180 pessoas que geraram ≥1 vídeo nos últimos 30 dias**, contas internas
excluídas, contando PESSOAS distintas.

| Etapa | Pessoas | % da anterior |
|---|---|---|
| Geraram ≥1 vídeo | 180 | — |
| **Olharam preço (`pricing_view`)** | **28** | **15,6%** |
| Iniciaram checkout (`checkout_started`) | 18 | 64% |
| Pagaram (`payment_success`) | 4 | 22% |

**O ponto não óbvio, e ele contradiz a tese que vinha sendo usada.** A primeira
query que rodei dizia "149 das 180 viram preço" e apontava o gargalo em
preço→checkout. Está errado: os 149 vinham de `inline_pricing_currency_resolved`
— **851 eventos para 148 pessoas (5,75× por pessoa)**, telemetria de MONTAGEM de
componente, não intenção de olhar preço. É exatamente a armadilha já registrada no
PROMPT-DIARIO ("componente medido só por exibição é instrumento cego" e "conte
PESSOAS, não eventos"). Se eu tivesse parado na primeira query, o relatório
mandaria a próxima sprint mexer em copy de paywall.

O funil honesto diz o contrário: **`pricing_view` → checkout converte 64% e
checkout → pago converte 22%. Os dois são saudáveis.** O buraco inteiro está em
**180 → 28: 84% de quem termina um vídeo nunca chega a olhar o preço.**

Três consequências diretas:
- **Valida a prioridade #1.** O reverse trial ataca exatamente isso — entrega
  valor antes de pedir dinheiro, em vez de melhorar a página do pedido.
- **Reforça o gate do TAAFT US$ 347.** Encher o topo de um funil que perde 84%
  antes da pergunta multiplica o vazamento, não a receita.
- **Mata uma alavanca:** mais superfície de paywall não move nada nesta coorte.
  A régua da conversão é o que acontece entre "vídeo pronto" e "olhar preço".

## PROVAS
- `tsc --noEmit --incremental` → **EXITCODE impresso no log da sprint** (ver bloco final).
- EOL conferido no HEAD (`git show HEAD:<arquivo> | grep -c $'\r$'` = 0 nos dois)
  ANTES e DEPOIS da escrita; CRLF introduzido pelo editor foi revertido.
- Funil: query única sobre `profiles`/`videos`/`events`, 30 dias, internas
  excluídas, `count(distinct user_id)` em todas as etapas.
- Regra Zero aplicada e ela evitou trabalho inútil: o cron `send-credits-back`
  parecia o maior vazamento de copy da troca atômica ("suas 3 grátis voltaram,
  todo dia, para sempre" com o free tier virando 1/mês). Ele **já estava morto**
  com a flag ON desde `15e4154` (`reason: 'reverse_trial_free_tier'`). Nada a fazer.
- Varredura de desconto: **nenhuma ocorrência de COMEBACK50 ou "50% off" em
  página, card ou modal público.** As ocorrências existentes são (a) a rota admin
  `send-comeback50`, (b) e-mails de lifecycle, (c) "first month half off", que é o
  preço-âncora vigente (US$ 4,90 no 1º mês), não o desconto de 3 meses. Item do
  GO-LIVE conferido: **limpo**.

## CORREÇÃO A UM GATE QUE ESTAVA DESATUALIZADO
`docs/GATES-ABERTOS.md` e o cabeçalho do `56-PUSH.bat` diziam que o vazamento de
`/api/render/[id]` continuava **em produção** e que havia 10 commits represados.
**Os dois estavam vencidos.** Conferido nesta sprint pelo método que o
PROMPT-DIARIO exige (CONTEÚDO, nunca topologia do grafo):
- `git ls-remote origin main` → **ec9f112**
- `git show ec9f112:app/api/render/[id]/route.ts | grep -c render_jobs` → **1**, com
  o marcador `KINEO-RENDER-OWNERSHIP-2026-08-06` presente
- `list_deployments` da Vercel → deploy **READY**, `target: production`,
  `githubCommitSha: ec9f112`

**O vazamento de URL de MP4 está FECHADO em produção.** Represados hoje são 4
commits (a4d73dd, 6dfad6a, 0deed3b, db455f6), nenhum deles de segurança, e nenhum
muda nada para usuário hoje (flag OFF). O `57-PUSH.bat` substitui o 56 com o
cabeçalho corrigido.

---

# RESUMO PARA O CEO — sprint 21h (06/08)

1. **PUSH PENDENTE — mande qualquer mensagem nesta conversa ou rode `scripts\57-PUSH.bat`** (4 commits; substitui o 56).
2. **Entregue:** os 2 últimos gates de FEATURE do trial (footage + characters). Fase 2 do reverse trial fica sem dívida de código; falta só o QA em produção, que depende de env var (gate seu).
3. **A revisão adversarial derrubou 4 defeitos MEUS** antes do commit — incluindo o erro do `.single()` engolido, que daria 402 "feature paga" para TODO usuário se as colunas de trial faltassem, e um número falso que eu mesmo escrevi ("Studio = 10"; é 3).
4. **Números (30d, pessoas, internas fora):** 945 perfis · 239 cadastros/7d · 5 cadastros/24h · 344 ativados · 6 `has_paid` / **3 planos pagos ativos** · **0 trials** (flag OFF) · 22 vídeos por 14 pessoas nas últimas 24h. **Custo de trial hoje = $0.**
5. **Aquisição (Funil) — o achado do dia:** de 180 pessoas que geraram vídeo em 30 dias, **só 28 olharam preço**, 18 foram ao checkout, 4 pagaram. `pricing_view`→checkout converte 64% e checkout→pago 22% — **os dois são saudáveis**. O gargalo inteiro é 180→28: **84% termina um vídeo e nunca chega a olhar o preço.** Mais paywall não move nada.
6. **Consequência para o seu caixa:** isso REFORÇA o gate do TAAFT US$347. Encher o topo de um funil que perde 84% antes da pergunta multiplica o vazamento, não a receita. E valida o reverse trial como a prioridade certa.
7. **Corrigido no doc:** o vazamento de `/api/render/[id]` **já está fechado em produção** (conferido por conteúdo + deploy READY). O gate anterior estava vencido.
8. **Gate novo, decisão sua em 1 linha:** `characterLimitFor` não conhece `creator`/`studio`/`autopilot` — um `plan='studio'` recebe **3 personagens em vez de 10**. Exposição hoje é baixa (só 3 pagantes, todos starter/basic), mas arma no dia do primeiro Studio. Qual é a cota de cada tier?
9. **Não fiz, de propósito:** Autopilot no trial (é plano de US$299, não é Creator), motores Studio (invariante 2), e não toquei em marca d'água/compose/preços/pagamentos — seus gates.
10. **Lixo a apagar:** sobrou um `tsconfig.sprint21h.json` vazio na raiz (o sandbox não consegue deletar). Pode apagar, é descartável.

---

# SPRINT 10h — 07/08/2026 · `KINEO-GATE-STALE-SNAPSHOT` + `KINEO-PROOF-ABOVE-FOLD`

## 🔴 PUSH PENDENTE — mande qualquer mensagem nesta conversa ou rode `scripts\58-PUSH.bat`

**1 commit só: `ee5106d`.** O 58-PUSH substitui o 57 (já executado — `origin/main` estava em `3faabec` quando esta sprint começou). Enquanto não subir, o único pagante ativo continua sem conseguir gastar o que já pagou.

## O ESTADO REAL ERA OUTRO (os docs estavam vencidos)

Antes de qualquer coisa, o protocolo mandou ler. E o que estava escrito não era mais verdade:

- **Push não era gate:** `HEAD == origin/main == 3faabec`, deploy READY em produção com esse SHA. Os "4 commits represados" do `GATES-ABERTOS.md` já subiram.
- **A flag `KINEO_REVERSE_TRIAL_ENABLED` está LIGADA em produção.** O GO-LIVE dizia que o passo 4 (redeploy) não tinha sido feito. Foi. O reverse trial está no ar.
- **O QA passo 5 rodou sozinho, com gente de verdade** — e o resultado está abaixo.

## O REVERSE TRIAL EM PRODUÇÃO — primeiros números reais

| Conta | Criada | Status | Concedidos | Usados | Saldo | Vídeos |
|---|---|---|---|---|---|---|
| `rjsh***` | 07/08 04:43Z | active 7d | 40 | 1 | 39 | 1 (fast) |
| `fork***` | 07/08 08:24Z | active 7d | 40 | 1 | 39 | 1 (fast) |

**Funciona.** Ativação automática, 40 créditos uma vez só, ambos geraram vídeo (100% de ativação). Custo de trial hoje: 2 créditos.

**Dois achados que importam mais que o "funciona":**

1. **🟠 97,5% da concessão não é usada.** Os dois fizeram exatamente 1 vídeo **Fast** (1 crédito) e pararam. Nenhum tocou o Seedance. O reverse trial existe para a pessoa PROVAR o motor caro — se ela sai com 39 créditos parados, ela nunca viu o que está comprando. Isso é a mesma doença do funil 180→28. **Não corrigi hoje** (mexer no default de motor é decisão de produto), mas é a fila número 1: o primeiro vídeo do trial deveria ser o motor caro, não o barato.
2. **🟠 O A/B está 3 de 3 em `7d`.** Com n=3 não é prova (12,5% de chance se fosse justo), mas se o hash estiver enviesado o A/B nunca roda. Vale conferir antes de acumular os ~150 signups/braço.

## 🚨 O ACHADO DA SPRINT — o único pagante ativo estava preso num botão morto

`valos87196` (75 créditos comprados, o único pagante ativo):

- Voltou hoje **08:20Z**
- Leu a **página de preço às 11:43:01Z** — intenção de compra explícita
- Entre 08:20 e **11:44Z**: **31 cliques bloqueados**, `analyze_blocked_active_render_gate`, `resolved=false resumed=false retries=0`
- **Zero** `analyze_idea_clicked`. **Zero** `generate_started`. **Nunca passou do primeiro clique**, em 3h23min.

**Causa raiz.** O gate `waitForActiveRenderRestore()` só fazia fail-open quando **não havia** snapshot no `localStorage`. O snapshot dele era de **30/07 12:39Z — oito dias**, contra um TTL de **2 horas**. A checagem de idade existe, mas mora **dentro do effect de restore**, que é exatamente o código pendurado (`retries=0` prova que `auth.getUser()` não deu erro e nunca resolveu). **O produto ficava fechado com base numa evidência que o próprio sistema já considera vencida.**

E o mais caro: o comentário na linha 3352 do `GenerateClient.tsx` diz, literalmente, *"valos87196 burned 28 clicks on this class of dead button across 29–30/07"*. **A correção de 31/07 foi escrita para ele e não o salvou** — porque olhou a presença do snapshot, não a idade.

### O que foi corrigido

1. **Fail-open por IDADE, não por presença** (`classifyStoredActiveRender()` → `absent` | `stale` | `live`), espelhando as regras de descarte do próprio effect.
2. **`handleGenerate` agora espera o gate** igual ao `handleAnalyze`. A assimetria (um `await` de 4s com fail-open, o outro uma leitura síncrona sem saída) não era intencional e deixava o CTA morto **para sempre** — nada no código o destravava por decurso de tempo.
3. **Saída visível para o usuário**, em `idle` **e** em `failed`. Antes, o único botão que resolvia (`handleReset`) só era renderizado dentro do bloco `showRender`, ou seja **nunca na fase em que o bloqueio acontece**. A única saída existente era um `?wm_unlock=1` não documentado.

### A revisão adversarial rodou 2× e derrubou 5 defeitos MEUS

Isto é o item mais importante desta sprint, e nenhum deles seria pego pelo `tsc`:

| # | Defeito que EU introduzi | Por que era grave |
|---|---|---|
| 1 | O guard anti-duplo-submit (#360) ficou **atrás** de um `await` de 4s | É o handler que **DEBITA CRÉDITO**. Antes o gate era síncrono (janela ~0ms); eu abri uma janela de 4s para dois cliques despacharem dois renders = dois débitos. Hoistado para antes do await |
| 2 | A claim do ref não sobrevive ao `await` | O effect de fase zera `generationInFlightRef` em qualquer transição para fase não-processing, e o restore effect pode fazer isso no meio da espera. Reafirmada logo após o gate abrir |
| 3 | O botão de escape aparecia **por cima de um render pago vivo** | O restore effect resolve segundos depois e põe `phase='composing'` sem limpar `error` — o botão ficaria ao lado da barra de progresso convidando a jogar o render fora. Agora é gated por `!isProcessingPhase(phase)` |
| 4 | O escape sumia justamente em `phase='failed'` | É onde mora o botão **Retry**, ou seja onde o usuário mais insiste. O bug original, de volta, na pior fase. Agora renderiza nos dois lugares |
| 5 | A 1ª versão **apagava** o snapshot stale, e a copy prometia "créditos intactos" | `age` vem do **relógio do cliente** (salto de NTP, VM retomada) e não temos user id verificado ali — apagar é irreversível. Agora só abre o gate. E o discard manual **consulta o servidor antes** e recusa se houver render na janela, resumable ou não (um cinematic no fal **não** é resumable, e o veto de 15min não o pega) |

`npx tsc --noEmit` → **EXITCODE=0**, zero erros. EOL conferido no HEAD: **0 CRLF** nos dois arquivos, diff proporcional (262 inserções / 26 remoções, 2 arquivos).

## AQUISIÇÃO CONCLUÍDA — a prova social existia e ninguém via

A empresa ganhou **#2 Produto do Dia no Fazier** (04/08) e leva **~1.000 cliques/semana do TAAFT**. As duas provas estavam renderizadas na home — **depois do CTA final**, sob um `borderTop`, na linha ~840. **Quem converte nunca chegava lá.**

Subiram para a dobra, logo abaixo do `LiveStatsBadge`. Decisões:

- **Fazier vai como `<span>`, não link.** "Product of the Day" é ranking **diário**; um link para `fazier.com` em 07/08 mostra outro produto e **desmentiria a própria prova no clique**. O backlink dofollow que o tier grátis exige continua sendo o do rodapé, que voltou a ser "Launched on Fazier" (neutro, não precisa de comprovação, não duplica a frase).
- **Nenhum número de tração na faixa.** O `LiveStatsBadge` acima já é a fonte medida, e "947 perfis" inclui contas internas — publicar isso seria a mentira que a limpeza de 28/06 já removeu do site.
- Sem o embed com `<script>` do Fazier: a restrição do `KINEO-FAZIER-2026-07-31` vale ainda mais na dobra.

## O QUE NÃO FIZ, DE PROPÓSITO

- **Não mexi em preço, marca d'água, compose, pagamentos, migração** — seus gates.
- **Não mexi no default de motor do trial** (achado #1 acima) — é decisão de produto, não de engenharia.
- **Não liguei nada novo**: a flag já estava ligada; esta sprint não muda estado de flag.

## RESUMO PARA O CEO

1. **PUSH PENDENTE:** 1 commit (`ee5106d`) — `scripts\58-PUSH.bat`.
2. **O reverse trial está NO AR e funcionando:** 2 trials reais hoje, 40 créditos cada, 100% geraram vídeo. Custo de trial hoje: 2 créditos. Zero conversão ainda (n=2, cedo demais).
3. **Achei o único pagante ativo preso num botão morto por 3h23** — 31 cliques, zero geração, depois de ler a página de preço. Snapshot de 8 dias travando um gate com TTL de 2h. Corrigido.
4. **A correção de 31/07 para o MESMO cliente não o salvou** porque olhou presença de snapshot em vez de idade. Está no comentário do código, com o nome dele.
5. **A revisão adversarial 2× derrubou 5 defeitos meus**, um deles abriria janela de duplo débito de crédito. Nenhum seria pego pelo `tsc` (que ficou verde nas duas versões).
6. **Aquisição:** prova social (#2 Fazier + TAAFT) subiu para a dobra — estava depois do CTA final, invisível para quem converte.
7. **Números:** 947 perfis · 205 cadastros/7d · 5/24h · 346 ativados · 6 `has_paid` · 3 planos pagos ativos · 2 trials ativos · 0 conversões de trial · 22 vídeos/24h.
8. **Fila nº1 para amanhã:** os dois trials gastaram **1 de 40 créditos** e nenhum tocou o motor caro. Trial que não prova o produto caro não vende assinatura.
9. **Sem alertas de dinheiro:** nenhum erro de checkout, custo de trial irrisório, fal acima de $20.
10. **Lixo a apagar:** `tsconfig.sprint21h.json` e `.tsbuild-sprint10h.json` na raiz (o sandbox não consegue deletar).
