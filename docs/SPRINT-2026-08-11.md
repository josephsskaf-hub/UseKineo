# SPRINT 2026-08-11

---

# RESUMO PARA O CEO — sprint 10h

**PUSH PENDENTE — mande qualquer mensagem nesta conversa ou rode `scripts\68-PUSH.bat`**

⚠️ **Rode o `68`, não o `52` nem o `67`.** `origin/main` deixou de ser `6fcc83b`
(é `4061731`), os commits estão em `refs/heads/main`, e apareceram **3 locks
órfãos** no `.git` (`HEAD.lock`, `index.lock`, `refs/heads/main.lock`) que o
sandbox Linux não consegue apagar — só o Windows. O 68 apaga e empurra.
**9 commits represados.** O script não cria commit, não faz `git add`, não faz
`git reset`: só apaga locks e roda `git push`.

1. **O apagão do Creatomate ACABOU sozinho** — renders voltaram 11/08 02:00Z após ~34h. 7 vídeos entregues desde então, 0 falhas. O gate nº1 dos docs está vencido; não há nada a decidir sobre isso hoje.
2. **7 pessoas estavam presas fora do trial por um teto que já tinha sido estornado.** Ressuscitadas em produção. Trials ativos: 89 → 97.
3. **Causa raiz:** a ressurreição do teto era código morto — só aceitava `expired`, e o cron de downgrade (55 min) sempre vence o refund-sweep (2h35+). Nunca disparou uma vez em produção. Corrigida.
4. **Duas revisões adversariais reprovaram meu próprio código** (1ª: "NÃO COMMITAR", defeito crítico de concessão de crédito; 2ª: a correção não salvava as 7 vítimas). Ambas corrigidas antes do commit.
5. **Aquisição:** achei 52 usuários free de alta intenção (240 vídeos, 50 ativos em 14 dias) que batem na parede do free tier e **nunca receberam oferta nenhuma**. Coorte documentada, não executada — depende do item 6.
6. **Números:** 1.072 signups (15 em 24h) · 97 trials ativos · 46% de ativação · 1 conversão · 12 trials vencem hoje.
7. **Alerta:** conversão trial→pago = **1 em 109 (0,9%)**, abaixo do gatilho de 5% que você definiu. Detalhe abaixo.

---

## 1. Estado real de produção (Regra Zero — medido, não herdado dos docs)

Antes de qualquer ação, conferi o que os docs afirmavam. **Três coisas estavam desatualizadas:**

| O que o doc dizia | O que a produção diz |
|---|---|
| "GATE ATUAL — 16 COMMITS", `origin/main` = `6fcc83b` | `git ls-remote` → `origin/main` = **`4061731`**. O push rodou. Gate real: **6 commits**, não 16 nem 22. |
| "O PRODUTO ESTÁ PARADO" (gate nº1) | **Renders normalizados desde 11/08 02:00Z.** 7 vídeos em 24h, 100% de sucesso. |
| "achado de segurança `/api/render/[id]` é o item 0 da Fase 2" | **Já fechado em produção** desde `ec9f112`, conferido em 07/08. Item já entregue. |

O apagão durou de 09/08 16:21Z a 11/08 02:00Z (~34h). Não sei dizer se voltou por top-up, upgrade de plano ou reset do fornecedor — **isso segue sendo a única pergunta aberta do Creatomate**, e importa porque a aritmética do plano prevê estouro de novo em 01/09.

---

## 2. A ENTREGA DE ENGENHARIA — `KINEO-TRIAL-REVIVE-RACE-2026-08-11`

### O sintoma

6 contas com **40 créditos consumidos e ZERO vídeos entregues**. Uma sétima com 40 consumidos e 1 vídeo.

### A cadeia causal, provada por timestamps

Todas as 7 foram vítimas do apagão. Os créditos delas **foram integralmente estornados**. E mesmo assim seguiam fora do trial:

```
T+0min       render recusado pelo Creatomate → débito de 20 cr
T+~40min     segundo débito → trial_credits_used = 40 → TETO → trial_status='expired'
T+26..55min  cron trial-downgrade (roda :55) → 'expired' → 'downgraded'
T+2h35..3h36 refund-sweep (roda :30, só varre débitos >2h) → estorna os 40 cr
             → tenta ressuscitar → vê 'downgraded' → DESISTE
```

O padrão se repete nas 7, sem exceção. **A corrida é estruturalmente perdida**, não é azar: o downgrade roda em ~1h e o estorno leva 2h+ por desenho.

### O erro conceitual na raiz

O código justificava ignorar `downgraded` com a frase *"créditos já revogados"*. A premissa é falsa exatamente no caso que a função existe para atender:

> `downgradeExpiredTrial` calcula `revoke = min(saldo, granted − used)`.
> Quem morre por **teto** tem `used ≥ granted` → `unspent = 0` → **revoke = 0**.
> **Quem morre por teto não tem um único crédito revogado.**

A justificativa só valia para quem morre por *relógio* — caso que já era barrado por outra guarda. Resultado: **a ressurreição nunca disparou uma vez sequer em produção.** Era código morto desde que foi escrita.

### O que foi entregue

- `lib/reverseTrial.ts` — a ressurreição passa a aceitar `downgraded`, atrás de 6 guardas.
- `app/api/cron/trial-lifecycle-emails/route.ts` — releitura de status antes do claim do e-mail.
- `app/api/stripe/webhook/route.ts` — comentário do par corrigido (a regra dele não muda).
- `supabase/migrations/20260811140000_trial_revive_backfill.sql` — **aplicada em produção**.

### As duas revisões adversariais (e por que elas valeram a sprint)

**1ª revisão — veredito `NÃO COMMITAR`.** Defeito CRÍTICO que eu não tinha visto: o decremento de `trial_credits_used` é *incondicional*. Alargar o gate para `downgraded` fazia com que um estorno numa conta morta por **relógio** baixasse `used` — e o cron de e-mail deriva `restore = granted − used` na extensão automática. Cenário concreto: conta rebaixada com 12 créditos usados recebe estorno de 10 → `used` cai para 2 → o cron de e-mail devolve **38 créditos que a pessoa nunca teve**, reabre o trial por uma porta sem nenhuma guarda, e ainda afirma *"38 unused trial credits — gone"* para quem perdeu 28. Corrigido com early-return: em linha terminal, **ou ressuscita, ou não escreve nada**.

**2ª revisão — veredito `COMMIT COM RESSALVAS`.** Pegou o que mais importava: **a correção não salvaria as 7 vítimas.** A função apaga a linha do `trial_debit_ledger` *antes* de olhar o status; nos estornos de 09–11/08 ela apagou a linha, viu `downgraded` e desistiu. Hoje não há linha para reprocessar e `refund_render_credits` já carimbou `refunded_at` — o código novo nunca seria alcançado. **Sem o backfill SQL, o commit consertaria o futuro e abandonaria as vítimas que o motivaram.** Também pegou uma corrida no cron de e-mail que reabriria o mesmo dano por outra porta.

Outros 11 defeitos menores corrigidos: guarda lendo `granted` da linha em vez de inferir da constante · limbo permanente e mudo em linhas `expired` · CAS mantido na 3ª tentativa quando ressuscita · `cache: 'no-store'` nas leituras (mesma assinatura do bug de 07/08) · dois comentários que afirmavam fatos falsos · docstring de `TRIAL_TERMINAL_STATUSES` que agora contradizia o código.

### O reparo em produção

Migração aplicada com 6 guardas em SQL, dry-run conferido antes:

| conta | antes | depois | usado antes | usado depois | saldo | vídeos |
|---|---|---|---|---|---|---|
| `2762a44e` | downgraded | **active** | 40 | **20** | 20 | 1 |
| `a345b3dc` | downgraded | **active** | 40 | **0** | 40 | 0 |
| `d61e5818` | downgraded | **active** | 40 | **0** | 40 | 0 |
| `3328afb2` | downgraded | **active** | 40 | **0** | 40 | 0 |
| `6eb47386` | downgraded | **active** | 40 | **0** | 40 | 0 |
| `6027f63f` | downgraded | **active** | 40 | **0** | 40 | 0 |
| `c16336a8` | downgraded | **active** | 40 | **0** | 40 | 0 |

O consumo **honesto** foi preservado: `2762a44e` entregou 1 vídeo de verdade, então volta com 20 usados, não zerada. Nenhuma conta pagante tocada, nenhum relógio vencido revivido, nenhum saldo acima de 40. Estado anterior gravado em `trial_revive_backfill_20260811` com rollback pronto.

**Trials ativos: 89 → 97.**

---

## 3. AQUISIÇÃO — a coorte invisível (documento próprio: `COORTE-PAREDE-FREE-2026-08-11.md`)

Fui atrás da maior perda de ativação (139 iniciam geração, 92 completam) e a causa nº1 era o apagão — já resolvido. O que sobrou **continua acontecendo agora**: `compose_daily_free_limit` (HTTP 402), o teto de 3 Fast/24h.

**16 das 17 pessoas que bateram nessa parede não têm trial nenhum** — e todas já entregaram vídeos (uma tem 18, outra 9, outra 8). São usuários recorrentes que voltaram até esbarrar no teto. Não têm trial porque a ativação exige perfil com menos de 24h, o que está certo — mas cria o efeito de que **o grupo com maior intenção demonstrada é o único sem oferta.**

```
free, sem trial, nunca pagou, com 2+ vídeos, ativos em 30d ...... 52
  └ ativos nos últimos 14 dias .................................. 50
vídeos já produzidos por esse grupo ............................ 240
```

**Regra Zero aplicada:** conferi a UI antes de propor conserto. `GenerateClient.tsx:3472` já trata o 402 corretamente desde 09/07 (modal de upgrade, roteiro preservado, sem copy de erro falso). **Não há nada a consertar na tela.** O buraco é de alcance: o modal só fala com quem está no site naquele segundo, e ninguém volta para essas pessoas depois.

**Documentei sem executar, de propósito.** A tentação era dar trial aos 52 — seriam 52 trials instantâneos de gente engajada. Sua ordem no item 6 é explícita: *"sem trial retroativo (entram no 50% off POR E-MAIL)"*. É exatamente por ser tentador que virou linha de relatório e não ação minha.

---

## 4. Números

| métrica | valor |
|---|---|
| Signups totais | 1.072 (**15** nas últimas 24h) |
| Trials ativos | **97** (89 + 7 ressuscitados + 1 novo) |
| Trials totais já criados | 109 |
| Ativação (trial com ≥1 vídeo) | 42 de 97 = **43%** |
| Trials que nunca tentaram gerar | 44 (45%) |
| Conversões | **1** |
| Vídeos entregues em 24h | 7 (100% de sucesso) |
| Trials que vencem HOJE | 12 |
| Créditos de trial queimados no apagão | 400 debitados, **390 estornados**, queima líquida 10 |

---

## 5. ALERTAS

🔴 **Conversão trial→pago em 0,9% (1 em 109), contra o gatilho de 5% que você definiu.** Mas o número está contaminado e eu não confio nele ainda: das 19 contas que já morreram, **12 morreram por teto e 7 delas eram vítimas do apagão** — nunca receberam o produto. A leitura honesta do A/B 3d×7d só começa a valer a partir da coorte de hoje, com renders funcionando. Não recomendo mudar nada de preço ou de teto com base neste 0,9%.

🟡 **A única conversão da história nunca recebeu um vídeo.** `75f76a4c` assinou em 10/08 durante o apagão, com 12 erros de render na conta. Risco de reembolso/chargeback continua de pé — e agora os renders voltaram, então vale a pena ela receber um vídeo antes de pedir o dinheiro de volta.

🟡 **O Creatomate vai estourar de novo em 01/09** pela mesma aritmética (queima ~1.038 cr/dia, Growth 10K dura 9,6 dias). Voltou a funcionar hoje, mas não sei por quê — e sem saber a causa não dá para afirmar que está resolvido.

🟢 **Gate de tráfego pago segue fechado e agora com um argumento a mais:** não faz sentido pagar $347 no TAAFT enquanto 52 pessoas de fundo de funil não receberam uma única oferta.

---

## 6. Dívidas que este commit deixa registradas (não escondidas)

1. As 7 ressuscitadas **não são avisadas** de que o trial voltou. `trial_extended` cobre outro caso e afirmaria coisa errada. 4 delas leram "Here's what you just lost access to" — o claim foi reaberto para o e-mail voltar a funcionar na morte real, mas a mensagem corretiva não existe.
2. O painel `/admin/trial-cohort` bucketiza por `trial_status` e **contaria uma conta revivida que depois converta como conversão limpa** do experimento. O evento `trial_cap_refunded` carrega `ab_cohort_note` para permitir excluí-la; o join ainda não existe. **Ler isto antes de citar a taxa do A/B.**
3. Assimetria conhecida: se a UPDATE falhar depois do DELETE do ledger, o custo some e o revive nunca acontece, em silêncio. Reinserir sem transação abriria dupla contagem — o dano maior. Fica como está, registrado.

---

## 7. Amanhã — 3 entregas

1. **Descobrir POR QUE o Creatomate voltou** (dashboard → API Log → contador real do ciclo). Sem isso, 01/09 é uma repetição anunciada. É a única pergunta que ainda exige seu login.
2. **Fechar o join do A/B** (`trial_cap_refunded` no painel de coorte) — sem ele a métrica que decide 3d×7d está silenciosamente errada.
3. **Medir a coorte limpa**: primeira leitura de conversão com renders funcionando o dia inteiro, sobre os 12 trials que vencem hoje + os 18 de amanhã.
