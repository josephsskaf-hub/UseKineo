# SPRINT 2026-08-02 (execução ~21:00Z / 18h local — primeira sprint documentada do dia)

> Obs.: não há doc de sprint de 01/08 — o dia foi coberto pelas sessões interativas
> (fundador + CEO) que fecharam os 2 incidentes. Este doc roda o placar completo.

## 1. SAÚDE — motor VIVO e entregando (primeira recuperação verificada de verdade)

- Última hora: **0 erros, 0 usuários** afetados.
- Vídeos `completed` 24h: **32** · desde o conserto da env (01/08 01:05Z): **87 total,
  86 de usuários EXTERNOS (64 pessoas distintas)**. A recuperação passou no teste que a
  regra pós-incidente exige: vídeo completado, não silêncio.
- Win-back pós-blackout: **37 e-mails enviados (37 vítimas)** — o cron funcionou sozinho.
- Creatomate (incidente #2): sem `compose_not_ok` desde 01/08 04h. Fechado de fato.

## 2. PLACAR COMPLETO (02/08 ~21:00Z)

| Métrica | 31/07 21h | Agora | Delta |
|---|---:|---:|---|
| Perfis externos | — | **872** | |
| Cadastros 7d | 78 | **176** | +125% em 2 dias — a onda TAAFT segue |
| Cadastros 24h | 68 (recorde) | 33 | arrefeceu do pico, ainda 2º melhor nível |
| Ativados (≥1 vídeo) | — | 290 | |
| Taxa 2º vídeo | 18,9% | **20,7%** | loop próximos-3-Shorts + motor de volta |
| Baixaram 7d | — | 25 | |
| **Já compraram** | 4 | **5** | 🎉 5º comprador em 01/08 (detalhe abaixo) |
| posted_shorts | 0 | **1 (1 usuário)** | primeira linha da métrica-verdade |
| Metas (docs/METAS.md) | 4/500 | 5/500 | 1% da meta 1 |

## 3. 🚨 AUTÓPSIA DO 5º COMPRADOR — pagou e NUNCA gerou um vídeo

**emiliomontinari@gmail.com** (Itália, veio do TAAFT): cadastro 01/08 21:17Z já com
destino /api/stripe/checkout → `payment_success` $4.90 starter intro às **21:18Z**
(60 segundos entre cadastro e pagamento — compra de intenção pura, sem nunca ter
gerado nada). Depois: 20 minutos vagando generate → pricing → autopilot (viu
"not_entitled") → generate → **saiu às 21:38Z sem clicar em gerar. 0 vídeos até hoje.**

Não houve NENHUM erro — foi fluxo: o checkout success redireciona em 5s para um
/generate **vazio**, e um comprador que nunca usou o produto não tem prompt na cabeça.
Lifecycle já cobriu (nudge 01/08 22:40Z + rescue 02/08 14:00Z), sem retorno → escalada:
**rascunho pessoal do fundador no Gmail (id r-3846233472729920308)** com link 1-click
que gera o primeiro vídeo sozinho + oferta "responde com um tema que eu gero pra você".
**Gate: apertar Send.**

## 4. 🔧 BUG REAL DO DIA NO MOTOR — `fast_threw` (SyntaxError/TypeError)

8 usuários externos hoje (12h–18h, último 18:25Z) morreram em `fast_threw`:
**metade nunca mais completou vídeo nenhum** (5 de 10 vítimas/24h, incl. craftkingno1
com 0 vídeos na vida). Causa no código: no branch Fast do GenerateClient,
`await res.json()` SEM catch e fetch SEM retry — um 502/504 da Vercel (corpo HTML) vira
SyntaxError, uma piscada de rede vira TypeError, e a tentativa morre com erro genérico.
O branch cinematic tem loop de reconexão desde o #315; o Fast — grátis e de MAIOR
volume — não tinha nada.

**Fix (commitado):** retry bounded (até 2, backoff 2,5s/5s) no dispatch do Fast para
throw de rede e corpo não-parseável; respostas intencionais do servidor (incl. o 503
honesto de blackout) NUNCA são retried porque sempre carregam JSON. + 1 retry silencioso
no fetch do b-roll plan (perdê-lo degrada para queries genéricas — a regressão "menina
aleatória" que o #346 matou). Mensagem ao usuário durante retry: "Connection hiccup —
retrying automatically. Nothing was lost."

## 5. 💡 A IDEIA DA SPRINT — "FIRST WIN IN ONE CLICK" no checkout success (executada)

Nascida da autópsia do Emilio: **todo comprador futuro** passa pela página que o perdeu.
`app/checkout/success/page.tsx` agora mostra **3 tópicos virais em 1 clique** (pool
determinístico `getViralNowTopics()` importado no bundle — sem fetch, sem modo de falha,
princípio demo-nunca-morre aplicado no nascimento) que deep-linkam para o trilho
existente `create_intent=fast&prompt=...` → o vídeo COMEÇA SOZINHO. Countdown do
auto-redirect 5s→15s (o redirect pro /generate vazio era exatamente o buraco).
Telemetria: `checkout_success_topic_clicked` + `utm_source=checkout_success`.

- Métrica-alvo: % de compradores com 1º vídeo em <1h do pagamento (baseline: Emilio = 0%)
  + cliques em `checkout_success_topic_clicked`.
- Morte: 7 dias sem comprador novo passar pela página = sem dado (não morre por si).

## 6. GATES NOVOS (só o que EU não posso fazer)

1. **Send do rascunho pro Emilio** no Gmail (id r-3846233472729920308) — 30s.
2. **Rodar `scripts\14-PUSH.bat`** (3 commits: fix fast-retry, first-win, docs) — 30s.
3. (Herdado, conferir antes) Send do rascunho valos, se ainda não foi.
4. Lembrete: **Fazier launch é AMANHÃ 03/08** — fazier.com/launches/kineo.

## 7. VERIFICAÇÃO

- `tsc --noEmit` foreground 2×: **EXITCODE=0** (após cada mudança).
- EOL por blob do HEAD: GenerateClient.tsx **LF** (preservado);
  checkout/success/page.tsx **CRLF** — renormalizado byte a byte pós-edit.
- Sem locks zumbis; commits por caminho via índice /tmp + commit-tree.
- Nenhuma migration, nenhum preço tocado, nenhuma página nova de SEO.

## 8. MUDANÇAS NO PROMPT (auto-melhoria)

1. **Comprador novo ganha autópsia de timeline na primeira sprint seguinte** — o placar
   diz "5 compraram"; só a timeline mostrou que o 5º nunca gerou vídeo. `has_paid=true`
   com 0 completed = alarme vermelho de churn/refund.
2. **Erro de cliente sem http_status (SyntaxError/TypeError) = infra/rede, não lógica** —
   procurar `res.json()` sem catch e fetch sem retry no caminho de maior volume.
3. **Superfície pós-pagamento é funil de ativação, não página de agradecimento.**


---

# SPRINT 02/08 — SEÇÃO 19h local (~22:00Z)

## 1. SAÚDE + CONTINUIDADE
- Última hora: **0 erros / 0 usuários**. Vídeos completed últimas 3h: 2. Motor saudável.
- Fundador rodou o push (origin/main = `845c6b5`): fast-retry, first-win e resgate 2-4h **EM PRODUÇÃO**.
- **Emilio: e-mail ENVIADO pelo fundador às 21:35Z** (verificado in:sent, joseph@usekineo.com). Gate fechado.
- Sessões paralelas commitaram as ordens (aquisição A-F + conversão 1-6) — 7 commits locais sem push → **16-PUSH criado**.

## 2. DELTAS DO PLACAR (vs. ~21:00Z)
Perfis ext **874** (+2) · 7d 178 · 24h 32 · ativados 292 (+2) · taxa 2º vídeo 20,5% (ruído) ·
compraram **5** (=) · posted_shorts 1 (=).

## 3. FOLLOW-UPS MEDIDOS (as 4 pendências da baseline)
- **Win-back: 2 das 37 vítimas JÁ VOLTARAM e completaram vídeo** após o e-mail. O loop
  detecção→503 honesto→win-back automático fechou ponta a ponta pela primeira vez. 5,4% em <24h.
- **fast_threw pós-19Z: 0** (fix do retry em produção; 8 vítimas ontem, zero hoje à noite — cedo, mas certo).
- `checkout_success_topic_clicked`: 0 — nenhum comprador novo passou pela página ainda (sem dado, não falha).
- Emilio: 0 eventos pós-e-mail (enviado há 30 min — janela D+1).

## 4. 💡 IDEIA/EXECUÇÃO DA SPRINT — ORDEM C (AEO) COM REGRA ZERO
Spec pedia criar `/youtube-shorts-script-generator`. **Regra Zero provou que a ferramenta JÁ
EXISTE**: `/free-script-generator` (era #484), live, no sitemap, com canonical e funil pro
/generate com marcadores verbatim. Criar página nova = conteúdo duplicado + crawl diluído.
O que FALTAVA do spec e foi entregue (`ee354b7`):
- **JSON-LD `SoftwareApplication`** (price 0, `isAccessibleForFree`) — o formato que
  Bing/Copilot leem para citar ferramentas grátis. Sem aggregateRating inventado.
- **308 exact-match**: `/youtube-shorts-script-generator` e variante singular → página real.
- IndexNow: fica para PÓS-DEPLOY (próxima sprint roda `node scripts/submit-indexnow.mjs --submit`
  depois do 16-PUSH — submeter antes do deploy submeteria o HTML velho).
Métrica: referral chatgpt.com/bing + indexação Bing. Registrada em IDEIAS-EXECUTADAS.

## 5. GATES (novos/restantes)
1. **Rodar `scripts\16-PUSH.bat`** (AEO + 7 commits de docs/ordens).
2. 🗓️ **Fazier launch AMANHÃ 03/08** — fazier.com/launches/kineo.
3. (Ordem E, quando quiser) Criar conta Product Hunt p/ agendar launch ter/qua.

## 6. VERIFICAÇÃO
tsc EXITCODE=0 · diff 28 inserções apenas · page.tsx normalizado p/ LF (disco estava CRLF,
HEAD é LF) · commit via índice /tmp + commit-tree (index.lock zumbi presente, receita ok) ·
encadeamento com commit paralelo `fec3ca9` verificado no git log.

## 7. MUDANÇAS NO PROMPT
Baseline atualizada via update_scheduled_task: win-back 2/37 retornaram · Emilio ENVIADO ·
ordem C executada · pendência nova = 16-PUSH + IndexNow pós-deploy.
