# SPRINT 31/07/2026 — relatório do dia (gates juntos no fim, em GATES-ABERTOS.md)

> Contexto do dia: a sessão de fluxo de 3h (manhã, extensão viva) está documentada no fim de
> `SPRINT-2026-07-30.md` e em `GATES-ABERTOS.md`. Este arquivo começa na sprint das 10h.

---

# SPRINT 10h (10:02–11:2x)

## 1. PLACAR (Supabase, contas internas filtradas — FATO DE PRODUÇÃO 31/07 ~10h)

| Métrica | Hoje | Referência |
|---|---:|---|
| Perfis externos | **742** | — |
| Cadastros 7d | **47** | semana passada inteira: 13 |
| **Cadastros 24h** | **37** | baseline de ontem: ~30 — a onda ACELEROU |
| Semana atual (seg-sex) | **45** | 13 → 24 → 45: reversão da queda |
| Ativados (≥1 vídeo) | 228 | — |
| Taxa de 2º vídeo | **18,9%** | baseline 18,4% — loop de 29/07 ainda sem efeito visível |
| Baixaram 7d (pessoas) | 8 | — |
| Já compraram (vida) | 4 | — |
| Lifecycle desde baseline 11:08Z | nudge **+5** · reminder 0 · rescue 0 · recovery 0 | ritmo saudável, sem disparo em massa |

**A ORIGEM DA ONDA (a resposta mais importante do placar):** dos 34 cadastros externos de
48h, **32 vêm do TAAFT** — incl. o subdomínio `free.theresanaiforthat.com` (a Kineo está na
seção de ferramentas grátis deles). Geografia: IN, NG, BR, PK, VN, GH, US(3). O canal que
decaía 48→16→9→5/semana ressuscitou sozinho — e cada um desses visitantes lê **3,0 estrelas
com 2 reviews** antes de decidir. As reviews (gate nº 1) valem mais hoje do que ontem.

## 2. CLIENTE PAGANTE — diagnóstico fechado (prioridade máxima do prompt)

**A dor:** `valos87196@gouziben.com` (AU, plano basic, assinatura Stripe ativa, **75 créditos
em conta**) teve **6 renders recusados** em 29–30/07 — a Creatomate renderizava, e a entrega
era negada na branch de débito ("couldn't confirm the credits"). Depois disso, **~28 cliques
num botão Generate morto** (`analyze_blocked_active_render_gate`, snapshot zumbi) e 2
`generate_empty_prompt`. Última atividade: 30/07 12:44Z. Ele parou de tentar.

**A causa (FATO CONFIRMADO em código+banco):** `credit_debits` dele = **0 linhas na vida**.
O RPC `debit_video_credits` só levanta 'not authenticated'/'invalid cost' — a falha era do
NOSSO lado, exatamente como o comentário `KINEO-FAILURE-REASON-2026-07-30` descreve. Os
`render_jobs` dele existem (custo 1, user certo); saldo nunca foi o problema.

**A virada (EVIDÊNCIA DE PRODUÇÃO, hoje):** o fix de ontem (`premium_debit_failed` nomeado +
retry 503 no blip transitório) SUBIU com o push que você rodou hoje de manhã — e às 10:55Z
um débito real liquidou e o vídeo completou (conta interna, teste). **O caminho pago funciona
em produção.** O que falta é O CLIENTE VOLTAR: criei rascunho no Gmail (curto, honesto,
"consertamos, teste 1 clique") para você apertar Send — detalhe no gate.

**Lição de arquitetura anotada:** exceção não-única no INSERT do RPC não era capturada;
qualquer recusa a pagante precisa nascer com `cause` gravado em `events`, não só em log da
Vercel que expira.

## 3. A IDEIA DA SPRINT — EXECUTADA: a ponte pós-download "postou? cola o link"

**Por quê esta:** SPRINT 30/07 §5 nomeou a verdade do produto — *"Fazer o vídeo não é o
produto. Postar é."* — e a empresa media downloads (proxy) com **zero visibilidade de UM
Short publicado**. O upload direto (`/api/youtube/upload`) logava o `videoId` num console
que a Vercel descarta. Com a onda TAAFT viva, cada dia sem medir POST é um dia de prova
social evaporando.

**O que está no ar (código local; sobe no `8-PUSH.bat`):**

| Peça | Detalhe |
|---|---|
| Tabela `posted_shorts` | **migration JÁ APLICADA em produção** (aditiva, RLS own-only, dedupe user+youtube_id, source pasted/direct_upload) |
| `POST /api/posted-shorts` | valida shorts/watch/youtu.be, extrai ID, upsert silencioso |
| Card na tela de sucesso | depois do download/share: "Posted your Short? Drop the link 🔗" → estado 🎉 featured queue |
| Upload direto | grava sozinho em `posted_shorts` (best-effort, nunca quebra o upload) |
| Evento novo | `posted_short_submitted` |

**Métrica-alvo:** linhas em `posted_shorts` (a 1ª métrica real de ativação-de-verdade) ·
morte em 07/08. **Próximo passo natural (fila):** vitrine pública alimentada por essa tabela.

## 4. BÔNUS UX/VENDAS — revive do TAAFT ask (11 shows, 0 cliques NA VIDA)

O pedido de review in-app existia desde 14/07 e estava morto: flag de "já pedi" queimada no
SHOW (um scroll = nunca mais), gate de 2º render (só ~19% chegam lá), copy sem motivo, CTA
fantasma. Agora: flag por AÇÃO (click/dismiss; teto de 3 exibições), gate no 1º render
(momento da entrega), copy honesta ("tiny team — sua review decide quem nos acha"), botão
primário azul. Com 94% da onda vindo do TAAFT, esta superfície fala com quase todo cadastro
novo. Métrica: `taaft_review_ask_clicked` sair de **0**.

## 5. VERIFICAÇÃO

- `tsc --noEmit` foreground: **EXITCODE=0**
- Commits POR CAMINHO com índice isolado em /tmp (locks zumbis da sessão das 10:01
  contornados com escrita direta do ref — detalhe em MUDANÇAS NO PROMPT):
  - `cf13d17` PONTE POS-DOWNLOAD (4 arquivos, +219)
  - `99fa4e2` TAAFT REVIVE (+68/-39)
- `git diff origin/main..HEAD --stat` = exatamente os 4 arquivos esperados
- Migration verificada com `success:true`; RLS select/insert own-only
- REGRA ZERO honrada: 6 checagens de existência ANTES de construir (caption-pack já existia,
  llms.txt já existia, /api/facts já existia, upload YouTube já existia, TaaftReviewAsk já
  existia, refund automático já existia) — 2 ideias minhas morreram no grep, custo zero

## 5.b SEARCH CONSOLE (extensão, dados do Google até 29/07 — atrasam ~2 dias)

- 7d: **0 cliques / 160 impressões / posição média 39**. Ainda TUDO pré-reindex de ontem —
  veredito do título de marca só com dados de 01-02/08.
- Marca: **"cineo" (typo) é a consulta nº 1 — 14 impressões**, mais que "kineo" (3) +
  "compare kineo" (4) + "kineo pricing" (2). O item da fila "registrar cineo.com" ganhou
  evidência de novo (gate, custa dinheiro).
- Genéricas: cluster "how much youtube (shorts) pay" ≈ 19 impressões — as páginas SEO
  existentes rankeiam nisso (posição ~39, longe da 1ª página).
- Indexação: **55 indexadas / 43 não** — Discovered-not-indexed **21** e Crawled-not-indexed
  5. A regra "melhorar existentes, não publicar página nova" segue valendo; a ponte de hoje
  não criou página pública nenhuma.

## 7. RASCUNHO PRONTO NO GMAIL (1 clique seu)

Rascunho `Fixed — your videos will deliver now` para o pagante valos87196: curto, honesto,
promete entrega manual se falhar de novo. Está na sua caixa de rascunhos — só apertar Send.

## 6. MUDANÇAS NO PROMPT (auto-melhoria)

1. **Locks que a sandbox não apaga:** sessão travada deixa `HEAD.lock`/`main.lock` com
   "Operation not permitted" no unlink — mas ESCRITA em `.git/` funciona. Receita que
   funcionou: criar objetos com `GIT_INDEX_FILE` em /tmp + `git commit-tree` e escrever o
   ref com `echo <hash> > .git/refs/heads/main`. E o N-PUSH.bat passa a apagar TAMBÉM
   `refs\heads\main.lock`.
2. **`git update-ref` pode falhar PELA METADE** (atualiza o ref, falha no reflog do HEAD) —
   sempre conferir `git log` depois de qualquer falha de ref antes de reexecutar, senão nasce
   commit duplicado (aconteceu; corrigido reescrevendo o ref para a cadeia limpa).
3. **`origin/main` pode andar DURANTE a sprint** (o fundador rodou o 7-PUSH às ~10:40) —
   revalidar `git status -sb` antes de montar o N-PUSH seguinte.

---

# SPRINT 11h (11:02–11:3x) — 🚨 INCIDENTE: OpenAI sem créditos, produção 100% parada

## 1. O INCIDENTE — prioridade absoluta ("se algo piorou, é a prioridade do dia")

**EVIDÊNCIA DE PRODUÇÃO (logs Vercel deploy `dpl_Fq2A…`, 13:44–13:46Z + events + videos):**

- **Causa raiz:** `429 You have no credits remaining. Add credits to continue using the API`
  — a conta OpenAI zerou. Não foi deploy: último vídeo OK às 10:55Z, 3-PUSH/4-PUSH/5-PUSH
  deployaram 10:34/10:58/11:03Z e o primeiro erro é 11:07Z; o erro é idêntico antes e depois
  do 7-PUSH (13:08Z). Coincidência de horário com o 5-PUSH foi descartada pelo log.
- **Extensão do dano (desde 11:07Z):** **0 vídeos criados** · 115 `generation_stage_error` ·
  **10 pessoas externas** afetadas (5+ tentativas cegas/pessoa; nenhum pagante — valos não
  voltou) · **9 cadastros TAAFT chegaram DURANTE o blackout** e não conseguem gerar nada ·
  as DUAS demos públicas da landing (`demo-script`, `demo-hooks`) também mortas — cada
  visitante TAAFT vê falha ANTES de se cadastrar. TTS é OpenAI (`tts-1-hd`) → mesmo cenas
  prontas morreriam na voz. Cron do Autopilot (canal case study) também não gera enquanto durar.
- **Por que 3h de silêncio:** o repo tem alarme para isso na fal.ai (`lib/falAlert.ts`,
  incidente 10/07 — "$0, 502 genérico, sem e-mail") mas NUNCA teve o equivalente OpenAI.
  FATO CONFIRMADO por grep antes de construir (Regra Zero).

## 2. RESPOSTA EXECUTADA (commit `c91f0c4`, sobe no 10-PUSH)

`lib/openaiAlert.ts` — espelho do falAlert: `looksOpenAiQuotaDead()` (429 + credit/quota/
billing · `insufficient_quota` · "no credits remaining") + `alertOpenAiExhausted()` (Resend →
josephsskaf@gmail.com com link direto de recarga, throttle 30 min/instância). Wiring em
**4 rotas** (generate-script, demo-script, demo-hooks, generate-video-fast/cenas), todas
fail-open: sem quota-dead, comportamento byte a byte igual ao de antes.

UX no blackout: **503 + copy honesta** ("Kineo is at full capacity right now — the team was
automatically alerted… your free videos and credits are untouched") no lugar do 500 mudo com
"try a different prompt" — que hoje fabricou 5+ tentativas cegas por pessoa. O GenerateClient
já exibe `data.error` verbatim no ramo `!res.ok` (FATO CONFIRMADO linha ~4130) → **zero
mudança no client**. `generate-video-fast` grava reason distinto `openai_quota_dead` — a
lista de vítimas do próximo blackout (para win-back) sai de uma query.

## 3. A IDEIA DA SPRINT — "blackout que se auto-reporta e não queima a onda"

Nova (checado em IDEIAS-EXECUTADAS; falAlert cobria só fal.ai), executada 100% por mim, e é
fluxo puro: a onda TAAFT (40/24h) parou de ser queimada por erro genérico — o visitante sai
com "estão cientes, volto depois" em vez de "está quebrado". Métrica: tempo-até-detecção
(hoje 3h → meta <30 min) e fim das tentativas cegas. Registrada em IDEIAS-EXECUTADAS.

## 4. DELTAS DO PLACAR (vs 10h)

| Métrica | 10h | 11h |
|---|---:|---:|
| Cadastros 24h | 37 | **40** (+5 nos últimos 100 min — onda acelerando PARA DENTRO do blackout) |
| Lifecycle | nudge 495 | nudge 497 · rescue 221 · recovery 201 (ritmo saudável) |
| posted_shorts | 0 | 0 (esperado — código só sobe no 10-PUSH) |
| valos (pagante) | silêncio | **segue sem voltar** (último evento 30/07 12:44Z) — Send do rascunho continua crítico |

## 5. GATE ZUMBI DO GENERATE (investigação encomendada às 10h — dados fechados)

**EVIDÊNCIA DE PRODUÇÃO:** hoje 08:05–08:46Z, **34 eventos / 2 pessoas** com reason
`analyze_blocked_active_render_gate` (stage idle) — o mesmo snapshot zumbi que comeu 28
cliques do valos em 29-30/07. É BUG DISTINTO do blackout (anterior a ele). Fix não coube
nesta sprint (mexe no estado do GenerateClient, 4k linhas); fica apontado para a sprint 16h
com os dados prontos: `git grep -n "active_render" app/(dashboard)/generate/GenerateClient.tsx`
(chaves nas linhas ~158/209/1861/3146/3798).

## 6. SESSÃO PARALELA DO FUNDADOR (11:11) — registrado para o relatório das 22h

Enquanto esta sprint rodava, sessão dirigida pelo fundador via celular commitou `caf39bb`:
Reddit POSTADO (fila dos mods), Fazier free tier completo (launch 03/08), e **Stripe
rebrand** (checkout mostrava "Aestivora Media"/extrato BOOKEDCALLS.CO → agora Kineo/
USEKINEO.COM — risco de chargeback invisível eliminado). Meu commit encadeou por cima sem
conflito; o 10-PUSH sobe tudo.

## 7. VERIFICAÇÃO

- `tsc --noEmit` foreground: **EXITCODE=0**
- Armadilha de EOL PEGA ANTES do commit: o editor gravou CRLF em 3 arquivos LF; diff virou
  whole-file (178/197/482 linhas). Normalizado com `sed -i 's/\r$//'` → diff final = só os
  hunks reais (+48 em 4 rotas + lib nova). Regra "EOL igual ao HEAD" honrada.
- Commit por caminho com índice em /tmp + `commit-tree` + escrita direta do ref (locks da
  sessão 10:01 seguem lá; 10-PUSH os apaga). `git log` conferido: cadeia limpa, sem duplicata,
  em cima do `56fb34b` da sessão paralela.
- Nenhuma migration; nenhum preço tocado; nenhuma página SEO nova.

## 8. MUDANÇAS NO PROMPT (auto-melhoria desta sprint)

1. Saúde de entrega TAMBÉM em janela curta: `generation_stage_error` na última 1h logo no
   início de TODA sprint (a janela 7d escondeu um blackout total de 3h em curso).
2. Blackout de fornecedor = usar o par `looksOpenAiQuotaDead`/`alertOpenAiExhausted` — e
   qualquer fornecedor novo de IA entra no padrão falAlert/openaiAlert no dia 1.
3. Edit em arquivo deste repo: conferir `git ls-files --eol` ANTES do commit (CRLF do
   OneDrive contamina; sed normaliza).

---

# SPRINT 13h (16:02–16:2xZ) — blackout SEGUE; a resposta agora tem as duas metades

## 1. CHECAGEM DE SAÚDE (primeira coisa, janela 1h)

**O blackout OpenAI continua às 16:02Z** — 5 horas: 88 erros / 7 pessoas na última hora,
0 vídeos completados em 2h, último erro 16:01:24Z. Dano acumulado desde 11:07Z:
**15 vítimas externas / 196 erros**. Gate 00 (recarga) segue sendo o único destravamento.

## 2. O 10-PUSH FOI RODADO — verificado em produção

Deploy `dpl_HAm8f2sP…` READY às ~15:40Z (commit `d824a6b`, cadeia completa do 10-PUSH).
Provas de que o alarme está vivo: **3 eventos com reason `openai_quota_dead`** já gravados
(o reason só existe no código novo) → o e-mail 🚨 automático ao fundador disparou, e o
usuário agora vê o 503 honesto. Tempo-até-detecção do PRÓXIMO blackout: segundos.
`posted_shorts` também está no ar desde 15:40Z (0 linhas — esperado, produção parada).

## 3. A IDEIA DA SPRINT — win-back pós-blackout AUTOMÁTICO (commit `3a54522`)

O alarme consertou a detecção; faltava a RECUPERAÇÃO. Novo cron
`/api/cron/send-blackout-winback` (a cada 30 min): detecta o fim do apagão (45 min sem
marker `openai_quota_dead` + ≥1 vídeo completado depois dele) e manda **UM** e-mail honesto
por vítima — "a falha foi nossa, está consertado, seus créditos estão intactos" — sem
desconto, link direto pro /generate. Janela de vítimas: (1º marker − 6h) até o último
marker, porque o alarme pode subir NO MEIO do blackout (foi o caso hoje: morto às 11:07Z,
markers só a partir de 15:40Z — a janela de 6h captura as 15 vítimas). Dedupe por evento
`blackout_winback_sent` (7 dias). Decisões deliberadas, documentadas no arquivo: pagantes
INCLUÍDOS (é aviso de serviço, não venda), sem trava cruzada de lifecycle (desculpa não
espera atrás de nudge), fail-closed (CRON_SECRET + gate KINEO_LIFECYCLE_EMAILS_ENABLED,
que está LIGADO). Por que é fluxo/aquisição: a onda TAAFT é o tráfego mais caro que já
tivemos; queimou 15 pessoas hoje — este cron é a diferença entre "usuário perdido no dia 1"
e "usuário que volta com a melhor impressão possível de um dia ruim".
Métrica: retornos de vítimas de blackout (eventos após `blackout_winback_sent`) — morte 07/08.

## 4. DELTAS DO PLACAR (vs 11h)

| Métrica | 11h | 13h |
|---|---:|---:|
| Cadastros 24h | 40 | **45** (recorde; onda segue PARA DENTRO do blackout) |
| Vítimas blackout | 10 | **15** (196 erros) |
| Lifecycle | nudge 497 | nudge **502** · rescue 221 · recovery 201 |
| posted_shorts | — (não deployado) | **0** (no ar desde 15:40Z; produção parada) |
| valos (pagante) | sem retorno | **sem retorno** (último evento 30/07 12:44Z) — Send do rascunho segue crítico |

## 5. VERIFICAÇÃO

- `tsc --noEmit` foreground: **EXITCODE=0**
- EOL: arquivos novos LF; `vercel.json` diff = só +4 linhas (cron novo); GATES normalizado
- Commit `3a54522` por caminho (índice em /tmp + commit-tree + ref direto), cadeia limpa
  em cima do `d824a6b`; `git log` conferido, sem duplicata
- Gate zumbi do Generate: **fica para a 16h como planejado** pela sprint 11h (dados prontos
  em §5 daquela seção) — terminado e verificado > dois pela metade

## 6. MUDANÇAS NO PROMPT (auto-melhoria)

1. Playbook de blackout agora tem 2 metades permanentes: detecção (openaiAlert) +
   recuperação (send-blackout-winback). Fornecedor novo de IA = replicar AMBAS no dia 1.
2. `list_deployments` da Vercel prova push do fundador melhor que git: o commit do deploy
   READY é a verdade do que está servindo (e o sha bate com o HEAD local).
