# SPRINT 05/08/2026

> Primeira sprint do dia cria este arquivo; as seguintes ACRESCENTAM seção com a hora.

---

## ⏰ SPRINT DAS 10h (13:00–14:10Z)

### PASSO 0 — replay? **NÃO.**
`select now()` = `2026-08-05 13:01:44Z`, env = 05/08. Divergência ~0. Execução legítima.
`HEAD = origin/main = ac791ec`, nada a rebasear, nada pendente de push no início.

⚠️ **Correção de rótulo:** a sessão CEO interativa desta manhã (commits `0335945`, `b627396`,
`ac791ec`, 09:49–09:54 BRT) datou a ORDEM Q e a AUTORIZAÇÃO PERMANENTE como **06/08**. A data
real dos commits é **05/08**. O conteúdo é de hoje e vale; só o rótulo está um dia à frente.
Próximas sprints: não procurar um "SPRINT-2026-08-06.md", ele não existe.

### 🩺 SAÚDE DO MOTOR — verde
Erros das últimas 12h: 1/hora às 01h, 08h e 09h UTC. Agrupado por hora, **não é apagão**.
E os "erros" de hoje não são falha: `free_fast_limit` + `compose_daily_free_limit`, ou seja
**5 pessoas bateram o teto do plano grátis nas últimas 36h** — isso é sinal de venda, não de bug
(o cron `send-cap-hit` já cobre). Falha real de motor hoje: 1 `analyze_threw` (1 pessoa, 00:40Z).

### 📊 PLACAR (05/08 13:05Z) — deltas contra a baseline de 05/08 00:10Z

| Métrica | 00:10Z | **13:05Z** | Δ |
|---|---|---|---|
| perfis externos | 924 | **931** | +7 |
| cadastros 24h | 28 | **21** | −7 |
| cadastros 7d | 221 | **227** | +6 |
| ativados (≥1 vídeo) | 327 | **333** | +6 |
| taxa de 2º vídeo | 23,9% | **23,7%** | −0,2 pt |
| has_paid | 6 | **6** | = |
| **pagantes ATIVOS** | 3 | **3** | = |
| posted_shorts | 1 | **2** | **+1** ✅ |

`posted_shorts` dobrou (1 → 2): o **POST TO EARN** que subiu ontem teve seu primeiro uso de
usuário. Base pequena, mas é o número que destrava a IDEIA CEO do **KINEO 100** (gatilho ≥25).

Nenhum comprador novo. Nenhuma métrica piorou de forma material.

---

## 🎯 A IDEIA DA SPRINT — **O ESTUDO VIVO** (e a promessa de tempo que estava mentindo)

**Marcador:** `KINEO-LIVE-STUDY-2026-08-05`

### Como cheguei aqui (a Regra Zero matou duas ordens antes de eu escrever uma linha)

Peguei as tarefas abertas da ORDEM Q e apliquei a Regra Zero em cada uma **antes** de construir:

**Tarefa 1 — "e-mail de ativação pros ~39 de checkout sem vídeo": MORTA. Já existe e já rodou.**
Medido ao vivo, não herdado:

| O que a ordem dizia | O que o banco diz |
|---|---|
| ~39 pessoas | **9** |
| precisam de e-mail | **8 das 9 já receberam** |
| criar rota nova | `app/api/admin/send-abandon-recovery` já existe **e roda todo dia por cron** |

Sobra **1 pessoa**, que a rota diária pega sozinha amanhã. Construir qualquer coisa aqui seria
uma sprint inteira jogada fora. (É a terceira vez que "ordem do fundador ≠ fato medido" — depois
dos "39 do nudge" que eram 35 e dos "24 quentes".)

**Tarefa 4 — "página /stats pública com dados reais": MORTA COMO PÁGINA NOVA. Já existe.**
É a `/state-of-ai-shorts-2026` (KINEO-DATA-PR, 24/07), já no sitemap. Criar uma `/stats` ao lado
seria SEO novo competindo com uma página existente — exatamente o que a armadilha proíbe.

**Mas ao abri-la, achei o problema de verdade.**

### O achado: um estudo público onde os CINCO números estavam errados

A página convida a citar ("free to cite", e-mail de imprensa no rodapé) e tinha os números
chumbados no JSX em 24/07. Doze dias depois, medidos contra a mesma base:

| Publicado (24/07) | Real (05/08) | Erro |
|---|---|---|
| 568 vídeos / 206 criadores | **472 / 331** | contava contas internas |
| 99,3% de conclusão | **91,9%** | otimista |
| 48% usam Fast Mode | **89,2%** | errado por 1,9x |
| "1 em cada 4 usa premium" | **4,0%** | **errado por 6x** |
| mediana **2,30 min** / p90 3,50 | **4,2 / 6,6** | **errado por ~1,9x** |

### O que isso custava em dinheiro (não é só integridade)

O número da velocidade não morava só ali. Ele mora em **`lib/kineoFacts.ts`**, que é a fonte
única que alimenta o **`/llms.txt`** e o **`/facts`** — os arquivos que a empresa serve de
bandeja para os motores de resposta. O cabeçalho do próprio `kineoFacts` diz que o ChatGPT já
manda **4x mais tráfego que o Google inteiro**.

Ou seja: **a Kineo estava ensinando o ChatGPT e o Bing a prometer, em nome dela, metade do tempo
real de espera.** E a medida antiga vinha de uma amostra de **DOZE renders** de julho.

Isso fecha com o maior buraco do funil. Quem chega pela citação chega esperando 2 minutos, o
render leva 4,2 na mediana e 6,6 no p90 — **a pessoa vai embora antes do vídeo ficar pronto.**
Não é problema de copy nem de botão: é expectativa calibrada errada, na porta de entrada.

### O que eu construí

1. **`lib/studyStats.ts` (novo)** — os números do estudo passam a vir do banco. Três funções SQL
   (`study_volume`, `study_speed`, `study_engine_mix`), **já aplicadas em produção**, com o DDL
   versionado em `docs/MIGRACAO-STUDY-STATS.sql`. Fallback estático, guarda de sanidade em todos
   os números publicados, piso de amostra de 30, timeout de 5s, `console.error` em todo caminho
   de falha, e `react.cache` para não ler duas vezes por request.
2. **`app/state-of-ai-shorts-2026/page.tsx`** — de `force-static` chumbado para ISR de 24h.
   `dateModified` acompanha sozinho, **JSON-LD `Dataset`** novo (é o que faz um motor de resposta
   tratar a página como FONTE DE DADOS, com licença CC-BY explícita), seção nova **"How long an
   AI Short actually takes"** com mediana/p90/conclusão, e curva mensal viva.
3. **`lib/kineoFacts.ts`** — mediana 2,3→**4,2**, p90 3,5→**6,6**, faixa "2–4 min"→**"3–7 min"**,
   amostra de 12→**114 renders Fast**. Conserta `/llms.txt` e `/facts` de uma vez.
4. **Varredura de 111 ocorrências em 31 arquivos** — a faixa "2–4 minutes" estava escrita à mão
   em 31 arquivos (landing, layout, 20+ páginas de nicho, páginas de comparação). Todas agora
   dizem 3–7. **Sem isso, o site publicaria dois tempos diferentes no mesmo domínio.**

### O número inédito (o ativo de aquisição)

Ninguém nesta categoria publica distribuição de tempo de render. A página agora publica
**mediana 4,2 min · p90 6,6 min · 91,9% de conclusão, com n e metodologia**. É dado original,
citável, sob CC-BY, que se atualiza sozinho — custo marginal $0 e escala sozinho.

### 🔴 A revisão adversarial pegou 4 bloqueadores — e eu tinha posto 2 deles

Rodei subagente revisor **antes do commit** (regra de 05/08). Achados corrigidos:

1. **A guarda de sanidade só cobria volume.** Com uma janela vazia, a página publicaria
   *"0 min de mediana · 0% concluem · amostra de 0"* com `live:true`, e o zero entraria no
   JSON-LD sob licença CC-BY. Corrigido: guarda em todos os números + piso de amostra 30.
2. **Rotulagem errada:** `study_speed` media todos os motores mas `kineoFacts` rotulava
   "Fast renders". Medi: **227 de 227 tentativas da janela são Fast**, então o número não muda —
   mas travei a semântica com filtro explícito na RPC (migração `study_speed_fast_only`).
3. **Claim comparativo falso contra concorrente nomeado.** `app/alternatives/[competitor]`
   respondia *"Is Kineo faster than Revid? **For a single faceless Short, yes**"* com base no
   tempo antigo. Reescrito: agora explica a diferença de fluxo, cita o estudo, e **declara que
   não fazemos afirmação sobre quem é mais rápido** porque o concorrente não publica o dele.
4. **O SQL das RPCs não estava no repo** — ninguém conseguiria auditar se a metodologia pública
   corresponde ao banco. Versionado em `docs/MIGRACAO-STUDY-STATS.sql`.

Também aplicados: `react.cache`, timeout de 5s, `console.error` em todo fallback silencioso,
`revalidate` derivado do módulo, `toFixed(1)` no `/facts` (publicava "4.20 min", precisão que a
fonte não tem), e o fallback inteiro quando a curva mensal vier vazia.

**Um `tsc` verde não teria pego nenhum dos quatro.** A revisão pré-commit se pagou de novo.

---

## 📉 DOWNLOAD TRUTH — leitura parcial (13h de janela) e um VEREDITO

Janela desde o deploy de 05/08 00:22:42Z:

| device | clicaram | baixaram | popup barrado | blob quebrado |
|---|---|---|---|---|
| mobile | 3 | **3** | **0** | **0** |
| desktop | 2 | **2** | **0** | **0** |

**Clique → entrega = 100%, inclusive no mobile. O download NÃO quebra.**

➡️ **A Medida 5 (CTA sticky de download no mobile, 380px) está MORTA.** A hipótese que ela
atacava era "o mobile perde o arquivo"; medido, o mobile entregou 3 de 3. Não construir.
`n` é pequeno (5 pessoas), mas a leitura de 24h completa sai a partir de **06/08 00:30Z** e a
direção já é inequívoca: zero popup barrado, zero blob quebrado.

### ⚠️ E um instrumento novo que também está cego
`video_ready_viewed` tem **37 pessoas** na história inteira, contra **69 que já baixaram**. Um
denominador menor que o numerador = o evento não cobre todas as telas de download. **Ninguém
deve calcular "taxa da tela de vídeo pronto" com ele** até isso ser corrigido. Fica como ordem.

---

## 💡 IDEIA CEO — **O RELÓGIO HONESTO** (de primeiro princípio, custo $0)

Pergunta de primeiro princípio: *qual ativo nosso vira CANAL em vez de feature?*

Hoje descobri que temos um dado que **nenhum concorrente publica e todo comprador quer**: quanto
tempo de verdade demora, com distribuição e n. Toda ferramenta desta categoria anuncia "in
seconds" e nenhuma mostra um p90. É a assimetria mais barata que temos.

**A jogada:** transformar a honestidade de medição em posicionamento, não em nota de rodapé.

1. **Um placar público de tempo real de render por ferramenta** — começando pelo nosso, medido e
   atualizado sozinho (já está no ar a partir desta sprint). O convite é o gancho: *"publique o
   seu p90"*. Nenhum concorrente vai publicar, e essa recusa É a história.
2. **Por que isso traz tráfego QUALIFICADO:** quem procura "how long does AI video generation
   take" ou "is X actually fast" está comparando ferramentas com cartão na mão. É intenção de
   compra pura, e é uma pergunta que hoje não tem NENHUMA fonte de dado real na web.
3. **Por que ninguém faz:** dá trabalho (precisa instrumentar) e expõe o próprio número. É
   exatamente o critério "o que ninguém faz porque dá trabalho".
4. **O reforço:** um selo "measured, not marketed" com link para o estudo, no rodapé das páginas
   de comparação — que já são 46 e já ranqueiam.

**Métrica:** referral de chatgpt/bing + cadastros com `utm_source=state-of-ai-shorts`.
**Prazo:** 7 dias (morte em 12/08 se não mover cadastro).
**Gatilho para a fase 2 (o placar comparativo):** a página do estudo receber ≥20 sessões/semana.

*(A IDEIA CEO anterior — **O KINEO 100** — segue na fila com gatilho objetivo `posted_shorts ≥ 25`.
Hoje: **2**.)*

---

## ✅ ENTREGUE NESTA SPRINT
- `lib/studyStats.ts` (novo) + `docs/MIGRACAO-STUDY-STATS.sql` (novo)
- 2 migrações aplicadas em produção: `study_stats_functions`, `study_speed_fast_only`
- `app/state-of-ai-shorts-2026/page.tsx` reescrito (ISR + JSON-LD Dataset + seção de velocidade)
- `lib/kineoFacts.ts` com o tempo real (conserta `/llms.txt` e `/facts`)
- 111 ocorrências de "2–4 minutes" corrigidas em 31 arquivos
- Claim comparativo falso contra a Revid reescrito
- `tsc EXITCODE=0` · revisão adversarial pré-commit com 4 bloqueadores corrigidos

## 📌 PARA AS PRÓXIMAS SPRINTS, NA ORDEM
1. **Verificar o deploy** do push 44 pelo CONTEÚDO servido (`/state-of-ai-shorts-2026` deve
   mostrar "updated daily · last read" e mediana 4.2; `/llms.txt` deve dizer 3–7 minutes).
2. **15:25Z — conferir o 1º disparo do NUDGE DIÁRIO** (cron `send-credits-back`, 54 elegíveis).
   Ainda não rodou no fechamento desta sprint (13:55Z).
3. **SHORTLIST de micro-criadores** (ORDEM P item 2) — o fundador quer pagar $50–100 hoje à
   tarde. **Não foi feita nesta sprint.** É a tarefa mais urgente com prazo do dia.
4. **BOUNTY manual no Whop** Kineo Clippers (ORDEM Q item 3).
5. **06/08 00:30Z — rodar `docs/SQL-DOWNLOAD-TRUTH.sql` com 24h.** A Medida 5 já está morta pela
   leitura parcial; a leitura completa só confirma.
6. **Corrigir `video_ready_viewed`** (denominador menor que o numerador — instrumento cego).
7. Medir as janelas de 04/08 22:00–22:30Z (`video_rating_shown/submitted`, `video_rating_reason`,
   `checkout_cancel_reason`) e a conversão do COMEBACK50 (12 enviados).
8. ORDEM M (hero que vende dinheiro) · pedir GO da ORDEM L · G2 prepara (lança seg 10/08).

## 🔧 MUDANÇAS NO PROMPT
Registradas em `docs/PROMPT-DIARIO.md` e aplicadas via `update_scheduled_task`:
- **Ordem do fundador vale como PRIORIDADE, não como fato — e agora nem como TAREFA.** Duas das
  seis tarefas da ORDEM Q já estavam feitas. Antes de executar qualquer item de uma ordem:
  medir a coorte ao vivo E procurar a rota/página que já a atende.
- **Número publicado apodrece igual a lista chumbada.** Toda superfície que publica número para
  fora (estudo, /facts, /llms.txt, prova social) nasce lendo a fonte, ou nasce com data de
  validade explícita. Amostra pequena (n<30) nunca vira número público.
- **Ao corrigir um número na fonte única, grepar a faixa em texto livre no repo inteiro.**
  Corrigir `kineoFacts` e deixar 111 cópias manuais criaria duas verdades no mesmo domínio.
- **Claim comparativo contra concorrente nomeado é passivo.** Ao mexer em número de performance,
  procurar "faster than"/"better than" e conferir se a afirmação sobrevive ao número novo.

---
---

# 🕐 SPRINT DAS 11h (14:01–14:30Z) — **A SHORTLIST QUE DÁ PARA PAGAR HOJE**

**Passo 0:** `select now()` = 14:01:45Z, env = 05/08. **Não é replay.** `git log` bate com o
disco, HEAD = `796df4b`, `main` **2 commits à frente de `origin/main`** — ou seja, **o 44-PUSH
ainda não foi clicado** e a correção do estudo continua fora do ar.

**Saúde do motor:** 1 `generation_stage_error` + 1 `compose_refused` às 08h e às 09h, um por
hora, um usuário cada. Não é explosão — nada a fazer.

**Placar (14:05Z, delta sobre 00:10Z):** perfis ext **932** (+8) · 24h **18** · 7d **228** ·
ativados **334** (+7) · taxa 2º vídeo **23,7%** (era 23,9% — ruído) · has_paid 6 ·
**ATIVOS 3** · posted_shorts **2** (+1). Nada piorou.

## 🎯 A IDEIA DA SPRINT — não inventei uma: **peguei a tarefa mais urgente com prazo do dia**

O doc das 10h deixou escrito: *"SHORTLIST de micro-criadores — o fundador quer pagar $50–100 hoje
à tarde. Não foi feita nesta sprint. É a tarefa mais urgente com prazo do dia."* Ordem aberta com
prazo vence ideia inventada.

**Regra Zero primeiro:** `docs/INFLUENCERS.md` já existia (02/08) — 5 nomes, **zero contatos**,
zero números, e a régua só da oferta grátis. Não era uma lista pagável; era uma lista de nomes.
Então a tarefa não era "descobrir criadores", era **transformá-la em decisão**.

### O que eu entreguei: `docs/SHORTLIST-MICROCRIADORES-2026-08-05.md`
6 canais **medidos hoje** na página real do YouTube (sessão logada, não estimativa): inscritos,
views totais, nº de vídeos, país, data de criação e contato. Mais 1 vetado, 4 descartados, 2
rascunhos prontos no Gmail e a régua de medição.

### 🥇 A recomendação: **Conor Martin, $100** — e o motivo não é o tamanho dele
`@conormartinai`: **4,24 mil inscritos** — o menor da lista útil. Mas **1.643.968 views em 266
vídeos** (~6,2k/vídeo), e o vídeo *"Revid AI Review (after 30 days)"* fez **15.134 views**.

Quem digita "Revid AI review" no YouTube está **comparando ferramentas do nosso nicho com o cartão
na mão**. É a intenção de compra mais pura que existe, e é **busca, não feed** — o vídeo continua
entregando daqui a um ano. $100 ÷ 15k views esperadas = **$0,0066 por view qualificada**. E o
About dele diz, em texto aberto, *"For Partnerships email me at conor@conormartin.com"*.

### ⛔ O que a shortlist EVITA gastar: **Ai Titan**
`@JoinAITitan` tem **91,7 mil inscritos** — de longe o maior número da lista — e
**117.489 views TOTAIS em 34 vídeos**. Um canal com 91k inscritos reais não faz 3,4k views por
vídeo. **A audiência é comprada.** Ele seria o "melhor negócio" da lista por qualquer critério
ingênuo, e entregaria zero.

➡️ **A regra que saiu daqui: inscritos são vaidade; o filtro é views totais ÷ nº de vídeos.**
Foi ela que promoveu um canal de 4k subs e reprovou um de 91k.

### O resto da lista (detalhe no doc)
**Sanji Nai-Chien** `@Sanji_Chien` — 9,26k subs, **12,7k views/vídeo**, canal de abril/2026 com
hit de 47,6k há 11 dias, EUA. **Steffen Miro** `@OfficialSteffenMiro` — 15,7k, a faixa exata que
o fundador pediu, e já vende funil 1:1 (quem monetiza responde proposta). **Adam Del Duca** —
2,49k subs com vídeo de 46k, sem contato público. **Malva AI** — 155k subs e e-mail de negócio
aberto, grande demais para $100 (o rascunho pede a tabela dele, sem comprometer caixa).
**Aftab Khan** — 61,9k subs, **127k views/vídeo**, agência com 70+ criadores.

## 🔴 A REVISÃO ADVERSARIAL PEGOU 4 BLOQUEADORES — nos e-mails, não no código

Rodei o subagente revisor **antes de commitar**, contra os rascunhos e o doc. Achados:

1. **A produção contradizia o e-mail no momento do envio.** Os dois e-mails mandam o reviewer
   para `/state-of-ai-shorts-2026` citando mediana de 4,2 min — e a página no ar, buscada ao
   vivo, ainda diz **2,30 min** e **99,3%**, porque **o 44-PUSH não foi clicado**. `/facts` idem.
   O e-mail que vende honestidade seria, ele próprio, a prova do descuido.
2. **O doc mandava clicar um `45-PUSH.bat` que eu não tinha criado.** Criado.
3. **"3 meses de Creator grátis" não tinha COMO ser entregue.** Nenhuma rota admin escreve plano
   pago fora do pagamento; e o SQL cru daria Creator **vitalício** com crédito de 1 mês
   (`plan_expires_at` só é honrado para `autopilot_pilot`). Caminho correto: **cupom Stripe 100%
   off por 3 meses** — o checkout já resolve `?promo=`. E-mail reescrito para *"I'll send you a
   100%-off code"*; criar o cupom virou gate de 2 min do fundador.
4. **"91,9%" já não era 91,9%** (92,0% hoje, e muda sozinho a cada revalidação diária). Virou
   **"~92%"** com a página como fonte viva. *(Exatamente o defeito de "número publicado apodrece"
   que a sprint das 10h acabou de escrever no prompt — e eu repeti dentro de um e-mail.)*

### 🟡 E três correções de honestidade que deixaram o e-mail MAIS forte
- **"~930 people have used it"** era exagero. Viraram **925 contas cadastradas, 332 que
  terminaram um vídeo, 473 vídeos** — com o *"dois terços nunca terminam"* **admitido de
  propósito**, antes que ele descubra sozinho.
- **A janela de 4,2 min começa em 02/08, e o e-mail pede um teste de 30 DIAS.** Nos 30 dias
  cheios a conclusão é **55%**, por causa dos apagões de 31/07 e 01/08. O e-mail agora dá **os
  dois números** e cita os incidentes. Tirar a munição antes dela existir é mais barato que
  responder a um vídeo.
- **"Terms"** virou **"How it works"**: `/partners` tem FAQ, não termos.

### ⚠️ Risco assumido de olhos abertos
A esteira de afiliado **nunca pagou ninguém**: `affiliate_clicks`=11, `affiliate_referrals`=0,
`affiliate_commissions`=0, sem rota de payout. Estamos prometendo 40% recorrente a um reviewer
público. **Teste ponta a ponta (clique → cadastro → assinatura → comissão) antes do vídeo dele ir
ao ar** — tarefa da próxima sprint. E o UTM é **first-touch que nunca sobrescreve**, com 66% dos
cadastros já não-atribuídos: a régua de ≥10 cadastros vai **subcontar**, e a tela certa de olhar é
`/admin/funnel`, não `/admin/overview` (que lê a coluna legada).

## 💡 IDEIA CEO — **O REVIEW QUE A CONCORRÊNCIA JÁ PAGOU PARA PRODUZIR**

De primeiro princípio: *onde o público já está com dor real, e qual ativo nosso vira canal?*

Hoje ficou visível uma assimetria que ninguém está explorando. Existe um **inventário pronto e
indexado de vídeos "X AI Review (after 30 days)"** — Revid, Crayo, AutoShorts, faceless.so. Cada
um desses vídeos é uma página de comparação que **o concorrente pagou (ou provocou) para
existir**, ranqueando para uma busca feita por gente com cartão na mão. E o criador que fez o
vídeo **já provou que aceita testar ferramenta por 30 dias e falar mal quando é ruim**.

**A jogada não é "contratar influencer". É comprar a segunda posição em cada vídeo que já ranqueia
para o nome do concorrente** — pelo preço de um jantar, com o mesmo criador, no mesmo formato.

1. **Mapear os 20 canais que já publicaram review de concorrente direto** (a busca é trivial:
   `"<concorrente>" review` no YouTube, ordenado por views). Cada um custa $50–150.
2. **Comprar sempre o formato que ELE já provou**, nunca um roteiro nosso. O vídeo do Conor com
   15k views não foi sorte — foi o formato dele.
3. **Por que ninguém faz:** dá trabalho (é garimpo manual, canal por canal) e exige coragem de
   entregar o produto sem controle de roteiro. É exatamente o critério "o que ninguém faz porque
   dá trabalho".
4. **Por que é tráfego qualificado, não alcance:** ninguém assiste a "Revid AI review" por
   entretenimento. Assiste para decidir o que assinar.
5. **O composto:** cada vídeo comprado é perene e vira, ele mesmo, prova social para o próximo
   criador — "o Conor já fez, olha o resultado".

**Métrica:** cadastros com `utm_source` do criador + ativação deles (não views).
**Prazo:** 7 dias após o 1º vídeo ir ao ar. **Morte:** <10 cadastros externos = não repetir.
**Gatilho da fase 2 (comprar 3 de uma vez):** o 1º review trazer ≥1 pagante ativo.

*(As duas IDEIAS CEO anteriores seguem na fila com gatilho objetivo: **O RELÓGIO HONESTO**
— placar público de p90 por ferramenta, gatilho ≥20 sessões/semana no estudo; **O KINEO 100** —
gatilho `posted_shorts` ≥ 25, hoje **2**.)*

## ✅ ENTREGUE NESTA SPRINT
- `docs/SHORTLIST-MICROCRIADORES-2026-08-05.md` (novo) — 6 canais medidos, 1 vetado com prova,
  contatos, oferta conferida linha a linha contra o código, régua de medição com os limites dela
- **2 rascunhos prontos no Gmail** (`conor@conormartin.com`, `aimalvabusiness@gmail.com`),
  reescritos depois da revisão adversarial
- `docs/INFLUENCERS.md` atualizado — de lista de nomes a histórico, com o beco do CAPTCHA resolvido
- `scripts/45-PUSH.bat` (substitui o 44, sobe os 3 commits do dia)
- Revisão adversarial pré-commit: **4 bloqueadores + 7 ajustes**, todos aplicados antes do commit
- Sem mudança de código nesta sprint (só docs) — `tsc` não aplicável

## 📌 PARA AS PRÓXIMAS SPRINTS, NA ORDEM
1. **Conferir o push 45 e o conteúdo servido** (`/state-of-ai-shorts-2026` = 4.2 min;
   `/llms.txt` = 3–7 minutes) — e só então confirmar que os e-mails podem sair.
2. **15:25Z — o NUDGE DIÁRIO ainda não disparou** (`credits_back_sent_at` = **0** às 14:21Z).
   Conferir e reportar.
3. **Teste ponta a ponta do afiliado** antes de qualquer criador público divulgar o link.
4. **Conversão do COMEBACK50** — 12 disparados hoje, medir aberturas/resgates.
5. **Corrigir `video_ready_viewed`** (denominador menor que o numerador).
6. **06/08 00:30Z — `docs/SQL-DOWNLOAD-TRUTH.sql`** com 24h (Medida 5 já está morta).
7. ORDEM M (hero que vende dinheiro) · GO da ORDEM L · G2 prepara (lança seg 10/08).
8. Publicar **termos de verdade** em `/partners` antes do primeiro afiliado com audiência.

## 🔧 MUDANÇAS NO PROMPT
- **Rascunho de e-mail é superfície publicada — passa pela revisão adversarial igual a código.**
  Os 4 bloqueadores desta sprint estavam todos em texto, nenhum em código, e o pior deles era o
  e-mail apontar para uma página que ainda contradiz o e-mail.
- **Antes de citar uma URL nossa num e-mail que vai para fora, buscar o conteúdo SERVIDO.**
  Commit local não é produção. `main` à frente de `origin/main` significa que a promessa está no
  seu disco e a contradição está no ar.
- **Prometer benefício exige achar o MECANISMO de entrega antes do Send.** "3 meses grátis" não
  tinha rota; o caminho óbvio (SQL) entregaria vitalício. Grep pela rota que concede, não pela
  coluna que armazena.
- **Ao avaliar criador/canal/parceiro: a métrica é views totais ÷ nº de vídeos, nunca inscritos.**
  91,7k inscritos com 3,4k views por vídeo é audiência comprada, e é o que parece a melhor compra.
- **Número com decimal dentro de e-mail apodrece em 24h** se a fonte é uma página que revalida.
  Em texto que sai para fora, arredondar e apontar para a fonte viva.

---

# 🕐 SPRINT DAS 13h (16:01Z–17:0xZ) — O MOTOR ESTAVA CAINDO ENQUANTO O PLACAR PARECIA SAUDÁVEL

**Passo 0 (replay):** `select now()` = 16:01:47Z, `env` = 16:01Z, `date` = 16:01:37Z. Sem replay.
**Push 45: CLICADO.** `origin/main` == `HEAD` == `68521e2`. O estudo vivo subiu.

## 🔴 A SPRINT INTEIRA VIROU UM INCIDENTE — E ELE AINDA ESTAVA ACONTECENDO

A checagem de saúde deu **9 `generation_stage_error` numa hora só** (15h), contra "1 erro isolado
às 08h e 09h" do baseline. Não era ruído:

| hora | usuário | o que aconteceu |
|---|---|---|
| 15:30:11 | `akposb347@…` | **cadastro NOVO, `utm_source=taaft`** |
| 15:36:40 | idem | `fast_dispatch_not_ok` **504** → `failed` |
| 15:41:26 | idem | `fast_threw` TypeError |
| 15:50:38 | idem | `fast_dispatch_not_ok` **504** → `failed` |
| 15:56:11 | idem | `generate_script_threw` TypeError |
| 15:54:22 | `obasindubuisi123@…` | 3× `generate_script_threw` TypeError |
| 16:0x | +1 | mais um `fast_dispatch_not_ok` — **ainda caindo na hora de escrever isto** |

**Uma pessoa que chegou pelo canal de ~94% da nossa aquisição tentou 4 vezes em 20 minutos, no
primeiro dia dela, e falhou nas 4.** Zero vídeos completados entre 13:45Z e o fim da sprint.

## 🔬 CAUSA RAIZ — TRÊS DEFEITOS EMPILHADOS, NENHUM DELES "LÓGICA"

**1. O cliente OpenAI não tinha orçamento de tempo.** `new OpenAI({ apiKey })` usa os defaults do
SDK: **timeout de 600_000ms (10 minutos)** e `maxRetries: 2`. Um upstream lento segura a função
até o gateway da Vercel matá-la. É exatamente a assinatura medida: 504 no `generate-video-fast`
(que tem `maxDuration = 120`) e **TypeError com `http_status: null`** no cliente — TypeError sem
status não é erro de aplicação, é o `fetch` do navegador morrendo junto com a lambda.

**2. Quando a lambda é morta, o nosso `catch` NUNCA RODA.** Logo `looksOpenAiQuotaDead()`,
`alertOpenAiExhausted()` e o fallback estático foram **todos pulados**. O alarme construído
depois do incidente de 31/07 só sabe reconhecer OpenAI que **responde** "insufficient_quota" —
é **cego ao silêncio**. Créditos estavam OK, o alarme não tocou, e a conversão foi a zero.

**3. `/api/generate-script` era a ÚNICA rota OpenAI do app sem `maxDuration`** — herdava o default
curto da Vercel (~15s) fazendo **duas chamadas gpt-4o em série** (a guardrail #383b regenera uma
vez). E desde o #310 AUTO-STRUCTURE **todo prompt livre passa por ela**: a rota mais crítica do
funil era a única sem orçamento.

### E o pior: a RECUPERAÇÃO estava tão cega quanto a detecção
O cron `send-blackout-winback` procurava **`.eq(reason, 'openai_quota_dead')`**. O blackout de
hoje não escreveu esse marcador (ninguém escreveu marcador nenhum — a lambda morria antes).
O cron teria respondido `no_blackout_in_window` e as duas vítimas **nunca receberiam nada**.
O playbook inteiro de blackout estava amarrado ao sintoma específico de 31/07.

## ✅ O QUE FOI CORRIGIDO (9 arquivos, `tsc EXITCODE=0`)

1. **`lib/openai.ts`** — cliente com `timeout: 20_000, maxRetries: 1`. Um hang agora vira
   `APIConnectionTimeoutError` **dentro do nosso catch**, com a resposta ainda na nossa mão.
2. **Orçamentos por tipo de chamada**, porque um cap único quebraria o caminho do dinheiro:
   `OPENAI_TTS_TIMEOUT_MS` 55s · `OPENAI_WHISPER_TIMEOUT_MS` 60s · `OPENAI_SCRIPT_TIMEOUT_MS` 35s.
3. **`maxRetries: 0` em todo call site de longa duração** — ver "a pegadinha" abaixo.
4. **`lib/openaiAlert.ts`** — `looksOpenAiHanging()` novo, e `alertOpenAiExhausted(ctx, kind)`
   com `kind: 'quota' | 'hang'`. **O e-mail de hang NÃO manda recarregar crédito** (recarregar não
   resolve nada) — os dois modos exigem ações opostas, e alerta com receita errada custa mais
   tempo que alerta nenhum. Throttle passou a ser **por tipo**: um hang não engole mais o alerta
   de quota, que é o acionável.
5. **`app/api/generate-script/route.ts`** — `maxDuration = 60` + trata hang com 503 honesto.
6. **`app/api/generate-video-fast/route.ts`** — trata hang com 503 + `recordFastFailure('openai_hang')`.
7. **`app/api/cron/send-blackout-winback/route.ts`** — passa a ler `BLACKOUT_MARKER_REASONS`
   (`openai_quota_dead` + `openai_hang`), uma lista única onde todo sintoma novo de queda se
   registra. Detecção e recuperação não podem mais conhecer conjuntos diferentes de sintomas.

## 🧨 A PEGADINHA QUE A REVISÃO ADVERSARIAL PEGOU (e que teria mantido o bug de pé)

`maxRetries: 1` **não é o pior caso — é o dobro dele.** Em `node_modules/openai/core.js` o timeout
é **retentado** enquanto sobrar tentativa, e só depois o erro é lançado. Ou seja o pior caso real é
`timeout × (maxRetries + 1)`:

| call site | timeout × 2 | maxDuration | veredicto |
|---|---|---|---|
| `generate-script` | 35 × 2 = **70s** | 60 | **estouraria — o bug original, de novo** |
| `voiceover` | 55 × 2 = **110s** | 60 | estouraria |
| `hostVoice` (via cinematic) | 55 × 2 = **110s** | 60 | estouraria |

Corrigido com `maxRetries: 0` nesses call sites (o padrão que o `analyze-idea` já usava).
**Sem essa correção, o fix teria parecido pronto, passado no `tsc`, e reproduzido o incidente.**

Outros dois bloqueadores da mesma revisão: `lib/hollywood/hostVoice.ts` (TTS esquecido — e o
fallback dele **ressuscitaria o bug da "voz diferente em cada cena"**) e `lib/compose.ts:770`
(Whisper com o mp3 inteiro; falha **silenciosa** que vira legenda dessincronizada no caminho pago).

## 🧪 DETECTOR VALIDADO CONTRA O SDK REAL (openai@4.104.0, executado em node)

| erro | `looksOpenAiHanging` | por quê importa |
|---|---|---|
| `APIConnectionTimeoutError` | **true** | é o de hoje |
| `APIConnectionError` (ECONNRESET/DNS) | **true** | metade dos "OpenAI inalcançável" escapava |
| `APIUserAbortError` | **false** | usuário navegando não pode virar "outage" |
| ECONNRESET **do Supabase** | **false** | o `catch` do generate-script envolve a rota inteira |
| `fetch failed` não-OpenAI | **false** | idem |
| 429 quota | **false** | é do outro detector, com outra receita |

Descoberta do caminho: `err.name` é **inútil** aqui — o SDK nunca atribui `name` nas subclasses
(`APIConnectionTimeoutError.name === 'Error'`). Detectar por nome de classe é código morto;
só `instanceof` funciona.

## 💡 IDEIA DA SPRINT — **O PLAYBOOK DE QUEDA NÃO PODE CONHECER O FORNECEDOR**

Não inventei ideia nova: a que importava estava escondida no incidente. Depois de 31/07 a empresa
construiu detecção + recuperação + fallback — e amarrou **os três ao sintoma exato daquele dia**
(`insufficient_quota`). Hoje o mesmo fornecedor caiu **de outro jeito** e a máquina inteira ficou
muda: sem alarme, sem 503 honesto, sem win-back, com o usuário mais caro que temos apertando
"gerar" quatro vezes contra uma parede.

A correção estrutural é a lista `BLACKOUT_MARKER_REASONS` como **fonte única de "caiu"**, com a
regra: *todo fornecedor novo registra o sintoma dele ali, e detecção e recuperação leem do mesmo
lugar*. **Métrica:** zero blackout sem marcador — todo `generation_stage_error` em cluster tem que
ter marcador correspondente. **Morte: 12/08** se um cluster passar sem marcador.

## 🧠 IDEIA CEO — **O SEGURO CONTRA O FORNECEDOR QUE AINDA NÃO NOS TRAIU**

Primeiro princípio: em 26 dias, **três** fornecedores diferentes derrubaram o produto de **três
jeitos diferentes** — fal a $0 com 502 genérico (10/07), OpenAI sem crédito com 500 mudo (31/07),
OpenAI sem responder com 504 (hoje). Toda vez a empresa construiu o detector **do sintoma
passado**. Estamos sempre blindados contra a última queda e nus contra a próxima.

A inversão: **parar de detectar o fornecedor e passar a detectar o CLIENTE.** Um cron de 10 em 10
minutos com uma pergunta só — *"quantas pessoas distintas tiveram erro nos últimos 15 min sem
nenhum vídeo completado no período?"* Se ≥3 pessoas e 0 conclusões → é queda, **não importa de
quem**, e dispara alarme + 503 honesto + a fila de win-back. Isso teria pegado hoje às 15:41,
**15 minutos depois do primeiro erro**, sem saber nada sobre OpenAI, sobre timeout ou sobre 504.

Por que ninguém faz: exige admitir que você **não vai** antecipar o próximo modo de falha, e
monitorar o sintoma do usuário (que é ruidoso) em vez do erro do fornecedor (que é limpo e
inútil). Por que traz dinheiro: a métrica que decide não é uptime, é **cadastro que ativa** — e
uma pessoa do TAAFT queimada no primeiro dia não volta, então cada blackout não detectado é
aquisição paga jogada fora. **Gatilho: imediato** (é uma query e um cron, ambos já têm molde).
**Métrica:** tempo entre 1º erro do cluster e alarme (hoje: **nunca**). **Morte:** 14 dias sem
detectar um cluster que os detectores por fornecedor tenham perdido. Retorno÷esforço ALTO/baixo.

## 📊 PLACAR (16:01Z) — deltas vs. 14:05Z

| métrica | 14:05Z | 16:01Z | Δ |
|---|---|---|---|
| perfis externos | 932 | **933** | +1 |
| cadastros 24h | 18 | 16 | janela deslizando |
| cadastros 7d | 228 | **229** | +1 |
| ativados | 334 | **334** | **0** ⚠️ |
| taxa 2º vídeo | 23,7% | 23,7% | = |
| has_paid / **ATIVOS** | 6 / **3** | 6 / **3** | = |
| posted_shorts | 2 | 2 | = |
| baixaram 7d | — | 44 | — |

**`ativados` parado em 334 com 16 cadastros em 24h é o incidente aparecendo no placar.** Zero
vídeos completados desde 13:45Z.

## 📌 PARA AS PRÓXIMAS SPRINTS, NA ORDEM
1. **🔴 PUSH 46 É A PRIORIDADE — o incidente ainda estava ativo às 16h.** Depois do push, conferir
   pelo conteúdo servido e checar se voltaram os `completed`.
2. **Recuperar as 2 vítimas de hoje à mão.** O win-back corrigido só enxerga blackouts FUTUROS
   (hoje não há marcador no banco para ele achar). São 2 pessoas, uma delas TAAFT de primeiro dia.
3. Executar a **IDEIA CEO** (detector de cluster por usuário) — o molde de cron já existe.
4. `credits_back_sent_at` (o cron das 15:25Z já rodou — conferir se saiu).
5. Teste ponta a ponta do afiliado · conversão do COMEBACK50 · corrigir `video_ready_viewed`.
6. 06/08 00:30Z rodar `SQL-DOWNLOAD-TRUTH` · ORDEM M · GO da L · G2 (lança 10/08) · termos em `/partners`.

## 🔧 MUDANÇAS NO PROMPT
- **Erro de cliente sem `http_status` é a assinatura de LAMBDA MORTA, não de bug de lógica** — e
  quando a lambda morre o `catch` não roda, então TODO tratamento de erro da rota é ficção.
  Ao ver TypeError com `http_status: null`, procurar orçamento de tempo, não `if`.
- **Timeout de SDK é RETENTADO: o pior caso é `timeout × (maxRetries + 1)`.** Todo call site cujo
  `maxDuration` não absorve a multiplicação precisa de `maxRetries: 0` explícito.
- **Detector de queda construído depois de um incidente tende a nascer amarrado ao SINTOMA daquele
  incidente.** Ao mexer em alarme, perguntar: "e se o fornecedor cair de OUTRO jeito?" — e conferir
  se a RECUPERAÇÃO lê os mesmos marcadores que a DETECÇÃO escreve.
- **Alarme com receita errada é pior que alarme nenhum** — modos de falha que exigem ações opostas
  (recarregar crédito × esperar) precisam de mensagens diferentes e throttle separado.
- **`err.name` não identifica classe de erro do SDK OpenAI** (nunca é atribuído — sempre `'Error'`).
  Usar `instanceof`, e gatear regex de mensagem por `instanceof` para não paginar por erro de outro
  fornecedor pego num `try` largo.

---

# 🕐 SPRINT DAS 16h (19:01–20:0xZ) — **O CRON QUE EXISTE PARA O MURO NÃO LIA O MURO**

### PASSO 0 — replay? **NÃO.**
`select now()` = `2026-08-05 19:01:47Z`, bash = `19:01:35Z`, env = 05/08. Divergência ~12s.
`git log` bate com o disco. `main` está **2 commits à frente de `origin/main`** — o
**46-PUSH.bat NÃO FOI CLICADO**. O hotfix do incidente das 15h continua parado no disco.

### 🩺 SAÚDE DO MOTOR — **INCIDENTE ENCERRADO**
Último 504 (`fast_dispatch_not_ok`) às **16:42Z**. Desde então, **11 vídeos `completed`**, o mais
recente às **18:58Z** — recuperação real pelo critério do prompt (vídeo COMPLETADO depois do fim).
O motor voltou sozinho; o hotfix segue necessário para a PRÓXIMA vez, não para esta.

⚠️ **O "erro" das 19:00Z não é erro.** É `compose_daily_free_limit` / `free_fast_limit`, HTTP 402:
uma pessoa no plano free pedindo o 4º vídeo do dia. **É um evento de VENDA sendo contado como
falha de motor** — e é a query de saúde que TODAS as sprints rodam. Ver MUDANÇAS NO PROMPT.

---

## 💡 IDEIA DA SPRINT — **O SINAL DE COMPRA MAIS QUENTE QUE TEMOS ESTAVA INDO PARA O LIXO**

Puxei o fio do falso alarme das 19:00Z e ele terminou no dinheiro.

**Quem bate no muro do plano free é a pessoa mais qualificada do funil inteiro.** Não é alguém que
olhou o preço: é alguém que usou o produto três vezes hoje e pediu a quarta. Levantou a mão.

**Em toda a história do produto, 11 pessoas fizeram isso. As 11 continuam `free`, `has_paid=false`.
Conversão: 0 de 11.** Existe um cron feito exatamente para esse momento (`send-cap-hit`, Ordem 4,
roda de 30 em 30 min desde 03/08). **8 das 11 nunca receberam o e-mail.**

### A causa: dois denominadores diferentes para a mesma regra
| | o que conta | fonte |
|---|---|---|
| **O muro** (`compose/route.ts`) | **RESERVAS** (`compose_submission_claim`) | reserva, não vídeo |
| **O cron** (`send-cap-hit`) | **VÍDEOS COMPLETOS** ≥ 3 em 24h | `videos` |

Os dois discordam sempre que uma reserva não vira vídeo. **As 3 pessoas que bateram no muro nas
últimas 24h têm 2 vídeos completos cada** — abaixo do corte de 3. O cron não as perdia por azar:
era **estruturalmente cego** para elas. O e-mail nasceu para um momento que ele não conseguia ver.

### O buraco por baixo (medido, não suposto)
**27 de 231 reservas gratuitas em 7 dias (11,7%) consumiram a cota do usuário e não produziram
vídeo.** A cota é cobrada na RESERVA e não é devolvida quando o render é abandonado ou morre.

Timeline de `eziafakaego2026@gmail.com` (cadastro de hoje 16:57Z, Nigéria, veio da homepage):
| hora | o que aconteceu |
|---|---|
| 16:58:53 | reserva #1 — usuário reanalisa o roteiro e abandona → **cota queimada, zero vídeo** |
| 17:00:18 | reserva #2 → ✅ vídeo 1 |
| 18:16:23 | reserva #3 → ✅ vídeo 2 |
| 19:00:14 | 4ª tentativa → **402 `used:4 limit:3`** |

**Prometemos 3 e entregamos 2.** No primeiro dia dele. E o e-mail que ia atrás dessa pessoa
começava com *"You just made your 3rd video today — Nice run."*

### ✅ O QUE FOI FEITO (commitado, tsc EXITCODE=0, revisado adversarialmente ANTES do commit)
`app/api/cron/send-cap-hit/route.ts` — **a coorte passa a ler o muro**:
1. Fonte primária = `compose_refused` (reason `free_fast_limit`, 24h) — o registro que o próprio
   muro escreve ao devolver 402. Sem intermediário, sem reconstrução.
2. Proxy antigo mantido como fonte secundária, mas **alinhado ao muro**: `quality_mode='fast'` +
   `credits_used=0`. Sem isso, renders cinematográficos e Fast PAGOS entravam na coorte e um
   comprador com `plan='free'` recebia "você bateu no teto do grátis".
3. **Copy honesta:** *"You've used up today's free Fast previews — the cap is 3 every 24 hours."*
   Sai o "3rd video / Nice run", que era **verificável e falso** para a coorte recém-destravada
   (a pessoa abre `/history` e conta 2), e cruel para quem acabou de perder renders num apagão.
   `FREE_CAP` agora é interpolado — o número não pode divergir da regra.
4. Tetos explícitos (`MAX_PER_RUN=60`, `MAX_COHORT_IDS=300`, `.limit(2000)`, `.order`) — apagão
   de fornecedor EMPURRA gente para o muro, então a coorte pode inchar de repente.
5. `isInternalEmail` (fonte única) no lugar da cópia local, que deixava passar a irmã do fundador,
   os aliases `joseph+…` e o revisor do TAAFT.
6. **Pagante não é mais carimbado no skip** — plano é reversível, o carimbo era vitalício.

**Métrica:** `cap_hit_sent_at` → `checkout_started` no mesmo dia. Hoje: 3 enviados, 0 checkouts.
**Prazo:** a próxima rodada (19:45Z / 20:15Z) tem que pegar `eziafakaego2026` (19:00Z),
`kingtopman` (09:00Z) e `obasin471` (01:02Z) — as 3 estão dentro da janela de 24h e com
`cap_hit_sent_at` nulo. **Morte: 12/08** se 10 envios não produzirem 1 checkout.

### 🔎 O QUE A REVISÃO ADVERSARIAL PEGOU (e por que ela veio ANTES do commit)
O revisor barrou a primeira versão com **6 bloqueadores**, e o pior deles era **texto, não código**
— a copy do "3rd video". Também achou o proxy desalinhado, a ausência de teto por rodada, o
`.in('id',…)` sem limite (a coorte nova elevava o teto de ~333 para ~1333 UUIDs na query string),
e o carimbo vitalício em pagante. `tsc` estava verde nas duas versões. **`tsc` não é revisão.**

Dois achados dele ficam como dívida registrada, em outros arquivos:
- **`PAID_PLANS` diverge entre `compose/route.ts` e os crons** — `autopilot` não está no set do
  compose, então um assinante autopilot leva marca d'água e teto de 3/24h. **Mexe em dinheiro:
  não toquei.** Conferir se `autopilot` é plano pago vivo antes de alterar.
- **`send-blackout-winback` marca com EVENTO, não com coluna de `profiles`** → é invisível para
  `lib/lifecycle/suppression.ts`. A mesma pessoa pode receber o pedido de desculpas do apagão e o
  "pague para tirar o muro" na mesma hora. Vira incidente de marca no próximo blackout.

---

## 🧠 IDEIA CEO — **VENDER A COTA QUE NÓS MESMOS QUEIMAMOS**

Primeiro princípio: o plano free não é marketing, é **uma promessa numérica** — "3 por dia". Uma
promessa é um contrato, e nós a quebramos em 11,7% dos casos, silenciosamente, e depois **pedimos
dinheiro na mesma tela**. Nenhum funil sobrevive a isso, e nenhum dashboard mostra isso, porque o
usuário não reclama: ele some.

A inversão: **devolver a cota antes que a pessoa perceba que a perdeu, e usar a devolução como a
peça de venda.** Reserva sem vídeo em 30 min → devolve o slot **e** manda uma linha: *"Uma das
suas 3 gerações de hoje travou do nosso lado. Devolvemos. Ela está te esperando."* Custo: zero.

Por que isso vende mais que o e-mail de upsell: a objeção real de quem experimenta um gerador de
IA não é preço, é **"será que isso funciona?"**. O upsell responde essa pergunta com uma cobrança;
a devolução responde com uma prova. É a mesma jogada da garantia estendida — quem confia no
produto a ponto de pagar pelo próprio erro está dizendo algo que nenhum anúncio diz.

E tem um ativo escondido aqui: **hoje a única forma de saber que o render morreu é o usuário
apertar de novo e bater no muro.** Ou seja, a nossa detecção de falha é o cliente irritado. A
devolução automática transforma esse mesmo evento em contato proativo — o oposto exato.

⚠️ **Tensão que a próxima sprint precisa resolver ANTES de construir:** liberar toda reserva sem
vídeo abre abuso (gerar, abandonar, repetir = computação infinita de graça). O caminho provável é
devolver só quando a falha for **nossa** (erro de fornecedor / lambda morta), nunca quando for
abandono do usuário — e o evento que distingue os dois já existe. **Não é ideia de sprint, é
ordem: mexe em cota, então nasce com revisão adversarial e conferência de migração.**
**Gatilho:** imediato — 11,7% já está medido. **Métrica:** % de reservas free sem vídeo (hoje
11,7%) e taxa de 2º vídeo (hoje 24,3%). **Morte:** 14 dias sem mover nenhuma das duas.

---

## 📊 PLACAR (19:30Z) — deltas vs. 16:01Z

| métrica | 14:05Z | 16:01Z | **19:30Z** | Δ |
|---|---|---|---|---|
| perfis externos | 932 | 933 | **937** | +4 |
| cadastros 24h | 18 | 16 | **19** | +3 |
| cadastros 7d | 228 | 229 | **233** | +4 |
| ativados | 334 | 334 ⚠️ | **337** | **+3 — destravou** |
| taxa 2º vídeo | 23,7% | 23,7% | **24,3%** | +0,6pp |
| has_paid / **ATIVOS** | 6 / 3 | 6 / 3 | **6 / 3** | = |
| posted_shorts | 2 | 2 | **2** | = |
| baixaram 7d | — | 44 | **46** | +2 |
| `credits_back_sent_at` | 0 | 0 | **101** | ✅ cron rodou |

`ativados` saiu de 334 (parado durante o apagão) para 337. **O item 4 da fila anterior está
FECHADO:** o NUDGE DIÁRIO disparou — 101 perfis carimbados.

---

## 📌 PARA AS PRÓXIMAS SPRINTS, NA ORDEM
1. **🔴 `scripts\47-PUSH.bat` — substitui o 46, que NÃO foi clicado. Sobe 3 commits**
   (hotfix do incidente + docs + este cron). Sem o push, nada disto está no ar.
2. **Conferir a rodada do `send-cap-hit` (19:45Z / 20:15Z) DEPOIS do push** — `cap_hit_sent_at`
   das 3 pessoas e o campo `refused` no retorno do cron. Se `sent=0` com `refused=3`, a coorte
   chegou mas a supressão de 24h segurou: reconferir na rodada seguinte.
3. **A ORDEM da IDEIA CEO:** devolver cota de reserva morta (11,7%), com o corte abuso × falha
   nossa resolvido ANTES de escrever código.
4. Dívidas da revisão: `PAID_PLANS` divergente (`autopilot`) · `send-blackout-winback` invisível
   para a supressão.
5. Teste ponta a ponta do afiliado (11 cliques, 0 comissões) · conversão do COMEBACK50 ·
   corrigir `video_ready_viewed`.
6. 06/08 00:30Z rodar `SQL-DOWNLOAD-TRUTH` · ORDEM M · GO da L · G2 (lança 10/08) · termos
   de verdade em `/partners`.

## 🔧 MUDANÇAS NO PROMPT
- **A CHECAGEM DE SAÚDE CONTA PAYWALL COMO APAGÃO.** `compose_daily_free_limit` /
  `free_fast_limit` (HTTP 402) entram em `generation_stage_error` e em `compose_refused`, que são
  exatamente os dois nomes da query de saúde. **A query tem que excluir
  `metadata->>'reason' in ('compose_daily_free_limit','free_fast_limit')`** — senão um dia de boas
  vendas parece um incidente, e um incidente de verdade fica escondido no meio do ruído.
- **EVENTO DE NEGÓCIO ENFIADO NUMA MÉTRICA DE FALHA VALE OS DOIS ERROS**: inventa apagão que não
  houve **e** esconde o sinal de compra. Ao ver um "erro", ler o `reason` antes de chamar de erro.
- **CRON DE MOMENTO TEM QUE LER O REGISTRO DAQUELE MOMENTO.** Se existe evento que marca o
  instante (`compose_refused`), a coorte nasce dele — nunca de uma reconstrução por outra tabela.
  Regra prática: **se o gatilho e o cron contam coisas diferentes, o cron está errado.**
- **COTA COBRADA NA RESERVA E NÃO DEVOLVIDA É PROMESSA QUEBRADA EM SILÊNCIO.** Ao mexer em
  qualquer teto, perguntar: *o que acontece com a reserva que não vira entrega?*
- **A REVISÃO ADVERSARIAL PEGA MAIS TEXTO DO QUE CÓDIGO** — de novo. Hoje o bloqueador mais grave
  foi uma frase de e-mail (`"You just made your 3rd video today"`) **verificável e falsa** para o
  destinatário. Toda afirmação factual sobre o que o usuário fez tem que ser conferida contra o
  banco, ou reescrita como afirmação sobre a REGRA (que é sempre verdadeira).
- **NÚMERO HARDCODED EM COPY AO LADO DA CONSTANTE QUE O DEFINE** (`"3rd"` × `FREE_CAP`) — sempre
  interpolar.

### ✅ ADENDO 19:28Z — **PUSH 47 CLICADO PELO FUNDADOR DURANTE A SPRINT**
`origin/main` = `457a46f`. **O gate #1 fechou sozinho enquanto eu escrevia o relatório.** Subiu
tudo o que estava parado: estudo vivo, shortlist de micro-criadores, hotfix do incidente
OpenAI/504 **e** o cap-hit lendo o muro. Correção do item 1 da fila acima: ele está FEITO. A fila
começa agora no item 2 (conferir a rodada `:15`/`:45` do `send-cap-hit`).

### 🔬 VERIFICAÇÃO 19:50Z — A CORREÇÃO FUNCIONA, MAS A SUPRESSÃO SEGUROU AS 3. E O MOTIVO É UM BUG IRMÃO.

Esperei a rodada das 19:45Z do `send-cap-hit` (deploy do push 47 concluído às ~19:30Z) e
consultei o banco. **`cap_hit_sent_at` continua nulo nas 3 pessoas.** Não é a coorte falhando —
é a **supressão de 24h** (`lib/lifecycle/suppression.ts`), e cada caso tem explicação exata:

| pessoa | recusa | o que suprimiu | libera |
|---|---|---|---|
| `eziafakaego2026` | 19:00Z | `activation_nudge_sent_at` **19:40:47Z** | 06/08 19:40Z |
| `kingtopman` | 09:00Z | `credits_back_sent_at` 15:25Z | 06/08 15:25Z |
| `obasin471` | 01:02Z | `video_ready_sent_at` 04/08 21:40Z (22h antes) | **hoje 21:40Z** |

**Previsão checável para a sprint das 21h:** a rodada das **21:45Z** deve pegar `obasin471` — a
supressão dele expira às 21:40Z e ele é o único dos três que destrava hoje. Se `cap_hit_sent_at`
dele continuar nulo às 22h, aí sim a correção falhou e o problema é outro.

#### 🐛 O BUG IRMÃO — **`activation_nudge_sent_at` MARCA TAMBÉM QUEM FOI PULADO, E A SUPRESSÃO LÊ COMO SE FOSSE ENVIO**
`eziafakaego2026` tem 2 vídeos completos. O `send-activation-nudge` tem uma guarda explícita:
*"Already generated a video? They're activated — no nudge needed"* → `skipped++` **e carimba
`activation_nudge_sent_at` assim mesmo**. Ou seja: o carimbo das 19:40Z **não foi um e-mail
enviado, foi um e-mail NÃO enviado.** Mas `lib/lifecycle/suppression.ts` lê a coluna como "esta
pessoa recebeu algo nas últimas 24h" e bloqueou o cap-hit.

**Resultado líquido:** o sinal de compra mais quente do funil foi calado por 24 horas **por uma
mensagem que nunca existiu**. É exatamente a mesma classe de defeito que esta sprint corrigiu —
**uma coluna com dois significados (enviei × pulei) lida como se tivesse um só** — e ela está
espalhada por 8 crons, porque o padrão "carimbar no skip" é a convenção da casa.

⚠️ **Não corrigi agora de propósito:** mexer em `suppression.ts` altera o comportamento dos 8
crons de lifecycle de uma vez. Isso nasce com revisão adversarial, não no fim de uma sprint.
O caminho provável é separar `*_sent_at` de `*_skipped_at`, ou a supressão passar a ler um
registro de ENVIO (evento), não a coluna de controle. **É o item 1 da próxima sprint.**

### 🔓 20:0xZ — **AUTORIZAÇÃO NOVA DO FUNDADOR: O CLAUDE RODA O PRÓPRIO PUSH**
> *"você tem autorização pra entrar no meu computador e fazer os .bat quando você precisar,
> os .bat dessas tasks diárias."* (05/08/2026)

**O gate de push deixa de ser gate.** A partir de agora a sprint executa o push sozinha via
computer use, e só reporta. Continuam gate: pagamentos, senhas/chaves, compras — nada disso mudou.

#### O obstáculo técnico e a solução (já implementada)
A janela do `cmd` é classificada como terminal: **dá para clicar, não dá para digitar.** Todos os
`N-PUSH.bat` têm `pause` (esperam Enter) — rodá-los sozinho travaria a janela aberta para sempre.

Criado `scripts/AUTO-PUSH.bat`: **sem nenhum `pause`, roda do início ao fim e fecha com `exit`.**
É **FIXO e reutilizável** — não criar `AUTO-PUSH-2`. Ele só empurra o que já foi commitado, nunca
commita. Escreve `docs/ULTIMO-AUTO-PUSH.log` (já coberto por `*.log` no .gitignore), que a sprint
lê depois para **confirmar o desfecho do push sem depender de screenshot**.

#### ⏳ Falta 1 clique, UMA vez só
Pedi acesso ao **Explorador de Arquivos** duas vezes e o diálogo expirou (180s) sem resposta.
**Quando o diálogo "Claude quer controlar o Explorador de Arquivos" aparecer, é só aprovar** — a
aprovação fica salva na task e vale para todas as sprints seguintes. Enquanto isso não acontecer,
o push continua dependendo de clique manual.

**Os `N-PUSH.bat` numerados ficam como plano B** (se o computador estiver desligado na hora da
sprint, o push fica para a próxima — nunca bloqueia o trabalho).

---
---

# 🕙 SPRINT DAS 21h — 05/08/2026, 22:01Z–23:00Z

**PASSO 0 (replay):** `select now()` = 22:01:48Z, `env` = 22:01:39Z. Divergência de 9s.
**Não é replay.** `main` local = `bdb8704` (18:58 -03 = 21:58Z), fresquíssimo.

**⚠️ REGRA ZERO PEGOU UMA SESSÃO PARALELA:** ao chegar, `origin/main` estava em `a2d4db3` mas
`main` local já tinha **4 commits** — três deles (`91f2982`, `b2bfe3b`, `bdb8704`) são a ORDEM R /
R-v2 e a blitz de shoutouts, escritos pela sprint das 19h. A baseline do meu prompt dizia
"1 commit pendente". Estava desatualizada em 3.

## 🩺 CHECAGEM DE SAÚDE — MOTOR SAUDÁVEL

Última falha real: **16:42Z** (fim do incidente OpenAI, já encerrado). Desde então, **zero**
`generation_stage_error` que não seja paywall. Último vídeo completado 19:23Z.

### 🐛 ACHADO NOVO NA PRÓPRIA CHECAGEM DE SAÚDE — existe um TERCEIRO reason de paywall
A query de saúde do prompt exclui `compose_daily_free_limit` e `free_fast_limit`. Hoje apareceu
**`compose_resume_daily_free_limit`** (19:05:39Z) — um HTTP 402 do teto do free que **passa pelo
filtro e é contado como falha do motor**. É o mesmo defeito que a sprint das 16h corrigiu, com um
nome a mais. O filtro tem que casar por PREFIXO, não por lista fechada:

```sql
and coalesce(metadata->>'reason','') !~ '(daily_free_limit|free_fast_limit)$'
```

Lição que generaliza: **lista fechada de exceções apodrece toda vez que alguém cria uma variante
do mesmo evento.** Onde o conceito é "família de reasons", o filtro é padrão, não enumeração.

## 📊 PLACAR (22:05Z) — deltas vs. 19:30Z

| métrica | 19:30Z | **22:05Z** | Δ |
|---|---|---|---|
| perfis externos | 937 | **938** | +1 |
| cadastros 24h | 19 | **16** | −3 (janela deslizante) |
| cadastros 7d | 233 | **234** | +1 |
| ativados | 337 | **338** | +1 |
| taxa 2º vídeo | 24,3% | **24,3%** | = |
| has_paid / **ATIVOS** | 6 / 3 | **6 / 3** | = |
| posted_shorts | 2 | **2** | = |
| baixaram 7d | 46 | **47** | +1 |

---

# ✅ A PREVISÃO DA SPRINT ANTERIOR FOI COBRADA — E BATEU

A sprint das 19h deixou uma previsão checável: *"a rodada das 21:45Z deve carimbar
`cap_hit_sent_at` de `obasin471`; se não carimbar, a correção do cap-hit falhou."*

```
obasin471@gmail.com  →  cap_hit_sent_at = 2026-08-05 21:45:04.713Z  ✅
```

**A correção do cap-hit funciona ponta a ponta em produção.** Coorte, deploy, cron e envio.
O e-mail do muro do plano free saiu para a primeira pessoa desde que o cron foi consertado.

---

# 🎯 A IDEIA DA SPRINT (EXECUTADA): A COLUNA QUE TINHA DOIS SIGNIFICADOS

Item 2 da fila anterior — e **ordem aberta > ideia inventada**.

## O tamanho real do defeito (medido, não estimado)

A sprint das 19h descobriu que `eziafakaego2026` foi calado por um `activation_nudge_sent_at`
que **não era um envio, era um pulo**. Eu fui medir quantos outros existem. A regra de prova é
exata e não tem falso-positivo: **o `send-activation-nudge` só envia para quem tem ZERO vídeos.
Logo, se já existia vídeo ANTES do carimbo, aquele carimbo não pode ter sido um e-mail.**

| | |
|---|---|
| carimbos de `activation_nudge_sent_at` | **679** |
| **provadamente pulos, não envios** | **267 (39%)** |
| **pessoas caladas NAQUELE momento** por e-mail que nunca existiu | **8** |

**39% do que aquela coluna afirmava era ficção.** Não era um caso isolado — era a convenção da
casa ("carimbar no pulo para nunca reconsiderar a linha") encontrando um sistema que lê a mesma
coluna com outro significado.

## Por que explodiu só agora
`lib/lifecycle/suppression.ts` documentava esta exata propriedade como *"conhecida e aceita"*, com
uma análise que provava que a sobreposição era vazia. **A análise estava correta para os jobs que
existiam quando foi escrita.** Ela apodreceu no dia em que o `send-cap-hit` entrou na lista: ele
dispara MINUTOS depois da recusa, não 24h depois. O comentário virou uma garantia falsa sem que
ninguém tocasse nele.

> **Aprendizado que vale mais que o patch:** *"verificamos que a sobreposição é vazia"* é uma
> afirmação com **prazo de validade**, e o prazo vence quando o próximo módulo entra. Se a
> segurança de um sistema depende de uma análise de sobreposição, a correção certa não é refazer
> a análise a cada módulo novo — é **eliminar a ambiguidade que exige a análise**.

## A correção, sem migration
O pulo passa a carimbar a **época Unix** (`LIFECYCLE_SKIP_STAMP = 1970-01-01`) em vez de `now()`.
Três propriedades na mesma coluna, sem coluna nova:

1. `.is(col, null)` continua falso → **"nunca reconsiderar" preservado**;
2. a janela de 24h nunca alcança 1970 → **o pulo para de calar os outros jobs**;
3. quem abrir o banco vê `1970-01-01` e entende que aquilo é um pulo → **significado legível**.

## O segundo defeito, corrigido nos mesmos sites
Carimbo **vitalício** sobre atributo **reversível**. `send-cap-hit` já tratava isso; **`nudge`,
`video-rescue` e `send-recovery` não.** Pagante e opt-out agora **pulam sem carimbar** — senão o
dia em que a pessoa volta ao free ela já nasce queimada para sempre. Custo de não carimbar: zero
escritas; toda coorte tem janela de tempo que a fecha sozinha.

## Backfill aplicado no banco — **267 linhas**
`UPDATE ... SET activation_nudge_sent_at = <sentinela>` no recorte provado. Sentinela, e não
`NULL`, de propósito: `NULL` recolocaria essas pessoas na fila de um nudge que elas não precisam
(elas já têm vídeo). **267 bombas desarmadas.**

## 🔬 A REVISÃO ADVERSARIAL PEGOU DOIS ACHADOS QUE IAM PARA PRODUÇÃO
1. **BLOQUEADOR — o diff levava 53.800 linhas.** O working tree inteiro tinha virado CRLF (442 de
   790 arquivos) e havia **deleções staged** de arquivos da sprint das 19h (`AUTO-PUSH.bat`,
   `BLITZ-SHOUTOUTS`). Commitado por caminho, com índice novo: **exatamente 7 arquivos**.
2. **SÉRIO — `credits_back_sent_at` teria virado uma bomba nova.** Ela é a única do conjunto lida
   como **JANELA** (cooldown de 3 dias), não como flag: `if (lastSent > 0 && ...)`. E
   `Date.parse('1970-01-01') === 0` **exatamente**, então o sentinela ficaria **invisível** ali:
   "nunca mais" viraria **"toda execução, para sempre"**, em silêncio. Aquele job hoje pula sem
   carimbar (está certo), mas a regra que escrevi no módulo autorizaria alguém a quebrá-lo.
   **Proibição explícita gravada no `skipStamp.ts`.**

   > **A distinção que ficou:** *coluna lida como FLAG aceita o sentinela; coluna lida como JANELA,
   > não.* Antes de carimbar um pulo numa coluna nova, olhe como ela é **lida**, não como se chama.

Também da revisão: comentários que passaram a mentir foram corrigidos, e a supressão agora corta o
carimbo de pulo na origem (`.gte`) em vez de trazer a linha e descartar.

## ⚠️ CALIBRAGEM HONESTA — o que este push NÃO faz
Ele **não** libera os 3 casos de hoje. Só `eziafakaego2026` estava preso por um pulo, e ele
continua suprimido até 06/08 18:10Z por um `video_ready_sent_at` que foi **envio de verdade**.
`kingtopman` idem (`credits_back` 15:25Z, envio real). **O ganho não é hoje: é que 267 carimbos
falsos pararam de poder calar alguém amanhã.**

`tsc` **EXITCODE=0**. Commit `5ca669a`.

---

# 🧠 IDEIA CEO — **O CONVITE QUE NUNCA CHEGA NA HORA EM QUE VALE**

**O número que ninguém está olhando: 47 pessoas baixaram um vídeo nos últimos 7 dias.
`posted_shorts` = 2. Na história inteira.**

O funil trata `download` como sucesso. **Não é.** O produto que vendemos não é um arquivo .mp4 —
é um Short **no ar**, com views. Enquanto o vídeo fica na pasta de Downloads, o usuário não teve
prova nenhuma de que a Kineo funciona, não tem motivo para voltar, e não tem motivo para pagar.
**Entre "baixou" e "postou" mora a conversão inteira**, e é o único trecho do funil onde não
existe nenhuma alavanca ativa.

**Regra Zero aplicada antes de propor construir:** o mecanismo **já existe inteiro** —
`lib/postToEarn.ts`, `lib/postToEarnGrant.ts`, `lib/wallOfProof.ts`, `/api/posted-shorts`,
`/wall`, `WallSubmitLink.tsx`. **Não falta produto. Falta o convite chegar.** Os 13 crons do
`vercel.json` cobrem "não gerou", "gerou e não baixou", "bateu no teto", "abandonou o checkout",
"sumiu por 3 dias" — e **nenhum** cobre *"baixou e não postou"*, que é o mais quente de todos,
porque a pessoa já fez o trabalho e está a um passo do fim.

> É exatamente o padrão que o prompt já registrou: **alavanca que existe pode estar CEGA, não
> ausente.** O post-to-earn está construído e ninguém é convidado no momento em que o arquivo está
> na mão.

**Execução (cron `send-post-nudge`, ~4-24h depois do download):**
· A coorte nasce do **evento que marca o momento** — `video_downloaded` — e não de reconstrução
  por outra tabela. É a lição de hoje aplicada de nascença.
· Uma pergunta só: *"foi pro ar? cola o link"* → cai direto no fluxo do `/wall` que já existe.
· Entra na supressão de 24h e no `LIFECYCLE_SKIP_STAMP` desde o primeiro commit.
· ⚠️ **Toca concessão de crédito (`postToEarnGrant`) → mexe em cota → revisão adversarial
  obrigatória e conferência da migração no banco antes do push.**

**Métrica e prazo:** `posted_shorts` de **2 → 12** em 7 dias após subir. Não moveu, morre.
**Gatilho: imediato** — é a primeira coisa da próxima sprint, junto com a devolução de cota.

---

## 📌 PARA AS PRÓXIMAS SPRINTS, NA ORDEM
1. **`send-post-nudge`** (IDEIA CEO acima) — o convite do post-to-earn na hora do download.
   `posted_shorts` é uma das quatro que decidem e está em **2**.
2. **Corrigir a checagem de saúde do próprio prompt** para casar paywall por PREFIXO
   (`compose_resume_daily_free_limit` escapa hoje). Já registrado abaixo.
3. **Devolver cota de reserva morta (11,7%)** — o corte abuso × falha nossa resolvido ANTES do código.
4. Dívidas: `PAID_PLANS` divergente (`autopilot` fora do set do compose — **mexe em dinheiro**) ·
   `send-blackout-winback` fora da supressão · `send-video-rescue` faz `.limit(5000)` **sem
   `.order()`** (achado da revisão: passando de 5000, o Postgres devolve linhas arbitrárias e um
   elegível pode nunca ser alcançado).
5. Teste ponta a ponta do afiliado (11 cliques, 0 comissões) · conversão do COMEBACK50 ·
   corrigir `video_ready_viewed`.
6. 06/08: `SQL-DOWNLOAD-TRUTH` · ORDEM M · GO da L · G2 (lança 10/08) · termos em `/partners`.

## 🔧 MUDANÇAS NO PROMPT
- **LISTA FECHADA DE EXCEÇÕES APODRECE.** O filtro de paywall da checagem de saúde enumerava dois
  `reason` e hoje apareceu um terceiro (`compose_resume_daily_free_limit`) que passou batido e
  virou "falha do motor". Onde o conceito é *família* de eventos, o filtro casa por **padrão**,
  nunca por enumeração.
- **"VERIFICAMOS QUE A SOBREPOSIÇÃO É VAZIA" TEM PRAZO DE VALIDADE**, e ele vence quando o próximo
  módulo entra. Se a segurança do sistema depende de uma análise de sobreposição, a correção certa
  é **eliminar a ambiguidade que exige a análise**, não refazer a análise a cada módulo.
- **COLUNA LIDA COMO FLAG ACEITA SENTINELA; COLUNA LIDA COMO JANELA, NÃO.** Antes de escrever um
  valor especial numa coluna, olhar como ela é **LIDA** — `Date.parse(epoch) === 0` fez um
  sentinela ficar invisível para um `if (x > 0)` e quase transformou "nunca mais" em "para sempre".
- **CARIMBO CORRIGIDO NO CÓDIGO NÃO CORRIGE O QUE JÁ ESTÁ NO BANCO.** Toda correção de semântica de
  coluna pergunta: *existe recorte PROVÁVEL do histórico errado?* Aqui existia (267 linhas, prova
  sem falso-positivo). Sem o backfill, o patch só valeria para o futuro.
- **ANTES DE COMMITAR, `git diff --stat` × `git diff --stat --ignore-all-space`.** Se os números
  divergem muito, o working tree virou CRLF e o commit por caminho deixa de ser conveniência para
  virar obrigação.

---

## 🔴 ADENDO 22:5xZ — **O PLANO DO AUTO-PUSH ESTAVA APOIADO NUM DIAGNOSTICO ERRADO**

Tentei rodar o push sozinho, como autorizado. O `request_access` devolveu, em texto:

> *"Computer-use access can't be approved **during a scheduled run**. To grant it, send a message
> in this conversation, or **add the app to the scheduled task's settings**."*

**Não é o timeout de 180s que a sprint das 19h registrou, e não é o fundador demorando a clicar:
uma sprint agendada não consegue nem levantar o diálogo de aprovação.** O diálogo nunca apareceu.
A conclusão anterior ("é só aprovar quando aparecer") descrevia um evento que não pode acontecer.

> **Lição, e ela é a mesma do resto do dia:** *timeout* foi lido como "ninguém respondeu" quando
> significava "a pergunta nunca foi feita". Sintoma idêntico, causa oposta — e a diferença decide
> se a ação certa é esperar ou mudar de caminho. **Antes de atribuir uma falha à outra ponta,
> confirmar que a mensagem chegou lá.**

**O que resolve de verdade (só o fundador pode, uma vez só):** adicionar **"Explorador de
Arquivos"** nas **configurações da tarefa agendada** `kineo-sprint-diario`. `update_scheduled_task`
não expõe esse campo — é pela interface da tarefa. Feito isso, o `AUTO-PUSH.bat` volta a ser o
plano A e o push deixa de ser gate para valer.

**Plano B de hoje: `scripts\\49-PUSH.bat`** (com `pause`, para clique manual) — **6 commits**.

### ✅ 23:0xZ — O CAMINHO EXATO PARA DESTRAVAR O PUSH DE VEZ (veio da propria plataforma)

Ao atualizar a tarefa agendada, o sistema devolveu a regra que faltava:

> *"Tool approvals granted during a run are stored on the task and auto-applied to future runs.
> If this task is likely to use remote connectors or browser control, recommend the user click
> **'Run now'** first to pre-approve the tools it needs — this prevents future runs from pausing
> on permission prompts."*

**Ou seja: aprovacao dada numa execucao INTERATIVA fica guardada NA TAREFA e vale para todas as
execucoes agendadas seguintes.** O que o fundador precisa fazer, **uma vez so**:

1. Abrir a tarefa `kineo-sprint-diario` e clicar **"Run now"** (execucao interativa).
2. Quando aparecer o cartao pedindo acesso ao **Explorador de Arquivos**, **aprovar**.
3. Pronto. A partir dai as 6 sprints diarias rodam o `scripts\AUTO-PUSH.bat` sozinhas e o push
   deixa de ser gate para sempre.

E menos trabalho que clicar um `N-PUSH.bat` por dia, e resolve permanentemente.

---
---

# ✅ VERIFICAÇÃO PÓS-PUSH (22:35Z–23:00Z) — O FUNDADOR PUSHOU DURANTE A SPRINT

`origin/main` = `90dbaa1` = HEAD local. **8 commits no ar, 0 pendentes.** Deploy
`dpl_4QEnGW33uoRjRb8mMv2jdEnvWsLx` **READY em produção às ~22:35Z**. O gate do push fechou.

## 🔬 A CORREÇÃO ESTÁ ESCREVENDO O SENTINELA EM PRODUÇÃO — PROVADO

Esperei a rodada das **22:40Z** do `send-activation-nudge` e consultei o banco:

| | 22:37Z (pré-rodada) | **22:46Z (pós-rodada)** |
|---|---|---|
| `activation_nudge_sent_at` = sentinela (1970) | 267 | **268** ✅ |
| `activation_nudge_sent_at` = envio real | 412 | **412** (parado) |

**O incremento veio pelo caminho do PULO e escreveu 1970, não `now()`.** É a prova de ponta a
ponta: código em produção, cron rodou, coluna carimbada com o significado certo. Antes de hoje,
aquele mesmo pulo teria calado a pessoa por 24h.

## ⚠️ UM ALARME MEU QUE A MEDIÇÃO MATOU — E EU DEVIA TER MEDIDO ANTES DE ME ASSUSTAR

Vi que a coorte do `send-cap-hit` filtra **um único** `reason` (`compose_refused` +
`free_fast_limit`) enquanto o muro emite **três** variantes, e a das 19:05Z de
`freeworkspace800` era uma das outras duas. Parecia o defeito do dia outra vez, uma camada mais
fundo: *lista fechada de exceções*, agora do lado da coorte.

**Fui medir antes de escrever a correção, e não existe buraco:**

| pessoas que já bateram no muro na história | **12** |
|---|---|
| que o cron enxerga | **12** |
| **só no suposto ponto cego** | **0** |

As outras duas variantes (`compose_daily_free_limit`, `compose_resume_daily_free_limit`) são
**eventos companheiros da MESMA recusa**, emitidos junto com o `compose_refused`, não coortes
separadas. **Nenhuma pessoa existe só neles.** A correção que eu ia escrever não teria consertado
nada e teria mexido no funil de dinheiro à toa.

> **Lição, e ela vale mais que o susto:** a mesma disciplina que aplico às ordens do fundador
> ("ordem ≠ fato medido") vale para o **meu próprio alarme**. Assinatura de bug conhecida em código
> novo é HIPÓTESE. Custou uma query descobrir que o buraco não existia — e o hábito de medir antes
> de construir acabou de se pagar contra mim mesmo.

## 🔎 DUAS COISAS QUE FICAM ABERTAS, DITAS COMO SÃO

**1. `obasin471` foi carimbado DUAS vezes: 21:45:04Z e 22:45:26Z.** O carimbo do meio da tarde
virou nulo em algum momento entre 22:02Z (li 21:45:04Z) e 22:45Z, e a rodada seguinte o
reselecionou e reenviou. **Não existe caminho no código que zere essa coluna** — conferido em
`app/`, `lib/`, `scripts/`, `supabase/`, `.sql` e `.mjs`: só há dois escritores (pulo → sentinela,
envio → `now()`) e um filtro `.is(col, null)`. Ou seja, o `NULL` veio **de fora da aplicação**
(SQL manual / sessão paralela, provavelmente para retestar o e-mail). Efeito: ele provavelmente
recebeu o mesmo e-mail duas vezes, com uma hora de intervalo. Não é grave, mas está registrado —
e **se ninguém mexeu no banco à mão, então existe um escritor que eu não encontrei, e isso é
prioridade da próxima sprint.**

**2. 📌 PREVISÃO CHECÁVEL PARA A PRÓXIMA SPRINT — e é o teste limpo de toda a correção de hoje:**

`freeworkspace800@gmail.com` bateu no muro às **19:05:38Z**, está no plano free, não optou por
sair, **está na coorte visível do cron**, e **nada o suprime**: a única coluna carimbada dele é
`activation_nudge_sent_at = 1970-01-01` — exatamente um dos 267 carimbos falsos que o backfill de
hoje desarmou. Antes de hoje ele estaria calado até 06/08.

> **A rodada das 23:15Z (ou a das 23:45Z) do `send-cap-hit` DEVE carimbar
> `cap_hit_sent_at` de `freeworkspace800@gmail.com`.**
> Se carimbar: a correção não só está no ar, ela **devolveu ao funil uma pessoa que o bug tinha
> tirado** — que é o objetivo inteiro.
> Se NÃO carimbar até 00:15Z: o diagnóstico está incompleto e a próxima sprint começa por aqui,
> olhando o payload do cron (`sent` × `skipped` × `suppressed` × `refused`).

---

## ❌ 23:16Z — **A PREVISÃO FALHOU, E A FALHA VALE MAIS QUE O ACERTO**

Esperei a rodada das **23:15Z**. `freeworkspace800@gmail.com` continua com `cap_hit_sent_at`
**NULO**. A previsão que eu registrei há 20 minutos **não bateu** — e, como estava escrito, isso
significa que o diagnóstico está incompleto.

E a rodada revelou o fato que muda a investigação:

| pessoa | 21:45Z | 22:45Z | 23:15Z |
|---|---|---|---|
| `obasin471` | carimbado | **carimbado de novo** | **carimbado de novo** |
| `freeworkspace800` | — | — | — |
| `eziafakaego2026` | — | — | — |
| `kingtopman` | — | — | — |

**`obasin471` foi carimbado TRÊS vezes.** Para ser reselecionado, a coluna dele precisa estar
**NULA no instante de cada rodada** — o filtro da coorte é `.is('cap_hit_sent_at', null)`. Algo
zera aquele campo entre as rodadas. Não é "o fundador mexeu à mão uma vez", como escrevi antes:
acontece de forma **recorrente e regular**.

### O que já foi ELIMINADO (não repetir na próxima sprint)
- **Trigger no banco: inocente.** Existe um `enforce_profile_client_guard` BEFORE UPDATE em
  `profiles`, mas ele (a) não menciona nenhuma coluna `*_sent_at` e (b) faz `return new` logo de
  cara para `service_role`, que é o papel dos crons. Lido linha a linha.
- **Código da aplicação: inocente.** `cap_hit_sent_at` tem exatamente dois escritores
  (`LIFECYCLE_SKIP_STAMP` no pulo, `now()` no envio) e nenhum caminho que escreva `NULL` —
  conferido em `app/`, `lib/`, `scripts/`, `supabase/`, `.sql`, `.mjs`.
- **Coorte: inocente.** Os 4 aparecem na coorte; 3 deles com `entra_na_query = true`.
- **Supressão explica 2 dos 3 que não receberam:** `eziafakaego2026` (`video_ready` 18:10Z, envio
  REAL) e `kingtopman` (`credits_back` 15:25Z, envio REAL). Corretos, liberam amanhã.

### A pergunta que sobra, e ela é uma só
**Por que `freeworkspace800` — na coorte, sem nada que o suprima, plano free, não opt-out —
é selecionado a cada rodada e nunca carimbado?**

No código só existe UM caminho que seleciona alguém e não carimba: **o envio pelo Resend voltar
`res.ok === false`**. Nesse caso o job loga o erro e **não carimba de propósito**, para retentar na
rodada seguinte — exatamente o comportamento observado, rodada após rodada.

> **Hipótese principal para a próxima sprint (checável em 5 minutos):** o Resend está **recusando
> o endereço** dele (bounce anterior, lista de supressão do próprio Resend, domínio bloqueado).
> Se for isso, o defeito não é nosso cron — é que **uma recusa permanente do provedor vira uma
> tentativa infinita e silenciosa**, sem alarme e sem desistência. É a mesma família de defeito do
> dia: *um estado que o sistema não sabe nomear*.
>
> **Como confirmar:** logs da Vercel do deploy `dpl_4QEnGW33uoRjRb8mMv2jdEnvWsLx`, escopo ≤1h,
> procurar `[send-cap-hit] resend failed for`. O corpo da resposta do Resend dá o motivo exato.
> E o painel do Resend, logado, mostra o histórico daquele endereço.

**Isto é o item 1 da próxima sprint**, à frente do `send-post-nudge`: é o mesmo funil de compra, é
uma pessoa quente esperando desde as 19:05Z, e o `obasin471` sendo reenviado de hora em hora é um
risco de queimar domínio.

### O que continua verdadeiro e verificado, apesar disso
A correção desta sprint **está no ar e funcionando**: a rodada das 22:40Z pulou alguém e escreveu
**1970**, não `now()` (sentinela 267 → 268, envios reais parados em 412). Os 267 carimbos falsos
foram desarmados. Isso é independente do problema acima — que, agora está claro, **sempre existiu
e não tinha nada a ver com o carimbo de pulo**. O bug de hoje escondia outro atrás dele.

---

## 🔬 23:20Z–23:35Z — OS LOGS DA VERCEL DERRUBARAM A MINHA HIPÓTESE. O QUE SOBRA É PIOR.

Puxei os logs do cron. As duas rodadas dizem exatamente a mesma coisa:

```
23:15:26 GET /api/cron/send-cap-hit 200
  [lifecycle-suppression] 3/4 suprimido(s) — e-mail de ciclo de vida nas últimas 24h
  [send-cap-hit] sent to obasin471@gmail.com
22:45:26 GET /api/cron/send-cap-hit 200   (idem)
```

**Não é o Resend.** O e-mail sai com `res.ok` verdadeiro, o job loga `sent`. Minha hipótese das
23:16Z estava errada, e o log matou em duas linhas.

### O fato que sobra, e ele é estranho

| | o CRON vê | eu vejo no banco |
|---|---|---|
| candidatos na coorte | **4** | **3** |
| `obasin471.cap_hit_sent_at` | **NULL** (senão não seria selecionado) | `23:15:26` — estável, reli às 23:22Z |
| `freeworkspace800` | **suprimido** | **nada o suprime** (único carimbo = `1970`) |

**As duas anomalias têm a MESMA forma: o cron enxerga uma versão mais VELHA da tabela `profiles`
do que a minha consulta.** Para ele, o carimbo do `obasin471` ainda não existe, e o
`activation_nudge_sent_at` do `freeworkspace800` ainda é o timestamp real de antes do backfill
das 22:20Z — que suprimiria mesmo.

### O que foi ELIMINADO com evidência (não repetir)
- **Trigger do banco** — `enforce_profile_client_guard` lido linha a linha: não menciona nenhuma
  coluna `*_sent_at` e faz `return new` imediato para `service_role`.
- **Código da aplicação** — dois escritores apenas, nenhum escreve `NULL`.
- **Linha duplicada** — `lower(btrim(email))` retorna **1** linha. Some espaço, some caixa alta.
- **Réplica de leitura** — projeto único, `us-east-1`, `ACTIVE_HEALTHY`, sem branches.
- **Outro cron sobrescrevendo** — `reset-cinematic-tokens` (o único que atualiza `profiles` fora
  dos jobs de e-mail) escreve só `{ cinematic_tokens: 1 }`.
- **Coorte / lista fechada de `reason`** — medido: as 12 pessoas do muro estão todas visíveis.

### 📌 O EXPERIMENTO QUE DECIDE (item 1 da próxima sprint — instrumentar, não adivinhar)
Parei de formular hipóteses porque já formulei duas erradas hoje, e as duas custaram tempo. O
caminho certo é fazer o cron **dizer o que ele leu**:

1. No laço do `send-cap-hit`, logar por candidato o **valor cru** de `cap_hit_sent_at` que veio no
   `select` (`console.log(email, u.cap_hit_sent_at)`).
2. **Checar o erro do `.update()`** — hoje ele é ignorado nos 5 crons (`await admin...update(...)`
   sem olhar `error`). Se a escrita falha em silêncio, ninguém sabe.
3. Ler o valor de volta logo após escrever e logar os dois.

Uma rodada responde. Se o log mostrar `NULL`, é leitura velha/cache e o alvo é a configuração do
cliente Supabase do cron. Se mostrar o timestamp, o filtro `.is(col, null)` do PostgREST não está
fazendo o que o código diz — e aí o problema é maior e vale para TODOS os crons de lifecycle.

### Impacto de negócio, dito sem enfeite
- `obasin471` recebeu **3 e-mails iguais em 90 minutos**. Risco de reputação de domínio.
- `freeworkspace800` bateu no muro às 19:05Z, é a pessoa mais quente do funil, e **continua sem
  receber há 4h+** por uma supressão que os dados dizem não existir.
- Isto **não** foi causado pela correção de hoje: o primeiro reenvio foi às 21:45Z→22:45Z, antes do
  deploy das 22:35Z. **O bug de hoje estava escondendo este.**
