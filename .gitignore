node_modules
node_modules/
.next/
.env.local
.vercel/
RODAR-DEPLOY.bat
CORRIGIR-E-PUSH.bat
RODAR-DEPLOY.bat
CORRIGIR-E-PUSH.bat
tsconfig.tsbuildinfo
# 16/08 — o tsconfig escopado da regra 3 gera <nome>.tsbuildinfo junto, e o
# OneDrive recusa o unlink dele (`rm` -> "Operation not permitted"), entao o
# arquivo SOBREVIVE a sprint que o criou. `tsconfig.*.json` acima ja cobre o
# config; faltava o buildinfo, que aparecia como `??` no status seguinte.
*.tsbuildinfo
# KINEO-BROLL-GC-2026-08-13 — tsconfigs escopados de sprint.
# A regra 3 do PROMPT-DIARIO manda checar tipos com um tsconfig que ESTENDE o do
# projeto e escopa o `include` no arquivo alterado (o `tsc` do projeto inteiro
# nao termina neste shell). Cada sprint cria o seu — e o OneDrive recusa o
# unlink, entao nenhuma consegue apagar o proprio: eram 10 arquivos soltos na
# raiz em 13/08. O padrao exige DOIS pontos, entao `tsconfig.json` (rastreado)
# nao e alcancado; so `tsconfig.<nome>.json` e.
tsconfig.*.json
*.log
.claude/
# KINEO-PUSH-AUTONOMO-2026-08-31 — arquivo de credencial do git que o fundador
# cria UMA vez para o Claude conseguir dar push sozinho (ver
# docs/PUSH-SEM-O-FUNDADOR-2026-08-31.md). NUNCA pode ser commitado.
.kineo-push-credentials
.kineo-push-credentials.*
